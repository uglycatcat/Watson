---
description: 项目通用代码风格与协作红线，始终生效
alwaysApply: true
---

# 代码风格约束

本项目遵循根目录 `coding_style.md` 的全部规则，请在生成/修改任何代码前阅读并严格遵守，重点：

- **最小改动**：只改与当前需求直接相关的代码，不顺手重构无关部分。
- **改前先说明**：较大改动先说清改哪些文件、为什么，得到确认再动手。
- **不确定就问**：接口/字段/业务规则有疑问先提问，不脑补假设。
- **复用优先**：优先复用已有函数/模块，新增内容尽量少。
- **如实交付**：改完实际运行验证并给出输出/日志/截图，跑不通就说跑不通，禁止伪造结果。
- **保留报错**：贴完整错误信息，不吞掉异常。
- **密钥外置**：Key/Token/地址/路径一律进 config/env，不硬编码；配置文件不入版本控制（见 `.gitignore`）。
- **注释**：新项目默认中文注释，标识符用英文；函数上方注释职责/入参/返回/副作用。

完整规则见 `coding_style.md`。本项目使用 GitHub Spec Kit 进行规格驱动开发（Spec-Driven Development），
工作流：/speckit-constitution → /speckit-specify → /speckit-plan → /speckit-tasks → /speckit-implement。
