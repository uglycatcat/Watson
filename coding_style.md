# 代码风格与协作约束 (Coding Style & Collaboration Rules)

> 本文档用于在 vibe coding 项目中约束 AI 及其他开发者的代码开发行为。
> 它是**语言无关的通用规范**,适用于 ROS / Node.js / C 嵌入式 / Python 等不同技术栈。
>
> **总原则:服从当前项目和所用语言的既有惯例,并与周围代码保持局部一致。**
> 当本文档与项目内已有的、明确的规范(如 `.clang-format`、`.eslintrc`、`.pre-commit-config.yaml`、
> 官方 style guide)冲突时,**以项目既有规范为准**,本文档退居其次。

---

## 一、行为红线 (Behavioral Rules)

这些是协作时最优先遵守的硬性要求。

### 必须 (Do)
1. **最小改动**:只修改与当前需求直接相关的代码。不要顺手"优化""重构"无关部分。
2. **改前先说明**:动手前先说清楚"改哪几个文件、为什么这么改";较大改动需先得到确认再执行。
3. **不确定就问**:对接口、字段名、业务规则、预期行为等有疑问时,先提问,不要自行脑补假设。
4. **复用优先**:实现新需求时优先复用已有函数/模块/组件;新增内容应尽可能少,以提高复用率、降低冗余。
5. **如实交付**:改完必须实际运行验证,并给出运行输出 / 日志 / 截图作为证据;跑不通就说跑不通。
6. **保留报错**:出错时贴出完整错误信息,不要吞掉、截断或掩盖异常。

### 禁止 (Don't)
1. **禁止伪造结果**:不得用编造的数据、假的输出、想象的返回值冒充真实运行结果。
2. **禁止擅自扩大范围**:不要在未说明的情况下改动、删除无关代码、注释或 TODO。
3. **禁止删除他人产物**:不要删掉已有的注释、TODO、FIXME 及被注释掉的代码,除非明确要求。
4. **禁止硬编码敏感信息**:密钥、Token、地址、路径等不得写死在源码里(详见结构章节)。
5. **禁止悄悄换技术方案**:不要在未说明的情况下替换库、框架、算法或数据结构。

---

## 二、注释规范 (Comments)

> 注释语言:**跟随项目现有注释语言**;新项目默认用中文注释,但标识符(变量/函数/文件名)一律用英文。

### 关键变量
1. **关键的全局变量 / 配置项 / 常量**,其定义上方应有注释,说明其**用途**及**取值含义**(尤其是单位、范围、约束)。

### 函数 / 方法
1. 每个函数在其实现上方都应有注释,内容至少覆盖:
   - **做什么**(一句话职责)
   - **关键入参**的含义(非自解释的参数)
   - **返回值**的含义
   - **副作用**(修改全局状态、IO、阻塞、抛异常等),如有必须标注
2. 优先使用**语言/生态的标准注释格式**:
   - C / C++:Doxygen (`/** ... */`)
   - Python:docstring (`"""..."""`)
   - JS/TS:JSDoc (`/** ... */`)
   若项目已有既定格式,则跟随之。

### 行内注释
1. 复杂逻辑块内部的注释应解释 **"为什么这么做"(意图 / 原因)**,而不是复述 **"做了什么"**(代码本身已经说明)。
2. 魔法数字、非显然的临界值、绕过 bug 的 workaround,必须有注释说明来由。
3. 不写无意义的废话注释(如 `i++ // i 加一`)。

---

## 三、命名与结构 (Naming & Structure)

> 命名不钉死某一种大小写风格,而是**服从所用语言的主流惯例,并与所在文件/模块保持一致**。

### 命名
1. **跟随语言惯例**并在同一文件/模块内保持统一。参考各生态常见约定:
   - Python:`snake_case` 函数与变量、`PascalCase` 类、`UPPER_CASE` 常量
   - C / 嵌入式:跟随项目既有风格(常见 `snake_case`),宏与常量用 `UPPER_CASE`
   - JS/TS:`camelCase` 变量与函数、`PascalCase` 类/组件、`UPPER_CASE` 常量
   - ROS:遵循 ROS 官方命名规范(包名、话题、参数等)
2. 命名应**表意清晰**,能看名知意;避免无意义缩写(除非是领域公认的,如 `pid`、`pwm`、`msg`)。
3. 同一概念在整个项目中使用**同一个词**,不要混用近义词(如 velocity / speed 混用)。

### 文件与目录结构
1. **按职责分层组织**代码,不要把不同职责堆进同一个文件(如 GUI / 数据 / 逻辑 / 配置 分开)。
2. 单个文件不宜过长;当职责明显超过一件事、或体量偏大时,应拆分。
3. 新增文件放入**职责对应的目录**,遵循项目已有的目录结构,不随意新建平行结构。

### 配置与常量外置
1. **关键参数需外置**到配置文件(如 `config.yaml`、`.env`、`params.yaml`),不散落、不硬编码在源码中。
2. **密钥 / Token / API Key / 服务地址** 一律放入配置文件或环境变量,且配置文件不纳入版本控制;
   如需示例,提供一份脱敏的 `config.example.*`。
3. 阈值、超时、端口、路径等"魔法数字"应抽为**具名常量或配置项**,不直接散布在代码里。
