# Watson — 第一轮 Spec Kit 使用指南

> 本文档指导你**第一轮**如何用 Cursor + GitHub Spec Kit 把 Watson（AI 日程管理系统）从想法推进到可实现的规格与任务。
> 产品定义见 [`README.md`](./README.md)。

---

## 0. 前置：怎么在 Cursor 里用这些命令

Spec Kit 的斜杠命令**不是命令行工具**，而是安装在 `.cursor/skills/` 下的 Cursor 技能。用法：

1. 用 **Cursor 打开本项目目录**（`~/MY_WS/watson`）。
2. 打开 Cursor 的 **Agent 聊天面板**（Cmd/Ctrl + L 或侧边 Chat）。
3. 在输入框里键入斜杠命令，例如 `/speckit-constitution`，后面跟上你的说明文字，回车。

> 这些命令会读写 `.specify/`（宪法、模板）和 `specs/`（规格/方案/任务）目录里的文件。

---

## 1. 第一轮的完整路径

```
① /speckit-constitution   →  ② /speckit-specify   →  ③ /speckit-clarify（可选但强烈建议）
                                                              ↓
⑥ /speckit-implement      ←  ⑤ /speckit-tasks     ←  ④ /speckit-plan
```

- ①②③ 属于**「想清楚做什么」**——不涉及代码。
- ④⑤⑥ 属于**「怎么做 + 做出来」**——这里才引入 Node.js 等技术栈。

**第一轮建议只走到 ⑤（生成任务清单）**，确认任务合理后，再开 ⑥ 让 AI 实现。别一上来就冲到 implement。

---

## 2. 一个关键认知（避免踩坑）⚠️

`/speckit-specify` 只关心 **WHAT（做什么）和 WHY（为什么）**，它会**刻意避开技术实现**——不要在这一步写「用 Node.js」「用 REST API」这类内容，那属于 `/speckit-plan` 的活。

- ✅ specify 阶段说：「用户能用自然语言让系统新建一张日程卡片」
- ❌ specify 阶段别说：「前端 React、后端 Express、数据库用 SQLite」

技术栈（Node.js、前后端、明暗主题实现、鉴权方案的具体技术）统统留到 **`/speckit-plan`**。

---

## 3. 逐步操作 + 示例

### ① `/speckit-constitution` — 确立项目原则

先给项目立「宪法」——即整个项目始终要遵守的硬性原则。它会填好 `.specify/memory/constitution.md`。

**示例输入：**

```
/speckit-constitution 为 Watson（单用户 AI 日程管理系统）确立项目宪法，包含以下原则：

1. 单用户安全第一（NON-NEGOTIABLE）：系统部署在公网但必须只有所有者本人可访问，
   任何未授权访问都视为严重缺陷；鉴权与会话是一等公民需求，不是附加项。
2. 密钥外置：所有 API Key / Token / 服务地址一律通过配置文件或环境变量注入，
   禁止硬编码、禁止进版本控制，必须提供脱敏的示例配置。
3. AI 能力可插拔：LLM provider（OpenAI / Claude / DeepSeek 等）由用户在使用时配置，
   代码不绑定任何单一模型或中转，接口需可切换。
4. 数据以「日程卡片」为核心：所有日程都是结构化卡片，具备时间性质、重要度、紧急度。
5. 多设备一致体验：手机/平板/PC 通过浏览器访问，同一份数据、同一套交互。

请据此生成宪法，版本从 1.0.0 起，ratification date 用今天。
```

> 完成后检查 `.specify/memory/constitution.md`，确认没有残留的 `[占位符]`。

---

### ② `/speckit-specify` — 写基线规格

这一步把「做什么」写成结构化规格（存到 `specs/NNN-<短名>/spec.md`）。**只描述用户价值和行为，不碰技术栈。**

第一轮建议**按功能域分次 specify**，别把整个产品塞进一次。推荐先做最核心的一条：**AI 聊天栏驱动的日程管理**。

**示例输入（核心功能）：**

```
/speckit-specify Watson 的核心能力是「对话式日程管理」。用户界面对标 Cursor：
主区域展示日程，侧边常驻一个 AI 聊天栏。用户在聊天栏用自然语言对话，AI 理解后
对日程数据执行操作：

- 添加：用户说「下周一上午十点体检」，系统自动创建一张日程卡片并填好时间与性质。
- 查询/问答：用户问「我这周三有什么安排？」或「最近有没有 deadline 快到了？」，系统给出答复。
- 修改：用户说「把周会改到下午四点」，系统更新对应卡片。
- 删除：用户说「取消明天的牙医预约」，系统删除对应卡片。

日程以「卡片」为基本单位，每张卡片包含：标题、时间性质（持续型=有开始+结束时间，
或截止型=只有一个 deadline）、重要程度（高/中/低）、紧急程度（高/中/低）、分类。

用户仅有一人（所有者本人），通过手机/平板/PC 浏览器访问。系统部署在公网，
但必须保证只有所有者本人能访问和操作自己的日程。
```

**会发生什么：**
- Spec Kit 生成 `spec.md`，含用户场景、功能需求、成功标准、关键实体（日程卡片）。
- 它可能抛出**最多 3 个** `[NEEDS CLARIFICATION]` 问题（以表格形式列 A/B/C 选项），
  优先问**范围 > 安全/隐私 > 体验**类的关键歧义。**认真回答这些问题**，它会据此改写规格。
- 自动生成质量检查清单 `specs/NNN-.../checklists/requirements.md`。

> 可能被追问的点举例：日程时间冲突了怎么处理？AI 误解指令时要不要二次确认？删除要不要确认？
> 想清楚就直接答，没想清楚就选它给的合理默认项。

**后续可再分次 specify 其余功能域**（可放第二轮，或本轮继续）：
- 「日/周/月/全部 四种日程视图，全部视图带筛选器与排序器」
- 「单用户访问控制：公网部署下只有所有者能登录访问」
- 「明暗双主题 + Cursor 风格布局（顶部控制栏 + 主视觉区 + 侧边聊天栏）」

---

### ③ `/speckit-clarify` —（强烈建议）规划前消歧

在进入 plan 之前，用它把规格里剩余的模糊点系统性问清楚，降低后面返工风险。

**示例输入：**

```
/speckit-clarify
```

（无需参数，它会针对当前 spec 逐条提出结构化问题，你逐个回答。）

---

### ④ `/speckit-plan` — 技术方案（**这里才引入技术栈**）

到这一步才把「怎么做」定下来。把你的技术约束一次性交代清楚。

**示例输入：**

```
/speckit-plan 技术栈与实现约束如下：

- 全栈使用 Node.js。
- 包含 Web 前端 + 后端完整设计；浏览器直接访问，无原生 App。
- UI 对标 Cursor：顶部控制栏 + 主视觉区（日程视图）+ 侧边 AI 聊天栏；支持明/暗两种主题。
- LLM 接入可插拔：通过用户配置的 API Key 调用 OpenAI / Claude / DeepSeek 等
  OpenAI-兼容接口，provider 可切换；Key/BASE_URL/MODEL 走 config.yaml 或 .env，不硬编码。
- 单用户鉴权：公网部署下需一套坚固的访问控制（如强密码登录 + 会话，别人拿到 URL 也进不来），
  支持多设备登录同一账户。
- 数据持久化选型请给出建议（轻量优先，单用户场景）。

请产出实现方案，并标注关键技术决策与取舍。
```

---

### ⑤ `/speckit-tasks` — 拆解为可执行任务

**示例输入：**

```
/speckit-tasks
```

它会把 plan 拆成一份带顺序、可勾选的任务清单（`tasks.md`）。**第一轮到这里先停下**，人工过一遍任务是否合理、粒度是否合适。

---

### ⑥ `/speckit-implement` — 执行实现

确认任务清单没问题后，再让 AI 按任务实现：

```
/speckit-implement
```

> 建议**分批实现 + 每批验证**，不要一次性跑完全部任务。按 coding 惯例：改完实际运行、给出运行输出/截图作为证据。

---

## 4. 第一轮推荐节奏（TL;DR）

| 步骤 | 命令 | 产物 | 第一轮是否必做 |
|---|---|---|---|
| 1 | `/speckit-constitution` | `constitution.md` | ✅ 必做 |
| 2 | `/speckit-specify` | `specs/NNN-*/spec.md` | ✅ 必做（先做核心「AI 聊天栏管日程」） |
| 3 | `/speckit-clarify` | 更新 spec | ⭐ 强烈建议 |
| 4 | `/speckit-plan` | `plan.md` | ✅ 必做（此时定 Node.js 等技术栈） |
| 5 | `/speckit-tasks` | `tasks.md` | ✅ 必做（人工审阅后再往下） |
| 6 | `/speckit-implement` | 代码 | 🔸 审阅任务后，分批做 |

**核心提醒：**
1. specify 阶段只谈「做什么」，技术栈留到 plan。
2. 认真回答 specify/clarify 的反问——歧义不消，后面必返工。
3. 安全（单用户私有访问）是本项目一等公民，从 constitution 到 plan 都要盯住它。
4. tasks 生成后先审阅再 implement，别盲目一把梭。

# Watson - 第二轮 Spec Kit 迭代
第一轮结束之后我根据测试结果提出以下的新要求
1. 在主题中，应当只有light和dark，删除system。
2. 在主导航栏类似于cursor添加一个控件，可以控制右侧AI助手面板隐藏或者出现
3. AI助手栏可以拖动边缘控制宽度
4. 月视图下，主视图界面中应该是当月的日历，每个“日”中有卡片。鼠标可以点击卡片查看日程详细信息
5. 周视图下，主视图界面应该为周一～周日的设计，每个日中有小卡片，鼠标可以点击卡片查看日程详细信息
6. 日视图下，直接就是当日日程卡片，
7. 全部视图下，直接就是所有日程卡片
8. 点击卡片之后，出现卡片的详细信息，包括该日程的基本属性，删除该卡片等功能
9. 在主导航栏添加一个新的“+”号表示手动创建日程

---

## 第二轮 Spec Kit 指令（可直接粘进 Cursor Agent 面板）

> 说明：本轮是对已上线的 `001-conversational-scheduling` 的**界面与交互迭代**，不改动数据模型与后端 CRUD 语义。因此**跳过 constitution**（宪法未变），直接走 `specify → clarify → plan → tasks → implement`。
> 沿用第一轮铁律：**specify 只谈「做什么/为什么」，技术栈（React/Tailwind/组件方案/拖拽实现）留到 plan。**
> 上面 9 条里既有**修改已有行为**（主题去掉 system、月/周视图从平铺列表改成日历网格），也有**新增能力**（聊天栏折叠+拖拽、卡片详情弹层、手动 + 创建），所以下面的 specify 指令里显式带上「当前是什么样、要变成什么样」，让规格能准确表达 delta。

---

### ② `/speckit-specify` — 写第二轮基线规格

```
/speckit-specify 这是对已实现功能 001-conversational-scheduling 的第二轮界面交互迭代，命名为「日程可视化与手动操作增强」。根据首轮测试反馈，需要改进日程的浏览体验、AI 助手栏的可控性，并补齐不依赖对话的手动操作入口。以下按用户价值描述要做什么，不涉及技术实现：

【主题偏好收敛】
- 现状：主题有 light / dark / system 三种，控制栏按钮在三者间循环切换。
- 目标：只保留 light 与 dark 两种。所有者点击主题控件时在明、暗之间直接切换，不再有「跟随系统」选项。历史上已保存为 system 的偏好应被视作一个明确的默认值（取 light），不得让界面停留在无法切换的状态。

【AI 助手栏可控性】
- 现状：侧边 AI 聊天栏在桌面端常驻显示、宽度固定，仅移动端有一个显示/隐藏按钮。
- 目标一：在顶部主导航栏提供一个常驻控件（对标 Cursor 的侧栏开关），所有者在任意设备上都能一键隐藏或重新显示右侧 AI 助手面板；隐藏后主日程区域占满整幅宽度。
- 目标二：AI 助手栏显示时，所有者可以拖动它与主区域之间的边缘来调整助手栏宽度；宽度应有合理的最小/最大范围，避免拖到不可用；调整后的宽度在同一设备上应被记住。

【四种视图的呈现方式】
- 现状：日/周/月/全部四种视图当前都只是把命中的卡片平铺成一个列表，月和周并没有真正的日历样式。
- 月视图目标：主区域呈现「当月日历」——按周排列的日期网格，每个日期格子里展示当天的日程卡片（数量多时可省略/折叠显示）。点击某张卡片可查看该日程的详细信息。
- 周视图目标：主区域呈现「周一至周日」的一周布局，每一天一列或一格，其中放当天的小卡片。点击某张小卡片可查看该日程的详细信息。
- 日视图目标：直接展示当日的日程卡片（保持卡片列表形态即可，无需日历网格）。
- 全部视图目标：直接展示所有日程卡片（沿用首轮已有的筛选与排序能力）。

【卡片详情与删除】
- 现状：卡片只在列表里展示概要，没有查看详情或在界面上直接删除的入口（删除只能靠对话）。
- 目标：在任意视图点击一张日程卡片后，弹出/展开该卡片的详细信息，至少包含标题、时间性质（持续型的开始与结束时间，或截止型的 deadline）、重要程度、紧急程度、分类等基本属性；并在详情中提供「删除该日程」的操作。删除须遵守既有规则——删除前二次确认，确认后才真正删除，删除后各视图应及时反映变化。

【手动创建日程】
- 现状：新建日程只能通过 AI 聊天栏用自然语言完成。
- 目标：在顶部主导航栏新增一个「+」控件，所有者点击后可以不经过对话、直接以表单方式手动创建一张日程卡片，填写标题、时间性质及对应时间、重要度、紧急度、分类等属性；创建成功后立即在当前视图中可见。

约束与边界：本轮不改变「日程卡片」的数据结构与字段含义，不改变后端已有的增删改查与鉴权逻辑，仅在其之上增强前端浏览与手动操作体验。手动创建与手动删除必须复用已有的卡片创建/删除能力（含删除二次确认），保证与对话式操作行为一致。
```

> 预期它会就一两处歧义抛出 `[NEEDS CLARIFICATION]`，可能问：月视图格子里卡片过多时如何取舍（显示前 N 张 + “更多”）、拖拽宽度是否需要跨设备同步（建议只记本设备）、手动创建表单是否允许留空可选字段。按下面 clarify 一起答即可。

---

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

逐条回答它的提问。若它没覆盖，主动补充这几条倾向：
- 主题：原 system 值一律回退为 light；控件只在 light/dark 间切换。
- 月视图单日卡片过多：格子内最多显示前若干张，其余以「+N 更多」提示，点击可展开或跳到该日视图。
- 助手栏宽度：仅记在当前设备本地，**不**跨设备同步（区别于日程数据的跨设备一致）。
- 卡片详情删除：必须走既有二次确认流程，与对话删除同一套语义。

---

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上做前端交互增强，不引入新的后端能力，沿用首轮既定选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向：

- 主题：改造现有 useTheme —— 去掉 system 分支，主题类型收敛为 "light" | "dark"；读取偏好时把历史 "system" 归一化为 "light"；控件从三态循环改为二态切换。偏好仍通过既有 preferences 接口持久化。
- AI 助手栏折叠：把控制权从移动端专用按钮提升为顶部导航栏常驻控件（对标 Cursor 侧栏开关图标）；沿用 AppShell 现有的 chatOpen 状态，隐藏时主区域用 flex 占满。
- 助手栏拖拽调宽：在主区域与 aside 之间加一个可拖拽分隔条，用指针事件调整宽度，设定 min/max 像素范围；宽度存 localStorage（仅本设备），刷新后恢复。
- 视图重构：MonthView 改为按周分行的日期网格（用 date-fns 计算当月起止与补齐首尾周），每格渲染当日卡片，超出上限显示「+N」；WeekView 改为周一~周日七列布局；DayView 与 AllView 保持卡片列表（AllView 继续用首轮的筛选/排序）。三视图统一通过既有 GET /api/cards 的 view/date/filter/sort 参数取数。
- 卡片详情弹层：新增一个卡片详情组件（模态或抽屉），点击任意视图中的卡片打开，展示卡片全部基本属性；内含删除按钮，复用既有 DELETE /api/cards/:id，并在前端保持删除前二次确认；成功后用 TanStack Query invalidate 刷新相关视图查询。
- 手动创建：顶部导航栏加「+」控件，打开一个创建表单（模态/抽屉），字段对应卡片数据模型（标题、时间性质及时间、重要度、紧急度、分类，分类可从既有 categories 接口选取）；提交调用既有 POST /api/cards；成功后 invalidate 当前视图查询即时可见。

请产出实现方案，标注：日历网格的日期计算与时区处理（沿用 Asia/Shanghai 偏好）、拖拽宽度的事件与边界处理、详情/创建弹层的组件形态与可复用性、以及哪些改动是纯前端、哪些需要触及后端（预期后端零改动，如有例外请指出）。
```

---

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成 `tasks.md` 后**先人工审阅**：确认任务按「主题收敛 → 助手栏折叠/拖拽 → 三视图重构 → 卡片详情+删除 → 手动创建」分组，且每条带明确文件路径（预期集中在 `frontend/src/` 下的 hooks / layouts / components，后端预期不动）。

---

### ⑥ `/speckit-implement` — 分批实现

```
/speckit-implement
```

> 建议按用户故事分批跑、每批跑完实际启动前端验证（截图/交互）再进下一批，别一次性梭完 9 条。改完记得 `npm run build` 双端零错误、`npm run dev` 手动过一遍每条改动。

# Watson - 第三轮 Spec Kit 迭代
1. 新增功能
  1.主导航栏添加搜索栏，点击搜索栏后，主视图面黯淡。搜索栏只支持搜索日程卡片的名称。比如搜索“测试”，那么就在搜索栏下方出现“*测试*”"*测*试*"等符合匹配方法的日程名称（类似于google浏览器，点击搜索框后下方出现可能的搜索目标的美术设计），点击某个目标后就打开该目标的详情。
  2. 点击主导航栏的”+“号后创建日程时，只要标题不为空，按下enter键可以直接创建
  3. 创建日程时，只有标题是必须的。没有标明时间的只显示在全部视图
  4. 全部视图下应该有更加丰富的筛选和排序机制
  5. 在任何视图下点击卡片打开日程详情之后都可以进行编辑
  6. AI助手现在不需要和主要功能产生联动，暂时先把我已经放在.env中的deepseek api key对话接入，作为一个AI对话板块

2. 美术设计方面修改
  1. 日视图和全部视图模式下，日程卡片应该以圆角矩形呈现矩阵排列
  2. 深色主题下把蓝色替换为nvidia图表的绿色，浅色模式下还用蓝色
  3. 周视图下，应该为七个横向排列的竖向圆角矩形铺满画面
  4. 月视图下的日历应该铺满整个画面

3. 其他bug
  1. 日程卡片应当不能重名

---

## 第三轮 Spec Kit 指令（可直接粘进 Cursor Agent 面板）

> 定位：第三轮是**成体系的新一轮**（新功能 + 美术改造 + bug 修复），按 SDD「开新 feature」模型，直接跑 `/speckit-specify` 会自动扫描现有编号（当前最大 002）并新建 **003**，同时把 `.specify/feature.json` 激活指针切到 003。**开新轮 = 跑 specify；跑了 specify 就必然新建一轮。**
> 铁律不变：**specify 只谈「做什么/为什么」，技术栈（组件、DeepSeek 接入、唯一约束实现）留到 plan。**
> 本轮多为**改已有行为**（AI 助手从联动改为纯对话、卡片详情从只读改为可编辑、四视图美术重排），所以 specify 指令里显式带「现状 → 目标」。
>
> ⚠️ 一处事实校正（写进了下面 plan）：`.env` 里**没有**独立的 `DEEPSEEK_API_KEY`，第一轮就是可插拔设计，DeepSeek 走通用变量 `LLM_PROVIDER=deepseek` + `LLM_API_KEY` + `LLM_BASE_URL` + `LLM_MODEL`，config 的 provider 枚举已含 `deepseek`。别让它去找不存在的 `DEEPSEEK_API_KEY`。

---

### ② `/speckit-specify` — 写第三轮基线规格

```
/speckit-specify 这是 Watson 的第三轮迭代，命名为「日程搜索、编辑增强与视觉重排」。在已上线的对话式日程管理（001）与可视化/手动操作（002）之上，继续增强查找、创建、编辑体验，把 AI 助手降级为独立对话板块，并统一各视图的视觉呈现。以下按用户价值描述要做什么，不涉及技术实现：

【新增功能】
1. 全局搜索：在顶部主导航栏新增一个搜索栏。所有者点击搜索栏后，主视图区域变暗（弱化背景、聚焦搜索）。搜索仅针对「日程卡片标题」，支持模糊匹配——例如输入「测试」，下方即时浮出所有标题命中的候选（含「*测试*」「*测*试*」这类中间/分散匹配），呈现为一个类似浏览器地址栏的下拉候选列表（有清晰的视觉设计）。点击某个候选，直接打开该日程的详情。
2. 快捷创建：点击导航栏「+」创建日程时，只要标题不为空，按下 Enter 即可直接创建（无需点提交按钮）。
3. 最简必填：创建日程时，唯一必填项是标题。未填写任何时间信息的日程视为「无时间」，它不出现在日/周/月视图中，只在「全部」视图里展示。
4. 更强筛选排序：全部视图提供更丰富的筛选与排序机制（现状仅支持按重要度筛选、按时间/优先级排序），需扩展到覆盖分类、紧急度、时间性质、有无时间等更多维度的筛选，以及更多排序选项。
5. 卡片可编辑：现状——点击卡片打开的详情弹层只能查看和删除，不能改。目标——在任意视图点击卡片打开详情后，均可进入编辑并保存对该日程各属性的修改（复用既有创建/编辑表单能力，遵守与创建一致的校验，如标题必填、不可重名）。
6. AI 助手解耦：现状——侧边 AI 助手与日程数据联动（对话可增删改卡片、并刷新视图）。目标——本轮先取消这种联动，把它降级为一个独立的「AI 对话板块」：所有者可以和 AI 自由对话，但对话不再操作日程数据。对话接入所有者已在环境中配置好的 DeepSeek 对话能力即可。

【视觉设计】
7. 日视图与全部视图：日程卡片以「圆角矩形」样式、以矩阵（网格）方式排列展示。
8. 主题强调色：深色主题下把强调用的蓝色替换为 NVIDIA 品牌那种绿色；浅色主题保持原有蓝色不变。
9. 周视图：呈现为七个横向并排、各自纵向延伸的圆角矩形列，铺满整个主视图画面（周一至周日各一列）。
10. 月视图：当月日历铺满整个主视图画面（不再是局促的小网格）。

【Bug 修复】
11. 日程卡片标题不允许重名：创建或编辑日程时，若标题与已有日程重复，应被拒绝并给出明确提示。

约束与边界：不改变「日程卡片」的核心字段含义；沿用既有的鉴权、卡片增删改查与手动操作能力。AI 助手解耦是本轮明确的行为收敛（从"能操作日程"退回"纯聊天"），不是删除对话功能。"不可重名"需在创建与编辑两条路径上都生效，并与"仅标题必填""无时间只进全部视图"等规则彼此自洽。
```

> 预期它会就一两处歧义抛 `[NEEDS CLARIFICATION]`，可能问：重名判定是否忽略大小写/前后空格、是否区分已删除卡片；搜索候选是否有数量上限；"无时间"卡片在全部视图里如何排序。见下面 clarify 一并答。

---

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

倾向答案（它没问到就主动补）：
- 重名判定：忽略大小写与首尾空格（trim + 小写比较，与既有分类名的 name_lower 唯一策略一致）；仅对未删除的日程生效。
- 搜索候选：即时模糊匹配，最多显示前若干条（如 8 条），过多时提示继续输入收窄。
- 无时间卡片：在全部视图中可正常参与筛选/排序，排序时归入「无时间」分组或排在有时间项之后。
- AI 对话：本轮不做工具调用/日程联动，纯文本往返即可；会话历史是否持久化沿用第一轮 chat 存储或简化为临时会话，二者皆可，倾向复用既有存储但关闭工具编排。

---

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上迭代，沿用首轮选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向：

- 全局搜索：TopBar 新增搜索输入；聚焦时给主视图区加一个变暗遮罩层（overlay + 降低不透明度）。前端做标题模糊匹配下拉候选（子串/分散字符匹配），数据源用既有 GET /api/cards（或新增一个轻量 title 检索参数，优先复用现有接口在前端过滤，卡片量小无需后端全文检索）。点击候选复用既有 CardDetailModal 打开详情。
- 快捷创建：CreateCardModal / CardFormFields 里给标题输入绑定 Enter 提交（标题非空时触发既有 POST /api/cards），保留提交按钮作为备选。
- 最简必填 + 无时间归属：校验规则改为仅标题必填（timeNature/时间字段可空）；后端 schedule.service 与 GET /api/cards 的 view 过滤需支持「无时间」卡片——日/周/月视图排除无时间项，all 视图包含。检查 data-model 是否需要允许时间字段为空。
- 全部视图筛选排序增强：AllView 现仅有 importance 筛选 + time/priority 排序，扩展为 category / importance / urgency / timeNature / 有无时间 多维筛选与多种排序；对应扩展 GET /api/cards 的 query 参数（沿用第二轮已加的 view/date/filter/sort 参数体系）。
- 卡片编辑：现有 CardDetailModal 为只读，改造为「查看 ↔ 编辑」两态，编辑态复用 CardFormFields + validateCardForm，保存走既有 PATCH /api/cards/:id；保存后 invalidate 相关视图查询。
- AI 助手解耦：ChatPanel 现在监听 affectedCards 并 invalidate cards——移除这条联动；后端 chat.service 关闭 LLM 工具编排（不再暴露 create/query/update/delete 工具），退化为纯文本对话。DeepSeek 接入沿用既有可插拔 provider：通过 LLM_PROVIDER=deepseek + LLM_API_KEY + LLM_BASE_URL + LLM_MODEL（provider 枚举已含 deepseek，openai-compatible 适配器可直连 DeepSeek 的 OpenAI 兼容端点）。注意：.env 中并无独立 DEEPSEEK_API_KEY，不要新增该变量。
- 视觉：日/全部视图卡片改为圆角矩形 + CSS grid 矩阵排列；主题强调色用 CSS 变量分叉——深色 --accent 换成 NVIDIA 绿（约 #76b900），浅色维持原蓝，改造 index.css 的主题变量而非散落硬编码；WeekView 改七列 flex 铺满、每列纵向圆角矩形容器；MonthView 日历网格改为占满主区高度的自适应布局。
- 重名约束：后端在 schedule_cards 增加标题唯一性保障——data-model 加 title 的规范化唯一（参考既有 categories 的 name/name_lower 双列策略，加 title_lower 唯一索引），create 与 update 路径都校验，命中返回明确错误码/信息；前端表单捕获该错误并提示。需要一条 Drizzle migration。

请产出实现方案，标注：哪些是纯前端、哪些触及后端与数据库（预期：无时间归属、筛选参数、AI 解耦、重名唯一约束会动后端；搜索/变暗/编辑态/视觉主要在前端）；重名唯一约束的迁移与既有数据兼容性（若现有数据已有重名需先处理）；主题色改造的收敛点（集中到 CSS 变量）。
```

---

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成后**人工审阅**：确认任务按「搜索 → 快捷/最简创建 → 无时间归属 → 全部视图筛选排序 → 卡片编辑 → AI 解耦+DeepSeek → 视觉重排 → 重名约束（含 migration）」分组，每条带明确文件路径，并明确标出触及后端/DB 的少数几条。

---

### 【新增一步】`/speckit-analyze` — 动手前查一致性

```
/speckit-analyze
```

> 只读检查 spec/plan/tasks 三者是否自洽（尤其「仅标题必填」「无时间只进全部视图」「不可重名」这三条相互牵连的规则有没有矛盾）。有问题先回改规格，再进 implement。

---

### ⑥ `/speckit-implement` — 分批实现

```
/speckit-implement 只执行第 N 阶段的任务，完成后停下等我验证，不要继续后续阶段。
```

> 分批做法：把上面的 N 换成具体阶段号，一次一批，跑完 `npm run build` 双端零错误 + `npm run dev` 手动验证该批效果，再喊它「继续执行第 N+1 阶段」。它靠 tasks.md 的 `[X]` 标记断点续跑。**特别是 AI 解耦、重名 migration、主题色改造这几批容易连带影响既有功能，务必单独成批、逐批验证。**

---

### 全部做完后 `/speckit-converge` — 兜底查缺

```
/speckit-converge
```

> implement 全跑完后用它对照 spec/plan/tasks 扫码，把「规格要求但没做全」的缺口 append 成新任务，再 `/speckit-implement` 补齐。converged 就代表本轮收口。

---

# 宪法对齐（第四轮开工前先做）

> 背景：第三轮之后我手动改过代码，登录与 AI 两处的**实际实现已经稳定**，但 `constitution.md` 的文本没完全跟上——它俩现在是「宪法欠了两笔账」，不是代码错了。这一小节**只对齐宪法文本，不写任何新功能**，跑完再进第四轮需求。
>
> 现状确认（以代码为准）：
> - **登录**：唯一方式就是**四字符验证码**（默认 `ANNA`，可 `WATSON_ACCESS_CODE` 覆盖）+ HttpOnly/SameSite=Strict 会话。不引入密码、令牌、2FA、白名单等任何其他方式。
> - **AI 作用**：侧边 AI 是**纯文本对话板块**，**不与日程管理产生任何交互**（不增删改查日程、不触发数据刷新）。（标注为「暂时这样」——本轮先固化现状，日后若要恢复联动再走 MAJOR 修订。）
>
> 为什么要单独跑一次 `/speckit-constitution`：这两处属于**收紧/明确既有原则的表述**（登录方案去掉历史废弃项、AI 范围正式收敛），是 **MINOR** 级修订，版本应从 **1.1.0 → 1.2.0**。宪法是最高治理文档，先把它对齐，第四轮的 specify/plan 才有正确的宪法基线可依。

### `/speckit-constitution` — 对齐登录与 AI 范围（直接粘进 Cursor Agent 面板）

```
/speckit-constitution 请对 Watson 现行宪法做一次 MINOR 修订，版本从 1.1.0 升到 1.2.0，Last Amended 用今天。本次不新增原则，只把两处既有原则的表述与项目当前实现对齐、收紧，并清理历史残留描述。修订点如下：

【一、原则 I 单用户安全第一 —— 收口登录方案】
- 明确唯一登录方式为「四字符验证码」：所有者在登录页输入四字符验证码（默认 ANNA，可通过环境变量 WATSON_ACCESS_CODE 覆盖），校验通过后签发 HttpOnly + SameSite=Strict 会话 cookie；除 login / health 外所有 /api/* 必须经会话中间件保护；login 端点应限流防暴力尝试。
- 明确排除其它任何鉴权方式：不引入用户名密码、长随机访问令牌、Bearer Token、2FA、IP 白名单等，均视为超出单用户产品范围。
- 彻底移除历史废弃描述：删掉正文与治理说明里所有关于 watson:init 初始化流程、WATSON_ACCESS_TOKEN_HASH、长随机访问令牌方案的残留字样（这些方案已不再使用，不需要再以“已废弃/MUST NOT 重新引入”的形式保留，直接从宪法正文清理即可）。

【二、原则 III「AI 能力可插拔」与 Technology & Scope —— 正式收敛 AI 交互范围】
- 正式确立（不再是待办 TODO）：侧边 AI 是一个纯文本对话板块，只与所有者自由对话，MUST NOT 执行任何日程的增删改查，也不触发日程数据的刷新或联动；所有日程操作只经 UI 与 REST API 的结构化路径完成。
- 保留 provider 可插拔原则不变（OpenAI / Claude / DeepSeek 等 OpenAI-兼容接口，配置外置、可切换），仅收敛「AI 能做什么」的范围，不改「AI 用什么模型接入」的机制。
- 请把 Technology & Scope Constraints 中的「AI 交互范围」表述与此对齐，并把宪法顶部 Sync Impact Report 里那条“可选 MINOR 修正案同步 AI 交互范围（003 纯聊天）”的 Follow-up TODO 标记为已完成（本次修订即完成它）。
- 备注：此收敛标注为当前阶段的产品决策（“暂时如此”）；若未来要恢复 AI 直接操作日程，属于重新定义原则范围，届时走 MAJOR 修订，本次不预留该路径。

【三、原则 IV 数据以「日程卡片」为核心 —— 顺带校正一处措辞】
- 原文有「AI MUST NOT 绕过卡片结构直接篡改存储」，在 AI 已完全不操作日程数据的前提下，请将该句调整为与新范围一致的表述（例如：日程增删改查一律经 UI/API 结构化路径；AI 对话不写入日程存储），避免读者误以为 AI 仍有写数据的通路。

【四、版本与同步】
- 更新宪法顶部的 Sync Impact Report：记录 1.1.0 → 1.2.0、本次修改的原则（I 收口登录、III + Scope 收敛 AI、IV 措辞校正）、清理的废弃项，并标出需要同步的模板/文档（README.md、setup.md、.env.example 若涉及登录或 AI 描述需一并对齐）。
- 底部版本行更新为：Version 1.2.0 | Ratified 保持 2026-07-03 不变 | Last Amended 用今天日期。

请据此产出修订后的 constitution.md，并在 Sync Impact Report 里如实标注哪些下游文档还需人工同步（你只改宪法本身，下游文档我另行处理）。
```

> 跑完后自检：`.specify/memory/constitution.md` 顶部 Version 应为 **1.2.0**，正文里**再也搜不到** `watson:init` / `WATSON_ACCESS_TOKEN_HASH` / 长随机令牌 字样；AI 范围一句明确写「不执行日程 CRUD」；Sync Report 里原来那条 AI 的 Follow-up TODO 已勾掉。
> 宪法对齐后，再往下写第四轮的 specify/plan/tasks。

---

# Watson - 第四轮 Spec Kit 迭代

当前需要添加的新功能
- 所有日程的紧急程度和重要程度属性范围改为0～10。0和10都可选。用户体验：点击该属性后上下拉开一个数字序列（最下面是0,最上面是10），点击数字修改具体值。
- 添加垃圾箱标志在页面最右上角（因为“登出”要删除，所以就取代“登出”的位置即可），用来存储删除的或者已完成的卡片。点击标志打开垃圾箱，垃圾箱的视图与全部视图类似，但是排列顺序依靠的是卡片进入垃圾箱的时间，左上角是刚刚被放入的，右下角是之前的。被打了勾（完成的）日程就标记对勾，被删除的就用红色圆形标记。
- 在日视图和全部视图中，添加“坐标视图”按键，点击后页面中心打开二维视图，以重要程度5和紧急程度5为原点构建二维坐标系，把处于当前日视图或者全部视图的日程卡片按照横坐标为紧急程度，纵坐标为重要程度对应上去。注意，二维视图的视觉效果，就是一个十字坐标系，对应的坐标如果有任务，那么就显示一个圆，该点的任务越多，圆就越大，鼠标放在圆上时，该圆上出现一个信息框，显示那个圆中包含的任务的名称，右上角方向为更重要更紧急。
- 日程卡片添加快捷完成按键，就在打开日程详情后的右上角添加一个方框，点击后出现一个对勾，然后该日程（视为完成）进入垃圾箱。
- 在日视图和全部视图中的日程卡片的右边也添加一个方框，点击后出现对勾，然后该日程（视为完成）进入垃圾箱
- 日程应当有“创建时间”“最后一次修改时间”属性，这两个属性只在点击卡片查看详情时小字浅色显示（显示格式示例 创建时间：2026-0715-1639）
- 删除“登出”键，这个按键没有意义。

旧功能修改
- 在点击日程卡片阅读详情时，应该可以直接更改，把现在的“编辑“改为“恢复”键，点击后回复到本次打开时的原设置，把”关闭“改为”确认“。而不需要点击“编辑”键。用户体验”点击卡片打开详情后右下角只有“恢复”（默认无法点击），”确认“键，直接点击确认或者点击其他地方即退出，如果发生了修改就可以点击恢复回到这次打开时的情况，发生了修改后点击确认或者其他地方就直接确认修改。不需要二次弹窗确认。
- 启动网页后，默认AI助手处于隐藏状态。把“隐藏助手”按键字符改为“AI”不变，按键框在隐藏时为白色，显示时为蓝色即可（深色模式下改为对应颜色。）
- 修改切换主题的按键“主题：浅色”只保留”浅色“，点击后换成”深色“，再次点击为”浅色“即可
- 点击打开卡片详情之后的“删除该日程”改为“删除”即可，点击删除后出现的确认程序的框体再窄一点，现在横向太宽了。
- 取消时间性质属性，一个任务关于时间只有“已安排”“未安排”（目前称为“有无时间”）这两种情况，生成任务时默认起始时间就是创建任务的时间就行了，如果有截至日期，那么就是创建日期到截至日期。如果没有安排时间，就按照未安排属性来创建。

美术/视觉调整
- 日视图/全部视图下，日程应当遵循创建时间排列，第一排中左边为最新创建，最右边为第一排中最晚创建，第二排第一个比第一排最右边更晚，再往右边或者下边就更晚。以此类推。
- 日视图/全部视图下，当页面发生放缩时，日程卡片也会拉伸横向宽度，然后在大幅变化的情况下，才会更改数量。这一轮需要减小拉伸的阈值，也就是说每排的数量更容易发生变化而不是更容易横向拉伸。
- 周视图/月视图下，针对持续性的日程，月视图中的每个日期格子中如果出现了持续性日程，不需要跨格子连接起来，但是需要比其他的任务稍宽一些表示特殊，周视图则延续跨格子连接。

需要解决的bug
- 最开始点击 + 号创建卡片时，是有确认的，在上一轮修改中被移除了，添加回来。最终的用户体验应该是“点击 + 号创建卡片，不管是键盘按enter还是手动点击确认键都可以成功创建“。
- 无论什么视图，留出页边距，现在周视图和月视图最左边的那一个或者那一列已经有点超出页面了。
- 现在的周视图和月视图下任务的竖向排列有问题，导致了日程之间挤在一起。

---

## 第四轮决策定案（澄清后的权威口径，与上面原始需求对照阅读）

> 上面是我原始口述的需求；下面是逐条逻辑分析后拍板的定案。**遇到两者措辞冲突，以本表为准。** 下面的 speckit 指令都按本表写。

| # | 议题 | 定案 |
|---|---|---|
| 1 | 重要/紧急度 | 由 高/中/低 改为 **0~10 整数**（含 0 与 10）。交互：点击属性后竖向拉开数字序列（下 0 上 10），点数字选值。 |
| 2 | 时间模型（重构） | **取消「时间性质」字段**。卡片只分两态：**已安排**（用户主动填了起始时间；可再填截止时间→区间为起始→截止）与 **未安排**（用户没填任何时间字段，起始也**不**默认填）。**未安排卡片只进「全部」视图**，不进日/周/月视图。（推翻原口述「默认起始=创建时间」的说法。） |
| 3 | 垃圾箱 | 右上角新增垃圾箱图标（占用原登出位置）。**删除的卡片**与**完成的卡片**都进箱，且**退出所有常规视图**（日/周/月/全部/坐标都不再显示）。箱内视图仿全部视图，但按**进箱时间倒序**（左上=最新进箱，右下=较早）。完成的标**对勾**、删除的标**红色圆形**。箱内每张卡片可**恢复**回活跃态，也可**永久删除**。 |
| 4 | 完成态 | 卡片新增「完成」操作：①详情右上角方框，点出对勾→视为完成→进箱；②日/全部视图卡片右侧方框，点出对勾→完成→进箱。**完成操作不需要二次确认**（低风险）。 |
| 5 | 坐标视图 | 日视图/全部视图新增「坐标视图」按钮。点击后页面中心打开十字坐标系：**X=紧急度、Y=重要度，原点=(5,5)**，右上=更重要更紧急。取当前视图（日 or 全部）的活跃卡片（**已完成/已删除的不画**；全部视图下**含未安排卡片**，它们也有重/急度）按坐标定位；同一坐标点的多张卡片聚成一个**圆**，**圆越大=该点任务越多**（半径设上限防溢出）；鼠标悬停圆上弹信息框列出该点所有任务名（超过若干条显示「+N」）。 |
| 6 | 详情编辑（重构） | 打开详情即为**可编辑态**，无需先点「编辑」。右下角两键：**「恢复」**（默认禁用；发生修改后可点，作用=把字段重置回本次打开时的值，**不关闭**弹层）、**「确认」**（保存修改并关闭）。**点弹层外部=放弃本次修改并关闭（当取消）**；只有点「确认」才保存。**不要二次弹窗**。保存时若校验失败（标题为空 / 与他卡重名）→ 就地红字提示，不关闭、不保存。 |
| 7 | 创建确认（bug 回归） | 手动创建（+ 号）恢复确认按钮。最终体验：**键盘 Enter 或 手动点确认，二者都能成功创建**。仅标题必填。 |
| 8 | 删除确认 | 详情内「删除该日程」文案改为**「删除」**；点删除后的二次确认框**改窄**（当前横向过宽）。删除进箱这一步保留二次确认；箱内「永久删除」也需二次确认。 |
| 9 | 时间戳属性 | 卡片新增 **创建时间 / 最后修改时间**，仅在详情里以**小字浅色**显示，格式示例 `创建时间：2026-0715-1639`。 |
| 10 | 数据迁移 | 重/急度：**高→8、中→5、低→2**。时间性质：原**持续型**→起始+截止都保留（已安排）；原**截止型**→起始置空、只留截止（已安排，区间=创建→截止 由前端展示时兜底）。旧数据精度可接受损失。 |
| 11 | 排列（日/全部） | 卡片按**创建时间**排列：第一排左=最新，向右递减新度到该排最右；第二排首个比第一排末尾更早；继续向右/向下更早。（即 grid 里按 createdAt 倒序铺，行优先。） |
| 12 | 缩放阈值（日/全部） | 现状缩放时卡片先横向拉伸、大幅变化才改每排数量。**本轮调小拉伸阈值**：让「每排数量」更容易变化，而非优先横向拉伸。 |
| 13 | 持续型展示 | 「持续型」在新模型下重定义=**已安排且起始→截止跨了多天**的卡片。**月视图**：单格内的跨天卡片**不跨格连接**，仅比普通卡片**稍宽**以示区别；**周视图**：**保留跨列连接**（延续 002/003 行为）。 |
| 14 | AI 按钮 | 默认启动时 **AI 助手隐藏**。原「隐藏助手」按钮文字统一为 **「AI」**；隐藏态按钮框**白色**、显示态**蓝色**（深色主题用对应色）。 |
| 15 | 主题按钮 | 主题切换按钮**只显示当前态**：显「浅色」→点击切深色并显「深色」→再点回「浅色」。（去掉「主题：」前缀，二态切换。） |
| 16 | 页边距 bug | 所有视图留出页边距；修复周/月视图最左侧格/列**超出页面**的问题。 |
| 17 | 竖向排列 bug | 修复周/月视图下任务**竖向排列拥挤/重叠**的问题。 |
| 18 | 登出键 | **删除登出键**（位置让给垃圾箱图标）。 |

### 触及后端/DB 的改动（少数几条，plan 里重点标）
- **重/急度 0~10**：data-model 字段类型由枚举改整数 + migration（迁移旧枚举值）。
- **取消时间性质 + 未安排语义**：移除/废弃 `timeNature` 字段、调整时间字段可空规则、GET /api/cards 的 view 过滤逻辑（未安排只进 all）+ migration。
- **卡片状态机（活跃/已完成/已删除 + 进箱时间）**：新增状态字段与 `trashedAt` 时间戳、完成/删除/恢复/永久删除的接口语义（软删除模型）+ migration。
- **创建/修改时间戳**：`createdAt` 已有则复用；新增 `updatedAt`，更新时刷新。
- 其余（坐标视图、拉伸阈值、排列、持续型展示、AI/主题按钮、页边距、竖向拥挤、创建确认、详情编辑态）**预期纯前端**。

---

## 第四轮 Spec Kit 指令（宪法对齐跑完后，依次粘进 Cursor Agent 面板）

> 前提：先跑完上面「宪法对齐」小节的 `/speckit-constitution`（宪法到 1.2.0）。当前激活 feature 是 003，直接跑 `/speckit-specify` 会自动新建 **004** 并把 `.specify/feature.json` 指针切过去——**开新轮 = 跑 specify**。
> 铁律不变：**specify 只谈「做什么/为什么」，技术栈（字段类型、migration、组件、坐标绘制方案）留到 plan。**
> 本轮含**数据模型重构**（重/急度 0~10、取消时间性质、卡片状态机），是三轮里改动最深的一轮，specify 指令显式带「现状 → 目标」并点明 delta。

### ② `/speckit-specify` — 写第四轮基线规格

```
/speckit-specify 这是 Watson 的第四轮迭代，命名为「优先级量化、垃圾箱与坐标视图」。在已上线的 001/002/003 之上，重构优先级与时间模型、引入卡片生命周期（完成/删除/恢复的垃圾箱）、新增二维坐标视图，并做一批交互与视觉修正。以下按用户价值描述要做什么，不涉及技术实现：

【优先级量化】
1. 现状：日程的重要程度、紧急程度都是「高/中/低」三档。目标：改为 0~10 的整数刻度（0 和 10 都可选）。所有者点击该属性时，竖向拉开一列数字（最下 0、最上 10），点选具体数字即设定该值。旧有的高/中/低数据需被合理转换为对应的数值。

【时间模型重构】
2. 现状：卡片有「时间性质」（持续型=起止时间 / 截止型=单一 deadline），且第三轮规定选了时间性质就必须填齐时间。目标：取消「时间性质」这一属性。卡片关于时间只有两种情况——「已安排」：所有者主动填写了起始时间（并可选填截止时间，此时视为一个时间区间）；「未安排」：所有者未填写任何时间字段（起始时间也不代填）。未安排的卡片只出现在「全部」视图，不进日/周/月视图。已安排卡片按其时间正常进入相应视图。

【卡片生命周期与垃圾箱】
3. 现状：卡片只有活跃态，删除即消失且只能靠界面删除（带二次确认）。目标：卡片引入「完成」与「删除」两种归档去向，二者都进入一个「垃圾箱」，进箱后从所有常规视图（日/周/月/全部/坐标）消失。
   - 完成：所有者可将一张日程标记为「已完成」，标记后该卡片进入垃圾箱（完成操作无需二次确认）。
   - 删除：所有者删除一张日程时进入垃圾箱（删除保留二次确认）。
   - 垃圾箱入口：页面右上角一个垃圾箱图标（取代原「登出」按钮的位置）。点击打开垃圾箱。
   - 垃圾箱视图：呈现方式类似「全部」视图，但按「进入垃圾箱的时间」倒序排列——最近进箱的在左上，较早的在右下。已完成的卡片标一个对勾标记，已删除的卡片标一个红色圆形标记，以区分两种进箱原因。
   - 恢复与永久删除：垃圾箱内每张卡片都可以「恢复」回活跃状态（重新出现在常规视图），也可以「永久删除」（不可逆，需二次确认）。

【快捷完成入口】
4. 现状：无一键完成。目标：提供两个完成入口——① 打开卡片详情后，右上角有一个方框，点击后出现对勾，该日程即视为完成并进入垃圾箱；② 在日视图和全部视图中，每张卡片右侧也有一个方框，点击出对勾即完成并进箱。两个入口语义一致。

【二维坐标视图】
5. 现状：日/全部视图是卡片网格/列表。目标：在日视图与全部视图各加一个「坐标视图」按钮，点击后在页面中心打开一个二维坐标视图：以「重要程度 5、紧急程度 5」为原点建立十字坐标系，横轴为紧急程度、纵轴为重要程度，右上方向代表更重要更紧急。把当前所在视图（日 or 全部）中的活跃卡片按其（紧急度, 重要度）坐标落点；同一坐标点上的多张卡片聚合为一个圆，该点任务越多圆越大；鼠标悬停某个圆时弹出信息框，列出该圆所包含的所有任务名称。已完成/已删除的卡片不出现在坐标视图中。

【卡片详情：即时编辑】
6. 现状：点击卡片打开的详情需先点「编辑」才能改，右下角有「编辑」「关闭」，删除/编辑走二次确认。目标：详情打开即可直接编辑各属性，无需先点编辑。右下角改为两个键：「恢复」（默认不可点，一旦本次有修改即可点，作用是把所有字段重置回本次打开详情时的原值，但不关闭详情）与「确认」（保存修改并关闭）。点击详情面板以外的区域＝放弃本次修改并关闭（等同取消）；只有点「确认」才保存。整个过程不再弹二次确认窗。若保存时标题为空或与其他日程重名，则就地提示错误、不关闭也不保存。

【时间戳属性】
7. 目标：每张卡片记录「创建时间」与「最后修改时间」，仅在打开卡片详情时以小号浅色文字展示（示例格式 创建时间：2026-0715-1639），不在常规卡片面上显示。

【交互与文案修正】
8. 删除「登出」按钮（其位置让给垃圾箱图标）——单用户场景下登出无实际意义。
9. 默认启动时右侧 AI 助手为隐藏状态。切换 AI 显隐的按钮文字统一为「AI」，隐藏时按钮为白色、显示时为蓝色（深色主题使用对应配色）。
10. 主题切换按钮只显示当前主题名：显示「浅色」时点击切到深色并显示「深色」，再点回「浅色」，如此二态切换。
11. 详情内的「删除该日程」文案精简为「删除」；点删除后弹出的二次确认框宽度收窄（当前过宽）。
12. 手动创建（+）恢复确认按钮：最终体验是「点 + 号后，无论按 Enter 还是点确认按钮都能成功创建」，仍然只有标题必填。

【视觉与布局】
13. 日/全部视图的卡片按创建时间排列：第一排最左为最新创建、向右递减到该排最右，第二排首个比第一排末尾更早，依此向右向下越来越早。
14. 日/全部视图在页面缩放时，降低卡片「横向拉伸」的阈值，使「每排卡片数量」更容易随宽度变化，而不是优先把卡片拉宽。
15. 持续性日程（已安排且起止跨多天）的展示：月视图中单个日期格内的跨天卡片不做跨格连接，仅比普通卡片略宽以示区别；周视图保留跨列的连续条带表示。
16. 所有视图留出页边距，修复周/月视图最左侧格子/列超出页面边界的问题。
17. 修复周/月视图下日程竖向排列拥挤、相互挤压的问题。

约束与边界：本轮不改变单用户鉴权与「所有日程操作经 UI/API 结构化路径、AI 不操作日程」的宪法约束。完成/删除/恢复/永久删除必须复用统一的卡片生命周期语义。取消时间性质后，「仅标题必填」「未安排只进全部视图」「标题不可重名」等既有规则需与新时间模型彼此自洽。
```

> 预期它会抛几处 `[NEEDS CLARIFICATION]`，见下面 clarify 一并答。

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

倾向答案（它没问到就主动补，均已在上面定案表拍板）：
- **未安排判定**：用户没填任何时间字段即未安排，起始时间不代填；未安排卡片只进全部视图，可正常参与全部视图的筛选/排序。
- **旧数据迁移**：重/急度 高=8、中=5、低=2；原持续型保留起止、原截止型起始置空只留截止。
- **完成 vs 删除**：完成无需二次确认；删除保留二次确认；垃圾箱内永久删除需二次确认；两者都可从箱内恢复回活跃。
- **进箱后可见性**：完成/删除进箱后从所有常规视图与坐标视图消失，仅在垃圾箱可见。
- **坐标视图取值范围**：全部视图开坐标视图时也纳入未安排卡片（它们仍有重/急度）；已完成/删除的不纳入。恰好落在轴线（某维=5）的卡片：按「≥5 归右/上侧」处理。
- **坐标聚合圆**：圆半径设上限防溢出；悬停信息框任务名过多时显示前若干条 + 「+N」。
- **详情编辑点外部**：点外部＝放弃修改并关闭（取消语义），仅「确认」保存；校验失败就地红字提示不关闭。
- **时间戳格式**：创建/最后修改时间按 `YYYY-MMDD-HHmm` 小字浅色示例格式显示（沿用 Asia/Shanghai）。

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上迭代，沿用既定选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向，并明确标注纯前端 vs 触及后端/DB：

【触及后端 + DB（需 Drizzle migration，注意旧数据兼容）】
- 优先级量化：schedule_cards 的 importance / urgency 由枚举列改为 0~10 整数列；写一条 migration 把历史值映射为 高=8/中=5/低=2；create/update 校验值域 [0,10]。
- 时间模型重构：移除或废弃 timeNature 列（保留列但停用，或迁移删除，二选一并说明兼容策略）；起始/截止时间字段改为可空；「未安排」= 起止均空。GET /api/cards 的 view 过滤：未安排卡片仅在 view=all 返回，日/周/月按已安排时间过滤。migration 处理旧持续型（保留起止）与旧截止型（起始置空、仅留截止）。
- 卡片生命周期（软删除 + 完成）：新增状态字段（如 status: active | completed | deleted）与 trashedAt 时间戳；新增/调整接口——完成（PATCH 状态→completed 并置 trashedAt）、删除（软删→deleted 并置 trashedAt，保留既有二次确认）、恢复（→active、清 trashedAt）、永久删除（真正 DELETE，需二次确认）。所有常规查询默认只返回 active；垃圾箱查询返回 completed+deleted，按 trashedAt 倒序。坐标视图数据同样只取 active。
- 时间戳：复用既有 createdAt；新增 updatedAt，任何 update/编辑保存时刷新。详情返回这两个字段。
- 重名约束沿用第三轮的 title_lower 唯一策略，但需只对 active（或 active+completed，按定案）生效——明确「已删除卡片是否占用标题」：倾向仅 active 卡片参与重名判定，进箱后释放标题。请在 plan 里定死这一点。

【纯前端】
- 0~10 数字选择器：点击重/急度属性弹出竖向数字列（下 0 上 10）的选择控件，复用在创建表单与详情编辑态。
- 垃圾箱页面：新增 TrashView，仿 AllView 布局，按 trashedAt 倒序；completed 卡片渲染对勾角标、deleted 卡片渲染红色圆形角标；每卡提供「恢复」「永久删除」操作（永久删除前二次确认，复用 ConfirmDialog）。TopBar 右上角以垃圾箱图标替换登出按钮入口。
- 快捷完成：详情右上角 + 日/全部视图卡片右侧各加一个 checkbox 方框，点击→调用完成接口→invalidate 相关查询使其从当前视图消失。
- 坐标视图：日/全部视图加「坐标视图」切换；新增 QuadrantView 组件，用 SVG/canvas 画十字坐标系（原点=(5,5)，X=紧急 Y=重要，右上更重更急）；按 (urgency,importance) 分桶聚合，圆半径随该桶计数增长并设 max 上限；hover 弹 tooltip 列任务名（超量 +N）。数据来自当前视图的 active 卡片（全部视图含未安排卡片）。
- 详情即时编辑：改造 CardDetailModal 为始终可编辑；右下角「恢复」（重置为打开时快照，dirty 后才启用，不关闭）+「确认」（保存并关闭）；点遮罩外部＝丢弃并关闭；移除原编辑/删除二次弹窗流程中的编辑弹窗，保留删除的二次确认（框体收窄）；保存时前端 + 后端双重校验（标题必填、不可重名），失败就地红字。
- 创建确认回归：CreateCardModal 恢复确认按钮，同时保留 Enter 提交（标题非空即可），二者等价。
- 视觉：日/全部视图卡片 grid 按 createdAt 倒序行优先排布；调小 grid 的 min 列宽 / auto-fit 阈值，使列数更早随宽度变化而非拉伸单卡（用 CSS grid auto-fill/minmax 调参）；持续型（跨天已安排）卡片在月视图单格内加宽标识、不跨格，周视图保留 SpanBar 跨列；给各视图容器统一内边距修复越界；排查并修复周/月视图竖向排列的高度/间距导致的拥挤重叠。
- AI 按钮：默认 chatOpen=false（隐藏）；切换按钮文案固定「AI」，隐藏态白底、显示态蓝底（深色主题对应配色，走 CSS 变量）。
- 主题按钮：显示当前主题名（浅色/深色）的二态切换，去掉「主题：」前缀。
- 删除登出：移除 TopBar 登出按钮及其调用（会话仍可通过 cookie 过期自然失效，无 UI 登出入口）。

请产出实现方案，重点标注：三处 migration（优先级值域、时间模型、生命周期状态）的顺序与旧数据兼容处理；「未安排 vs 已安排」在后端查询与前端各视图的一致过滤口径；重名判定的作用范围（仅 active）；坐标视图的分桶/聚合与轴线归属规则；哪些改动纯前端、哪些触及后端与 DB（预期后端集中在优先级值域、时间模型、生命周期状态机、时间戳这几处）。
```

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成后**人工审阅**：确认任务按「DB migration（优先级值域 → 时间模型 → 生命周期状态）→ 后端接口（完成/删除/恢复/永久删除/未安排过滤/时间戳）→ 优先级 0~10 选择器 → 垃圾箱视图 → 快捷完成入口 → 坐标视图 → 详情即时编辑 → 创建确认回归 → 视觉与布局修正 → AI/主题按钮 + 删登出」分组，每条带明确文件路径，并把触及后端/DB 的少数几条清晰标出。

### 【动手前】`/speckit-analyze` — 查一致性

```
/speckit-analyze
```

> 只读检查 spec/plan/tasks 三者是否自洽，尤其盯这几组相互牵连的规则有没有矛盾：
> - 「取消时间性质」×「未安排只进全部视图」×「持续型（跨天）在月/周视图的展示」是否自洽；
> - 「完成/删除都进箱且退出所有视图」×「坐标视图只取 active」×「重名判定仅对 active」是否一致；
> - 「详情点外部=取消」×「恢复键=重置不关闭」×「确认=保存关闭」三态无歧义。
> 有问题先回改规格，再进 implement。

### ⑥ `/speckit-implement` — 分批实现（务必分批，本轮改动深）

```
/speckit-implement 只执行第 N 阶段的任务，完成后停下等我验证，不要继续后续阶段。
```

> 强烈建议按下面顺序**逐批跑、每批 `npm run build` 双端零错误 + `npm run dev` 手动验证**再进下一批。尤其前两批动了数据库和数据模型，最容易连带打挂既有功能：
> 1. **DB migration + 后端生命周期/时间模型/优先级值域**（最高风险，单独成批；migration 跑完先确认旧数据被正确迁移，再验证 GET /api/cards 各 view 过滤、完成/删除/恢复/永久删除接口）。
> 2. **优先级 0~10 选择器 + 时间戳显示**（前端消费新字段）。
> 3. **垃圾箱视图 + 快捷完成入口**（生命周期的前端闭环）。
> 4. **坐标视图**（相对独立，可单独验证聚合圆与 tooltip）。
> 5. **详情即时编辑 + 创建确认回归 + 删除确认收窄**（交互重构，注意别丢失校验）。
> 6. **视觉与布局修正 + AI/主题按钮 + 删登出**（纯样式收尾）。
> 每批完成用「继续执行第 N+1 阶段」推进；它靠 tasks.md 的 `[X]` 标记断点续跑。

### 全部做完后 `/speckit-converge` — 兜底查缺

```
/speckit-converge
```

> implement 全跑完后用它对照 spec/plan/tasks 扫缺口，append 成新任务再补齐。converged 即本轮收口。别忘了本轮改了数据模型，收口后建议再跑一次端到端：登录 → 建卡（已安排/未安排各一）→ 改优先级 0~10 → 完成/删除进箱 → 箱内恢复/永久删除 → 坐标视图 → 主题/AI 按钮，确认全链路无回归。

# Watson - 第五轮 Spec Kit 迭代
新增内容:
- 在登陆页面下方留出占位符用于放置备案要求的图标和信息
- 日视图和月视图应当和周视图同样具有“上一周”“本周”“下一周”类似的按键和功能
- 日视图的坐标视图按键删除，无需点击坐标视图按键，而是直接出现在日视图下的右侧（用一个圆角方形的凹陷效果背景和其他部分做区分，然后方形内部是二维图）
- 新增交互，任何视图下，用户可以拖拽卡片到垃圾箱。

美术风格调整:
- 周视图下的每个日的框体以及月视图下最下面那一排。下边沿需要向上稍微调整（月视图下是整体向上收缩一点），留出更多空间在页面底部。
- 周视图下的每个日的日程的长条框体需要对齐间隔，最高处稍微向下调整。
- 周视图的日程条应该是现在的2.5倍粗。

待修复的bug:
- 用户点击网页内上方导航栏的日历后，日历有一个删除选项，点击删除后，页面会直接清空（刷新后才可以恢复）。
- 用户导航栏上的日历中的删除键没有意义，应修改为”回到今天“
- 用户点击全部视图后，导航栏中的日历会消失，此设计没有意义并且影响观感。无论切换到什么视图，日历都不需要消失，并且可以继续调整，只不过在全部视图下不会引起主视图部分的改变而已。
- 创建日程时，当前必须先填写开始时间才能填写结束时间，正确的逻辑应该是：用户可以两个时间都不填，视为未安排的卡片；或者只填结束时间，此时默认开始时间就是创建卡片的时刻；但用户填了开始时间，就必须存在结束时间，结束时间默认是24h之后，也就是说用户修改完开始时间后，结束时间自动变为24h之后，然后用户再根据实际情况修改结束时间；

---

## 第五轮决策定案（澄清后的权威口径，与上面原始需求对照阅读）

> 上面是原始口述需求；下面是逐条逻辑分析后拍板的定案。**遇到两者措辞冲突，以本表为准。** 下面的 speckit 指令都按本表写。
> ⚠️ 本轮最重要的一点：**需求 694 主动改写了第四轮定的时间模型**，新规则彻底取代旧规则（见 #6），务必让 specify/plan/analyze 都盯住这处 delta。

| # | 议题 | 定案 |
|---|---|---|
| 1 | 备案占位（登录页） | 登录页下方留出一块**空占位区**（放备案图标/信息的位置），本轮只留位、**暂不填真实备案号**（尚未备案）。占位需在明暗主题下都不破坏登录页现有视觉。 |
| 2 | 三视图前后导航 | 日/周/月三个视图都要有与周视图**对等**的前后翻页 + 回到当前按钮，**各按自身时间粒度**：日视图=前一天 / 今天 / 后一天；周视图=上一周 / 本周 / 下一周（维持现状）；月视图=上一月 / 本月 / 下一月。（原口述「日/月也用上一周本周下一周」是话术照抄，实际按各自粒度。） |
| 3 | 日视图内嵌坐标视图 | **仅日视图**：删除「坐标视图」切换按钮，坐标二维图**常驻显示在日视图右侧**，用一个圆角矩形、凹陷效果背景与主区域区分，方框内部即二维坐标图。**全部视图的坐标视图保持第四轮的「按钮点击弹出」方式不变。** |
| 4 | 拖拽卡片到垃圾箱 | 新增交互：**任何视图**下用户可把日程卡片**拖拽到右上角垃圾箱图标**，松手即视为**删除**（进箱后标红色圆形，与「完成」的对勾区分）。**拖拽删除不弹二次确认**（拖拽动作本身即确认）。注意：这与「点详情内删除按钮仍弹二次确认」并存——两条删除路径，按钮删除留确认、拖拽删除不留。 |
| 5 | 全部视图恢复日历 | 现状：切到全部视图时导航栏日期控件被隐藏（代码 `view !== "all"` 条件）。目标：**全部视图也显示日历/日期控件且可正常调整**，只是全部视图下调整它**不影响主视图内容**（全部视图不按日期过滤）。**垃圾箱视图仍不显示**日期控件（对按进箱时间排序的箱无意义）。 |
| 6 | 时间字段填写逻辑（**取代第四轮时间模型**） | 现状：结束时间输入框在没填开始时间时被禁用（`disabled={!values.startAt}`），且校验要求「填结束必先有开始」。新规则三种情况：**A 两个都空 = 未安排**（只进全部视图）；**B 只填结束 = 已安排**，开始时间默认为创建卡片的时刻（区间=创建时刻→结束）；**C 填了开始 = 已安排且必须有结束**，用户改完开始时间后结束时间**自动置为「开始+24h」**，用户可再手动改，但**不允许最终没有结束时间**。→ 即：已安排卡片在存储层**一定同时有开始与结束**；「只有开始、无结束」这种第四轮曾允许的状态**本轮起不再存在**。 |
| 7 | 底部留白（周/月） | 周视图每个「日」框体、月视图最下面一排的**下边沿向上收**（月视图整体向上收缩一点），在页面底部留出更多空间，避免贴边。 |
| 8 | 周视图日程条对齐 | 周视图每个日内的日程长条框体需**对齐间隔**，最高处（顶端起始位置）**稍微向下调整**，不要顶到格子最上沿。 |
| 9 | 周视图日程条加粗 | 周视图的日程条粗细改为**当前的 2.5 倍**。 |
| 10 | bug：日历清除导致清空 | 现状：导航栏日期控件是原生 `<input type="date">`，其自带的「清除（✕）」会把 anchorDate 置空，导致主视图算不出锚点日期而清空，刷新后重新初始化才恢复。修复：**移除/禁用该清除入口**，日期控件不允许被清空为无效值；锚点日期始终有效。（这就是原口述说的「日历的删除选项」。） |
| 11 | bug：日历「删除/清除」改为「回到今天」 | 与 #10 同源：把那个无意义的清除入口**替换为「回到今天」功能**——一个明确的按钮/操作，点击后把锚点日期重置为今天（配合 #2 的「今天/本周/本月」语义统一）。 |

### 触及后端/DB 的改动（本轮预期极少）
- **仅 #6 时间模型规则收紧可能触及后端校验**：create/update 的校验逻辑需改为——已安排卡片必须同时有开始与结束（只填结束时后端补开始=创建时刻，或由前端提交时补齐后端只做一致性校验，plan 里定死在哪层补）。data-model 字段本身第四轮已是可空的开始/结束，**预期无需新 migration**，只调校验与默认值逻辑。若第四轮已存在「只有开始无结束」的历史卡片，需一条兜底把它们的结束补为「开始+24h」——plan 里评估是否需要。
- 其余（备案占位、三视图导航、日视图内嵌坐标、拖拽删除、全部视图显示日历、周/月留白与日程条样式、日历清除改回到今天）**预期纯前端**。

---

## 第五轮 Spec Kit 指令（第四轮收口后，依次粘进 Cursor Agent 面板）

> 前提：第四轮（004）已 implement + converge 收口。当前激活 feature 是 004，直接跑 `/speckit-specify` 会自动新建 **005** 并把 `.specify/feature.json` 指针切过去。
> 铁律不变：**specify 只谈「做什么/为什么」，技术栈（组件、拖拽实现、日期控件替换方案、校验落点）留到 plan。**
> 本轮多为**改已有行为 + bug 修复**，且 #6 会**推翻第四轮的时间模型**，specify 指令显式带「现状 → 目标」并点明它取代第四轮的哪条规则。

### ② `/speckit-specify` — 写第五轮基线规格

```
/speckit-specify 这是 Watson 的第五轮迭代，命名为「视图导航、拖拽归档与时间模型收紧」。在已上线的 001~004 之上，补齐各视图的时间导航、把日视图的优先级坐标图常驻化、新增拖拽删除、修复导航栏日历相关缺陷，并收紧日程的时间填写规则。以下按用户价值描述要做什么，不涉及技术实现：

【新增能力】
1. 登录页备案占位：在登录页下方留出一块占位区域，用于日后放置网站备案（ICP 等）要求的图标与信息文字。本轮仅预留该位置（暂不填入真实备案号），占位在明暗两种主题下都不应破坏登录页现有的视觉风格。
2. 各视图时间导航：日视图、月视图应当像周视图一样，具备"前一 / 回到当前 / 后一"的时间导航控件，按各自的时间粒度——日视图在相邻日之间前后切换并可一键回到今天；月视图在相邻月之间前后切换并可一键回到本月；周视图维持现有的上一周 / 本周 / 下一周。
3. 日视图内嵌优先级坐标图：现状——日视图里有一个"坐标视图"按钮，点击才打开二维优先级图。目标——日视图取消该按钮，把二维坐标图常驻显示在日视图的右侧区域，用一个带圆角、凹陷质感背景的方形框与主日程区域作视觉区分，方框内部即该二维优先级图（横轴紧急、纵轴重要，原点为 5、5，右上更重更急，沿用第四轮坐标图的其余行为）。注意：全部视图里的坐标视图维持第四轮"按钮点击打开"的方式不变，本条只改日视图。
4. 拖拽归档到垃圾箱：新增一种交互——在任何视图下，用户都可以把一张日程卡片拖拽到右上角的垃圾箱图标上，松手即把该卡片删除并送入垃圾箱（在箱内以"已删除"的红色圆形标记呈现，与"已完成"的对勾区分）。拖拽删除这一动作本身即视为确认，无需再弹二次确认框。（保留另一条路径：从卡片详情里点"删除"按钮删除时仍走既有的二次确认。）

【视觉调整】
5. 周视图每个"日"的框体、以及月视图最下面一排的下边沿，需要整体向上收一些（月视图整体向上收缩），在页面底部留出更多空白，避免内容贴住页面底边。
6. 周视图中每个日内的日程长条，需要对齐彼此的间隔，且其顶端起始位置稍微向下移一点，不要顶到格子最上沿。
7. 周视图的日程长条粗细增加到当前的约 2.5 倍。

【缺陷修复】
8. 导航栏日历清空缺陷：现状——导航栏的日期选择器带有一个"清除"入口，用户触发后当前锚定日期被清空，导致主视图内容直接清空，只有刷新页面才能恢复。目标——锚定日期必须始终是一个有效日期，不允许被清空成无效状态；移除这个会导致清空的"清除/删除"入口。
9. 把上述无意义的"清除/删除"入口替换为"回到今天"：提供一个明确的操作，点击后把锚定日期重置为今天（与第 2 条日/周/月各视图的"回到当前"语义一致）。
10. 全部视图不再隐藏日历：现状——切换到全部视图时，导航栏的日期控件会消失。目标——无论切到哪个常规视图，日期控件都保持显示且可调整；只是在全部视图下调整它不会改变主视图内容（全部视图本就不按单一日期过滤）。（垃圾箱视图仍无需显示日期控件，因为它按进箱时间排序。）
11. 时间填写规则收紧（本条取代第四轮的时间模型）：现状——创建日程时必须先填开始时间才能填结束时间；第四轮曾允许"只有开始时间、没有结束时间"的已安排卡片。目标——重新定义为三种情况且互斥：① 开始与结束都不填 = 未安排卡片（只出现在全部视图）；② 只填结束时间 = 已安排卡片，其开始时间默认为创建该卡片的时刻（形成"创建时刻→结束"的区间）；③ 填了开始时间 = 已安排卡片且必须有结束时间——用户设定/修改开始时间后，结束时间自动置为"开始时间 + 24 小时"，用户可再据实调整，但不允许最终把结束时间留空。也就是说，从本轮起，凡"已安排"卡片都同时具备开始与结束时间，不再存在"只有开始、没有结束"的卡片。

约束与边界：本轮不改变单用户鉴权、卡片核心字段含义、"AI 不操作日程"等宪法约束，也不改变第四轮已确立的垃圾箱生命周期（完成/删除/恢复/永久删除）语义——拖拽只是新增一条触发"删除"的入口。第 11 条时间规则收紧后，"未安排只进全部视图""标题不可重名""已安排卡片一定有起止"等规则需彼此自洽，并与第四轮的坐标视图、各日历视图的取数逻辑保持一致。
```

> 预期它会就一两处抛 `[NEEDS CLARIFICATION]`，见下面 clarify 一并答。

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

倾向答案（它没问到就主动补，均已在上面定案表拍板）：
- **时间规则（#6/11）**：已安排卡片一定同时有开始+结束；只填结束→开始补为创建时刻；填开始→结束自动补开始+24h 且可改但不能清空；两者皆空=未安排。若库里有第四轮遗留的"只有开始无结束"卡片，兜底把结束补为开始+24h。
- **拖拽删除**：等于"删除"（红色圆标记），不弹二次确认；详情内删除按钮仍保留二次确认。
- **日视图坐标图常驻**：只改日视图（右侧凹陷方框常驻）；全部视图保持按钮弹出式。
- **全部视图日历**：全部视图恢复显示且可调，但不影响主视图；垃圾箱视图不显示日期控件。
- **日历"清除"入口**：移除并替换为"回到今天"；锚定日期始终有效、不可为空。
- **备案占位**：只留空位，不填真实备案号；明暗主题下均不破坏登录页视觉。
- **三视图导航粒度**：日=±1 天+今天，月=±1 月+本月，周维持。

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上做以前端为主的迭代，沿用既定选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向，并明确标注纯前端 vs 触及后端：

【可能触及后端（预期无新 migration，只改校验/默认值）】
- 时间规则收紧：把"已安排卡片必须同时有开始与结束"落实为校验规则。明确在哪层补齐：前端 CardFormFields/validateCardForm 负责——只填结束时提交前把开始补为创建时刻（或提交 null 开始、由后端补为 now），填开始时自动把结束设为开始+24h；后端 create/update 校验做一致性兜底（有开始必有结束、结束≥开始）。现有 CardFormFields 里 `disabled={!values.startAt}` 与 validate 中「填结束需先有开始」的旧限制要一并改掉/反向。评估是否需要一次性数据兜底：把库中"有开始无结束"的历史卡片结束补为开始+24h（若存在）。

【纯前端】
- 登录页备案占位：在 LoginPage 底部加一块预留占位容器（如固定高度的 footer 区，含占位文字/图标位），响应式且明暗主题适配，暂不填真实备案信息。
- 各视图时间导航：把周视图已有的前后翻页控件抽象/复用，给日视图（±1 天 + 今天）、月视图（±1 月 + 本月）加对等控件；统一操作 anchorDate（沿用 TopBar 的 anchorDate/onDateChange 与 date-fns 计算）。
- 日视图内嵌坐标图：DayView 布局改为左侧日程 + 右侧常驻坐标图（圆角、inset/凹陷背景的方形容器），复用第四轮 QuadrantView 组件（原点 5,5 / X 紧急 Y 重要 / 右上更重急）；移除日视图的"坐标视图"切换按钮。AllView 保持按钮弹出式不动。
- 拖拽删除：给各视图的卡片加 draggable，垃圾箱图标作为 drop target；drop 时调用既有"删除→进箱"接口（deleted 状态 + trashedAt），无二次确认；用 HTML5 DnD 或指针事件实现，注意移动端触摸兜底（若不做触摸拖拽，需在 spec 注明降级）。详情内删除按钮的二次确认保持不变。
- 全部视图日历：去掉 TopBar 中日期控件对 `view === "all"` 的隐藏（保留对 trash 的隐藏）；全部视图下 anchorDate 变化不触发主视图重查（AllView 不消费 anchorDate 过滤）。
- 日历清除→回到今天：把 TopBar 的原生 `<input type="date">` 换成不可清空的日期控件（自定义封装或屏蔽其 clear），并新增"回到今天"按钮把 anchorDate 设为今天；保证 anchorDate 永不为空。
- 周/月视觉：周视图日框体与月视图末行下边沿上收留白（调容器 padding/height 计算）；周视图日程条对齐间隔、顶端下移、粗细×2.5（改 SpanBar / weekGrid 的尺寸与定位常量）；排查这些改动是否与第四轮"竖向拥挤"修复冲突。

请产出实现方案，重点标注：时间规则收紧的校验落点（前端补齐 vs 后端兜底）与历史数据兼容；拖拽删除的实现方式与移动端降级；日期控件替换为不可清空 + 回到今天的方案；日视图左右布局对既有 DayView 的改动面；哪些纯前端、哪些触及后端（预期仅时间校验一处）。
```

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成后**人工审阅**：确认任务按「时间规则收紧（校验+可能的数据兜底）→ 各视图时间导航 → 日视图内嵌坐标图 → 拖拽删除 → 全部视图恢复日历 → 日历清除改回到今天 → 登录页备案占位 → 周/月视觉调整」分组，每条带明确文件路径，并把可能触及后端校验的那一条单独标出。

### 【动手前】`/speckit-analyze` — 查一致性

```
/speckit-analyze
```

> 只读检查 spec/plan/tasks 三者是否自洽，尤其盯这几组相互牵连处：
> - **#6 时间规则 vs 第四轮**：新规「已安排必有起止」与第四轮 spec/data-model 里「可只有开始」的旧表述是否有残留矛盾（若第四轮 spec 仍写着旧规则，属预期的跨轮 delta，确认 005 明确声明取代，不要求回改 004 历史规格）。
> - **拖拽删除 vs 详情删除**：一个不确认、一个确认，两条路径是否都指向同一"删除→进箱"语义。
> - **全部视图显示日历 vs 全部视图不按日期过滤**：显示但不影响主视图，逻辑是否讲清。
> 有问题先回改规格再 implement。

### ⑥ `/speckit-implement` — 分批实现

```
/speckit-implement 只执行第 N 阶段的任务，完成后停下等我验证，不要继续后续阶段。
```

> 建议分批顺序（每批 `npm run build` 双端零错误 + `npm run dev` 手动验证再进下一批）：
> 1. **时间规则收紧**（唯一可能触后端的一批，先做并验证：未安排/只填结束/填开始自动补24h 三种情况，以及历史数据兜底）。
> 2. **各视图时间导航 + 全部视图恢复日历 + 日历清除改回到今天**（都围绕 TopBar 与 anchorDate，一起做减少反复）。
> 3. **日视图内嵌坐标图**（DayView 布局重构，单独验证左右布局与坐标图）。
> 4. **拖拽删除**（新交互，单独验证桌面拖拽 + 移动端降级）。
> 5. **登录页备案占位 + 周/月视觉调整（留白、日程条对齐/加粗）**（纯样式收尾）。
> 每批完成用「继续执行第 N+1 阶段」推进；靠 tasks.md 的 `[X]` 断点续跑。

### 全部做完后 `/speckit-converge` — 兜底查缺

```
/speckit-converge
```

> implement 全跑完后用它对照 spec/plan/tasks 扫缺口，append 成新任务再补齐。converged 即本轮收口。收口后建议端到端过一遍：登录（看底部占位）→ 日/周/月视图各翻页+回到当前 → 日视图右侧坐标图常驻 → 建卡验证三种时间情况（未安排/只填结束/填开始自动补24h）→ 从各视图拖卡片进垃圾箱 → 全部视图下日历仍在且可调不影响主视图，确认无回归。
