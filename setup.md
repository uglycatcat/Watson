# Watson — 第一轮 Spec Kit 使用指南

> 本文档指导你**第一轮**如何用 Cursor + GitHub Spec Kit 把 Watson（AI 日程管理系统）从想法推进到可实现的规格与任务。
> 产品定义见 [`README.md`](./README.md)。

---

## 0. 前置：怎么在 Cursor 里用这些命令

Spec Kit 的斜杠命令**不是命令行工具**，而是安装在 `.cursor/skills/` 下的 Cursor 技能。用法：

1. 用 **Cursor 打开本项目目录**（`~/MY_WS/watson`）。
2. 打开 Cursor 的 **Agent 聊天面板**（Cmd/Ctrl + L 或侧边 Chat）。
3. 在输入框里键入斜杠命令，例如 `/speckit-constitution`，后面跟上你的说明文字，回车。

> 这些命令会读写 `.specify/`（宪法、模板）和 `specs/`（规格/方案/任务）目录里的文件。

---

## 1. 第一轮的完整路径

```
① /speckit-constitution   →  ② /speckit-specify   →  ③ /speckit-clarify（可选但强烈建议）
                                                              ↓
⑥ /speckit-implement      ←  ⑤ /speckit-tasks     ←  ④ /speckit-plan
```

- ①②③ 属于**「想清楚做什么」**——不涉及代码。
- ④⑤⑥ 属于**「怎么做 + 做出来」**——这里才引入 Node.js 等技术栈。

**第一轮建议只走到 ⑤（生成任务清单）**，确认任务合理后，再开 ⑥ 让 AI 实现。别一上来就冲到 implement。

---

## 2. 一个关键认知（避免踩坑）⚠️

`/speckit-specify` 只关心 **WHAT（做什么）和 WHY（为什么）**，它会**刻意避开技术实现**——不要在这一步写「用 Node.js」「用 REST API」这类内容，那属于 `/speckit-plan` 的活。

- ✅ specify 阶段说：「用户能用自然语言让系统新建一张日程卡片」
- ❌ specify 阶段别说：「前端 React、后端 Express、数据库用 SQLite」

技术栈（Node.js、前后端、明暗主题实现、鉴权方案的具体技术）统统留到 **`/speckit-plan`**。

---

## 3. 逐步操作 + 示例

### ① `/speckit-constitution` — 确立项目原则

先给项目立「宪法」——即整个项目始终要遵守的硬性原则。它会填好 `.specify/memory/constitution.md`。

**示例输入：**

```
/speckit-constitution 为 Watson（单用户 AI 日程管理系统）确立项目宪法，包含以下原则：

1. 单用户安全第一（NON-NEGOTIABLE）：系统部署在公网但必须只有所有者本人可访问，
   任何未授权访问都视为严重缺陷；鉴权与会话是一等公民需求，不是附加项。
2. 密钥外置：所有 API Key / Token / 服务地址一律通过配置文件或环境变量注入，
   禁止硬编码、禁止进版本控制，必须提供脱敏的示例配置。
3. AI 能力可插拔：LLM provider（OpenAI / Claude / DeepSeek 等）由用户在使用时配置，
   代码不绑定任何单一模型或中转，接口需可切换。
4. 数据以「日程卡片」为核心：所有日程都是结构化卡片，具备时间性质、重要度、紧急度。
5. 多设备一致体验：手机/平板/PC 通过浏览器访问，同一份数据、同一套交互。

请据此生成宪法，版本从 1.0.0 起，ratification date 用今天。
```

> 完成后检查 `.specify/memory/constitution.md`，确认没有残留的 `[占位符]`。

---

### ② `/speckit-specify` — 写基线规格

这一步把「做什么」写成结构化规格（存到 `specs/NNN-<短名>/spec.md`）。**只描述用户价值和行为，不碰技术栈。**

第一轮建议**按功能域分次 specify**，别把整个产品塞进一次。推荐先做最核心的一条：**AI 聊天栏驱动的日程管理**。

**示例输入（核心功能）：**

```
/speckit-specify Watson 的核心能力是「对话式日程管理」。用户界面对标 Cursor：
主区域展示日程，侧边常驻一个 AI 聊天栏。用户在聊天栏用自然语言对话，AI 理解后
对日程数据执行操作：

- 添加：用户说「下周一上午十点体检」，系统自动创建一张日程卡片并填好时间与性质。
- 查询/问答：用户问「我这周三有什么安排？」或「最近有没有 deadline 快到了？」，系统给出答复。
- 修改：用户说「把周会改到下午四点」，系统更新对应卡片。
- 删除：用户说「取消明天的牙医预约」，系统删除对应卡片。

日程以「卡片」为基本单位，每张卡片包含：标题、时间性质（持续型=有开始+结束时间，
或截止型=只有一个 deadline）、重要程度（高/中/低）、紧急程度（高/中/低）、分类。

用户仅有一人（所有者本人），通过手机/平板/PC 浏览器访问。系统部署在公网，
但必须保证只有所有者本人能访问和操作自己的日程。
```

**会发生什么：**
- Spec Kit 生成 `spec.md`，含用户场景、功能需求、成功标准、关键实体（日程卡片）。
- 它可能抛出**最多 3 个** `[NEEDS CLARIFICATION]` 问题（以表格形式列 A/B/C 选项），
  优先问**范围 > 安全/隐私 > 体验**类的关键歧义。**认真回答这些问题**，它会据此改写规格。
- 自动生成质量检查清单 `specs/NNN-.../checklists/requirements.md`。

> 可能被追问的点举例：日程时间冲突了怎么处理？AI 误解指令时要不要二次确认？删除要不要确认？
> 想清楚就直接答，没想清楚就选它给的合理默认项。

**后续可再分次 specify 其余功能域**（可放第二轮，或本轮继续）：
- 「日/周/月/全部 四种日程视图，全部视图带筛选器与排序器」
- 「单用户访问控制：公网部署下只有所有者能登录访问」
- 「明暗双主题 + Cursor 风格布局（顶部控制栏 + 主视觉区 + 侧边聊天栏）」

---

### ③ `/speckit-clarify` —（强烈建议）规划前消歧

在进入 plan 之前，用它把规格里剩余的模糊点系统性问清楚，降低后面返工风险。

**示例输入：**

```
/speckit-clarify
```

（无需参数，它会针对当前 spec 逐条提出结构化问题，你逐个回答。）

---

### ④ `/speckit-plan` — 技术方案（**这里才引入技术栈**）

到这一步才把「怎么做」定下来。把你的技术约束一次性交代清楚。

**示例输入：**

```
/speckit-plan 技术栈与实现约束如下：

- 全栈使用 Node.js。
- 包含 Web 前端 + 后端完整设计；浏览器直接访问，无原生 App。
- UI 对标 Cursor：顶部控制栏 + 主视觉区（日程视图）+ 侧边 AI 聊天栏；支持明/暗两种主题。
- LLM 接入可插拔：通过用户配置的 API Key 调用 OpenAI / Claude / DeepSeek 等
  OpenAI-兼容接口，provider 可切换；Key/BASE_URL/MODEL 走 config.yaml 或 .env，不硬编码。
- 单用户鉴权：公网部署下需一套坚固的访问控制（如强密码登录 + 会话，别人拿到 URL 也进不来），
  支持多设备登录同一账户。
- 数据持久化选型请给出建议（轻量优先，单用户场景）。

请产出实现方案，并标注关键技术决策与取舍。
```

---

### ⑤ `/speckit-tasks` — 拆解为可执行任务

**示例输入：**

```
/speckit-tasks
```

它会把 plan 拆成一份带顺序、可勾选的任务清单（`tasks.md`）。**第一轮到这里先停下**，人工过一遍任务是否合理、粒度是否合适。

---

### ⑥ `/speckit-implement` — 执行实现

确认任务清单没问题后，再让 AI 按任务实现：

```
/speckit-implement
```

> 建议**分批实现 + 每批验证**，不要一次性跑完全部任务。按 coding 惯例：改完实际运行、给出运行输出/截图作为证据。

---

## 4. 第一轮推荐节奏（TL;DR）

| 步骤 | 命令 | 产物 | 第一轮是否必做 |
|---|---|---|---|
| 1 | `/speckit-constitution` | `constitution.md` | ✅ 必做 |
| 2 | `/speckit-specify` | `specs/NNN-*/spec.md` | ✅ 必做（先做核心「AI 聊天栏管日程」） |
| 3 | `/speckit-clarify` | 更新 spec | ⭐ 强烈建议 |
| 4 | `/speckit-plan` | `plan.md` | ✅ 必做（此时定 Node.js 等技术栈） |
| 5 | `/speckit-tasks` | `tasks.md` | ✅ 必做（人工审阅后再往下） |
| 6 | `/speckit-implement` | 代码 | 🔸 审阅任务后，分批做 |

**核心提醒：**
1. specify 阶段只谈「做什么」，技术栈留到 plan。
2. 认真回答 specify/clarify 的反问——歧义不消，后面必返工。
3. 安全（单用户私有访问）是本项目一等公民，从 constitution 到 plan 都要盯住它。
4. tasks 生成后先审阅再 implement，别盲目一把梭。

# Watson - 第二轮 Spec Kit 迭代
第一轮结束之后我根据测试结果提出以下的新要求
1. 在主题中，应当只有light和dark，删除system。
2. 在主导航栏类似于cursor添加一个控件，可以控制右侧AI助手面板隐藏或者出现
3. AI助手栏可以拖动边缘控制宽度
4. 月视图下，主视图界面中应该是当月的日历，每个“日”中有卡片。鼠标可以点击卡片查看日程详细信息
5. 周视图下，主视图界面应该为周一～周日的设计，每个日中有小卡片，鼠标可以点击卡片查看日程详细信息
6. 日视图下，直接就是当日日程卡片，
7. 全部视图下，直接就是所有日程卡片
8. 点击卡片之后，出现卡片的详细信息，包括该日程的基本属性，删除该卡片等功能
9. 在主导航栏添加一个新的“+”号表示手动创建日程

---

## 第二轮 Spec Kit 指令（可直接粘进 Cursor Agent 面板）

> 说明：本轮是对已上线的 `001-conversational-scheduling` 的**界面与交互迭代**，不改动数据模型与后端 CRUD 语义。因此**跳过 constitution**（宪法未变），直接走 `specify → clarify → plan → tasks → implement`。
> 沿用第一轮铁律：**specify 只谈「做什么/为什么」，技术栈（React/Tailwind/组件方案/拖拽实现）留到 plan。**
> 上面 9 条里既有**修改已有行为**（主题去掉 system、月/周视图从平铺列表改成日历网格），也有**新增能力**（聊天栏折叠+拖拽、卡片详情弹层、手动 + 创建），所以下面的 specify 指令里显式带上「当前是什么样、要变成什么样」，让规格能准确表达 delta。

---

### ② `/speckit-specify` — 写第二轮基线规格

```
/speckit-specify 这是对已实现功能 001-conversational-scheduling 的第二轮界面交互迭代，命名为「日程可视化与手动操作增强」。根据首轮测试反馈，需要改进日程的浏览体验、AI 助手栏的可控性，并补齐不依赖对话的手动操作入口。以下按用户价值描述要做什么，不涉及技术实现：

【主题偏好收敛】
- 现状：主题有 light / dark / system 三种，控制栏按钮在三者间循环切换。
- 目标：只保留 light 与 dark 两种。所有者点击主题控件时在明、暗之间直接切换，不再有「跟随系统」选项。历史上已保存为 system 的偏好应被视作一个明确的默认值（取 light），不得让界面停留在无法切换的状态。

【AI 助手栏可控性】
- 现状：侧边 AI 聊天栏在桌面端常驻显示、宽度固定，仅移动端有一个显示/隐藏按钮。
- 目标一：在顶部主导航栏提供一个常驻控件（对标 Cursor 的侧栏开关），所有者在任意设备上都能一键隐藏或重新显示右侧 AI 助手面板；隐藏后主日程区域占满整幅宽度。
- 目标二：AI 助手栏显示时，所有者可以拖动它与主区域之间的边缘来调整助手栏宽度；宽度应有合理的最小/最大范围，避免拖到不可用；调整后的宽度在同一设备上应被记住。

【四种视图的呈现方式】
- 现状：日/周/月/全部四种视图当前都只是把命中的卡片平铺成一个列表，月和周并没有真正的日历样式。
- 月视图目标：主区域呈现「当月日历」——按周排列的日期网格，每个日期格子里展示当天的日程卡片（数量多时可省略/折叠显示）。点击某张卡片可查看该日程的详细信息。
- 周视图目标：主区域呈现「周一至周日」的一周布局，每一天一列或一格，其中放当天的小卡片。点击某张小卡片可查看该日程的详细信息。
- 日视图目标：直接展示当日的日程卡片（保持卡片列表形态即可，无需日历网格）。
- 全部视图目标：直接展示所有日程卡片（沿用首轮已有的筛选与排序能力）。

【卡片详情与删除】
- 现状：卡片只在列表里展示概要，没有查看详情或在界面上直接删除的入口（删除只能靠对话）。
- 目标：在任意视图点击一张日程卡片后，弹出/展开该卡片的详细信息，至少包含标题、时间性质（持续型的开始与结束时间，或截止型的 deadline）、重要程度、紧急程度、分类等基本属性；并在详情中提供「删除该日程」的操作。删除须遵守既有规则——删除前二次确认，确认后才真正删除，删除后各视图应及时反映变化。

【手动创建日程】
- 现状：新建日程只能通过 AI 聊天栏用自然语言完成。
- 目标：在顶部主导航栏新增一个「+」控件，所有者点击后可以不经过对话、直接以表单方式手动创建一张日程卡片，填写标题、时间性质及对应时间、重要度、紧急度、分类等属性；创建成功后立即在当前视图中可见。

约束与边界：本轮不改变「日程卡片」的数据结构与字段含义，不改变后端已有的增删改查与鉴权逻辑，仅在其之上增强前端浏览与手动操作体验。手动创建与手动删除必须复用已有的卡片创建/删除能力（含删除二次确认），保证与对话式操作行为一致。
```

> 预期它会就一两处歧义抛出 `[NEEDS CLARIFICATION]`，可能问：月视图格子里卡片过多时如何取舍（显示前 N 张 + “更多”）、拖拽宽度是否需要跨设备同步（建议只记本设备）、手动创建表单是否允许留空可选字段。按下面 clarify 一起答即可。

---

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

逐条回答它的提问。若它没覆盖，主动补充这几条倾向：
- 主题：原 system 值一律回退为 light；控件只在 light/dark 间切换。
- 月视图单日卡片过多：格子内最多显示前若干张，其余以「+N 更多」提示，点击可展开或跳到该日视图。
- 助手栏宽度：仅记在当前设备本地，**不**跨设备同步（区别于日程数据的跨设备一致）。
- 卡片详情删除：必须走既有二次确认流程，与对话删除同一套语义。

---

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上做前端交互增强，不引入新的后端能力，沿用首轮既定选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向：

- 主题：改造现有 useTheme —— 去掉 system 分支，主题类型收敛为 "light" | "dark"；读取偏好时把历史 "system" 归一化为 "light"；控件从三态循环改为二态切换。偏好仍通过既有 preferences 接口持久化。
- AI 助手栏折叠：把控制权从移动端专用按钮提升为顶部导航栏常驻控件（对标 Cursor 侧栏开关图标）；沿用 AppShell 现有的 chatOpen 状态，隐藏时主区域用 flex 占满。
- 助手栏拖拽调宽：在主区域与 aside 之间加一个可拖拽分隔条，用指针事件调整宽度，设定 min/max 像素范围；宽度存 localStorage（仅本设备），刷新后恢复。
- 视图重构：MonthView 改为按周分行的日期网格（用 date-fns 计算当月起止与补齐首尾周），每格渲染当日卡片，超出上限显示「+N」；WeekView 改为周一~周日七列布局；DayView 与 AllView 保持卡片列表（AllView 继续用首轮的筛选/排序）。三视图统一通过既有 GET /api/cards 的 view/date/filter/sort 参数取数。
- 卡片详情弹层：新增一个卡片详情组件（模态或抽屉），点击任意视图中的卡片打开，展示卡片全部基本属性；内含删除按钮，复用既有 DELETE /api/cards/:id，并在前端保持删除前二次确认；成功后用 TanStack Query invalidate 刷新相关视图查询。
- 手动创建：顶部导航栏加「+」控件，打开一个创建表单（模态/抽屉），字段对应卡片数据模型（标题、时间性质及时间、重要度、紧急度、分类，分类可从既有 categories 接口选取）；提交调用既有 POST /api/cards；成功后 invalidate 当前视图查询即时可见。

请产出实现方案，标注：日历网格的日期计算与时区处理（沿用 Asia/Shanghai 偏好）、拖拽宽度的事件与边界处理、详情/创建弹层的组件形态与可复用性、以及哪些改动是纯前端、哪些需要触及后端（预期后端零改动，如有例外请指出）。
```

---

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成 `tasks.md` 后**先人工审阅**：确认任务按「主题收敛 → 助手栏折叠/拖拽 → 三视图重构 → 卡片详情+删除 → 手动创建」分组，且每条带明确文件路径（预期集中在 `frontend/src/` 下的 hooks / layouts / components，后端预期不动）。

---

### ⑥ `/speckit-implement` — 分批实现

```
/speckit-implement
```

> 建议按用户故事分批跑、每批跑完实际启动前端验证（截图/交互）再进下一批，别一次性梭完 9 条。改完记得 `npm run build` 双端零错误、`npm run dev` 手动过一遍每条改动。

# Watson - 第三轮 Spec Kit 迭代
1. 新增功能
  1.主导航栏添加搜索栏，点击搜索栏后，主视图面黯淡。搜索栏只支持搜索日程卡片的名称。比如搜索“测试”，那么就在搜索栏下方出现“*测试*”"*测*试*"等符合匹配方法的日程名称（类似于google浏览器，点击搜索框后下方出现可能的搜索目标的美术设计），点击某个目标后就打开该目标的详情。
  2. 点击主导航栏的”+“号后创建日程时，只要标题不为空，按下enter键可以直接创建
  3. 创建日程时，只有标题是必须的。没有标明时间的只显示在全部视图
  4. 全部视图下应该有更加丰富的筛选和排序机制
  5. 在任何视图下点击卡片打开日程详情之后都可以进行编辑
  6. AI助手现在不需要和主要功能产生联动，暂时先把我已经放在.env中的deepseek api key对话接入，作为一个AI对话板块

2. 美术设计方面修改
  1. 日视图和全部视图模式下，日程卡片应该以圆角矩形呈现矩阵排列
  2. 深色主题下把蓝色替换为nvidia图表的绿色，浅色模式下还用蓝色
  3. 周视图下，应该为七个横向排列的竖向圆角矩形铺满画面
  4. 月视图下的日历应该铺满整个画面

3. 其他bug
  1. 日程卡片应当不能重名

---

## 第三轮 Spec Kit 指令（可直接粘进 Cursor Agent 面板）

> 定位：第三轮是**成体系的新一轮**（新功能 + 美术改造 + bug 修复），按 SDD「开新 feature」模型，直接跑 `/speckit-specify` 会自动扫描现有编号（当前最大 002）并新建 **003**，同时把 `.specify/feature.json` 激活指针切到 003。**开新轮 = 跑 specify；跑了 specify 就必然新建一轮。**
> 铁律不变：**specify 只谈「做什么/为什么」，技术栈（组件、DeepSeek 接入、唯一约束实现）留到 plan。**
> 本轮多为**改已有行为**（AI 助手从联动改为纯对话、卡片详情从只读改为可编辑、四视图美术重排），所以 specify 指令里显式带「现状 → 目标」。
>
> ⚠️ 一处事实校正（写进了下面 plan）：`.env` 里**没有**独立的 `DEEPSEEK_API_KEY`，第一轮就是可插拔设计，DeepSeek 走通用变量 `LLM_PROVIDER=deepseek` + `LLM_API_KEY` + `LLM_BASE_URL` + `LLM_MODEL`，config 的 provider 枚举已含 `deepseek`。别让它去找不存在的 `DEEPSEEK_API_KEY`。

---

### ② `/speckit-specify` — 写第三轮基线规格

```
/speckit-specify 这是 Watson 的第三轮迭代，命名为「日程搜索、编辑增强与视觉重排」。在已上线的对话式日程管理（001）与可视化/手动操作（002）之上，继续增强查找、创建、编辑体验，把 AI 助手降级为独立对话板块，并统一各视图的视觉呈现。以下按用户价值描述要做什么，不涉及技术实现：

【新增功能】
1. 全局搜索：在顶部主导航栏新增一个搜索栏。所有者点击搜索栏后，主视图区域变暗（弱化背景、聚焦搜索）。搜索仅针对「日程卡片标题」，支持模糊匹配——例如输入「测试」，下方即时浮出所有标题命中的候选（含「*测试*」「*测*试*」这类中间/分散匹配），呈现为一个类似浏览器地址栏的下拉候选列表（有清晰的视觉设计）。点击某个候选，直接打开该日程的详情。
2. 快捷创建：点击导航栏「+」创建日程时，只要标题不为空，按下 Enter 即可直接创建（无需点提交按钮）。
3. 最简必填：创建日程时，唯一必填项是标题。未填写任何时间信息的日程视为「无时间」，它不出现在日/周/月视图中，只在「全部」视图里展示。
4. 更强筛选排序：全部视图提供更丰富的筛选与排序机制（现状仅支持按重要度筛选、按时间/优先级排序），需扩展到覆盖分类、紧急度、时间性质、有无时间等更多维度的筛选，以及更多排序选项。
5. 卡片可编辑：现状——点击卡片打开的详情弹层只能查看和删除，不能改。目标——在任意视图点击卡片打开详情后，均可进入编辑并保存对该日程各属性的修改（复用既有创建/编辑表单能力，遵守与创建一致的校验，如标题必填、不可重名）。
6. AI 助手解耦：现状——侧边 AI 助手与日程数据联动（对话可增删改卡片、并刷新视图）。目标——本轮先取消这种联动，把它降级为一个独立的「AI 对话板块」：所有者可以和 AI 自由对话，但对话不再操作日程数据。对话接入所有者已在环境中配置好的 DeepSeek 对话能力即可。

【视觉设计】
7. 日视图与全部视图：日程卡片以「圆角矩形」样式、以矩阵（网格）方式排列展示。
8. 主题强调色：深色主题下把强调用的蓝色替换为 NVIDIA 品牌那种绿色；浅色主题保持原有蓝色不变。
9. 周视图：呈现为七个横向并排、各自纵向延伸的圆角矩形列，铺满整个主视图画面（周一至周日各一列）。
10. 月视图：当月日历铺满整个主视图画面（不再是局促的小网格）。

【Bug 修复】
11. 日程卡片标题不允许重名：创建或编辑日程时，若标题与已有日程重复，应被拒绝并给出明确提示。

约束与边界：不改变「日程卡片」的核心字段含义；沿用既有的鉴权、卡片增删改查与手动操作能力。AI 助手解耦是本轮明确的行为收敛（从"能操作日程"退回"纯聊天"），不是删除对话功能。"不可重名"需在创建与编辑两条路径上都生效，并与"仅标题必填""无时间只进全部视图"等规则彼此自洽。
```

> 预期它会就一两处歧义抛 `[NEEDS CLARIFICATION]`，可能问：重名判定是否忽略大小写/前后空格、是否区分已删除卡片；搜索候选是否有数量上限；"无时间"卡片在全部视图里如何排序。见下面 clarify 一并答。

---

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

倾向答案（它没问到就主动补）：
- 重名判定：忽略大小写与首尾空格（trim + 小写比较，与既有分类名的 name_lower 唯一策略一致）；仅对未删除的日程生效。
- 搜索候选：即时模糊匹配，最多显示前若干条（如 8 条），过多时提示继续输入收窄。
- 无时间卡片：在全部视图中可正常参与筛选/排序，排序时归入「无时间」分组或排在有时间项之后。
- AI 对话：本轮不做工具调用/日程联动，纯文本往返即可；会话历史是否持久化沿用第一轮 chat 存储或简化为临时会话，二者皆可，倾向复用既有存储但关闭工具编排。

---

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上迭代，沿用首轮选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向：

- 全局搜索：TopBar 新增搜索输入；聚焦时给主视图区加一个变暗遮罩层（overlay + 降低不透明度）。前端做标题模糊匹配下拉候选（子串/分散字符匹配），数据源用既有 GET /api/cards（或新增一个轻量 title 检索参数，优先复用现有接口在前端过滤，卡片量小无需后端全文检索）。点击候选复用既有 CardDetailModal 打开详情。
- 快捷创建：CreateCardModal / CardFormFields 里给标题输入绑定 Enter 提交（标题非空时触发既有 POST /api/cards），保留提交按钮作为备选。
- 最简必填 + 无时间归属：校验规则改为仅标题必填（timeNature/时间字段可空）；后端 schedule.service 与 GET /api/cards 的 view 过滤需支持「无时间」卡片——日/周/月视图排除无时间项，all 视图包含。检查 data-model 是否需要允许时间字段为空。
- 全部视图筛选排序增强：AllView 现仅有 importance 筛选 + time/priority 排序，扩展为 category / importance / urgency / timeNature / 有无时间 多维筛选与多种排序；对应扩展 GET /api/cards 的 query 参数（沿用第二轮已加的 view/date/filter/sort 参数体系）。
- 卡片编辑：现有 CardDetailModal 为只读，改造为「查看 ↔ 编辑」两态，编辑态复用 CardFormFields + validateCardForm，保存走既有 PATCH /api/cards/:id；保存后 invalidate 相关视图查询。
- AI 助手解耦：ChatPanel 现在监听 affectedCards 并 invalidate cards——移除这条联动；后端 chat.service 关闭 LLM 工具编排（不再暴露 create/query/update/delete 工具），退化为纯文本对话。DeepSeek 接入沿用既有可插拔 provider：通过 LLM_PROVIDER=deepseek + LLM_API_KEY + LLM_BASE_URL + LLM_MODEL（provider 枚举已含 deepseek，openai-compatible 适配器可直连 DeepSeek 的 OpenAI 兼容端点）。注意：.env 中并无独立 DEEPSEEK_API_KEY，不要新增该变量。
- 视觉：日/全部视图卡片改为圆角矩形 + CSS grid 矩阵排列；主题强调色用 CSS 变量分叉——深色 --accent 换成 NVIDIA 绿（约 #76b900），浅色维持原蓝，改造 index.css 的主题变量而非散落硬编码；WeekView 改七列 flex 铺满、每列纵向圆角矩形容器；MonthView 日历网格改为占满主区高度的自适应布局。
- 重名约束：后端在 schedule_cards 增加标题唯一性保障——data-model 加 title 的规范化唯一（参考既有 categories 的 name/name_lower 双列策略，加 title_lower 唯一索引），create 与 update 路径都校验，命中返回明确错误码/信息；前端表单捕获该错误并提示。需要一条 Drizzle migration。

请产出实现方案，标注：哪些是纯前端、哪些触及后端与数据库（预期：无时间归属、筛选参数、AI 解耦、重名唯一约束会动后端；搜索/变暗/编辑态/视觉主要在前端）；重名唯一约束的迁移与既有数据兼容性（若现有数据已有重名需先处理）；主题色改造的收敛点（集中到 CSS 变量）。
```

---

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成后**人工审阅**：确认任务按「搜索 → 快捷/最简创建 → 无时间归属 → 全部视图筛选排序 → 卡片编辑 → AI 解耦+DeepSeek → 视觉重排 → 重名约束（含 migration）」分组，每条带明确文件路径，并明确标出触及后端/DB 的少数几条。

---

### 【新增一步】`/speckit-analyze` — 动手前查一致性

```
/speckit-analyze
```

> 只读检查 spec/plan/tasks 三者是否自洽（尤其「仅标题必填」「无时间只进全部视图」「不可重名」这三条相互牵连的规则有没有矛盾）。有问题先回改规格，再进 implement。

---

### ⑥ `/speckit-implement` — 分批实现

```
/speckit-implement 只执行第 N 阶段的任务，完成后停下等我验证，不要继续后续阶段。
```

> 分批做法：把上面的 N 换成具体阶段号，一次一批，跑完 `npm run build` 双端零错误 + `npm run dev` 手动验证该批效果，再喊它「继续执行第 N+1 阶段」。它靠 tasks.md 的 `[X]` 标记断点续跑。**特别是 AI 解耦、重名 migration、主题色改造这几批容易连带影响既有功能，务必单独成批、逐批验证。**

---

### 全部做完后 `/speckit-converge` — 兜底查缺

```
/speckit-converge
```

> implement 全跑完后用它对照 spec/plan/tasks 扫码，把「规格要求但没做全」的缺口 append 成新任务，再 `/speckit-implement` 补齐。converged 就代表本轮收口。

---

# 宪法对齐（第四轮开工前先做）

> 背景：第三轮之后我手动改过代码，登录与 AI 两处的**实际实现已经稳定**，但 `constitution.md` 的文本没完全跟上——它俩现在是「宪法欠了两笔账」，不是代码错了。这一小节**只对齐宪法文本，不写任何新功能**，跑完再进第四轮需求。
>
> 现状确认（以代码为准）：
> - **登录**：唯一方式就是**四字符验证码**（默认 `ANNA`，可 `WATSON_ACCESS_CODE` 覆盖）+ HttpOnly/SameSite=Strict 会话。不引入密码、令牌、2FA、白名单等任何其他方式。
> - **AI 作用**：侧边 AI 是**纯文本对话板块**，**不与日程管理产生任何交互**（不增删改查日程、不触发数据刷新）。（标注为「暂时这样」——本轮先固化现状，日后若要恢复联动再走 MAJOR 修订。）
>
> 为什么要单独跑一次 `/speckit-constitution`：这两处属于**收紧/明确既有原则的表述**（登录方案去掉历史废弃项、AI 范围正式收敛），是 **MINOR** 级修订，版本应从 **1.1.0 → 1.2.0**。宪法是最高治理文档，先把它对齐，第四轮的 specify/plan 才有正确的宪法基线可依。

### `/speckit-constitution` — 对齐登录与 AI 范围（直接粘进 Cursor Agent 面板）

```
/speckit-constitution 请对 Watson 现行宪法做一次 MINOR 修订，版本从 1.1.0 升到 1.2.0，Last Amended 用今天。本次不新增原则，只把两处既有原则的表述与项目当前实现对齐、收紧，并清理历史残留描述。修订点如下：

【一、原则 I 单用户安全第一 —— 收口登录方案】
- 明确唯一登录方式为「四字符验证码」：所有者在登录页输入四字符验证码（默认 ANNA，可通过环境变量 WATSON_ACCESS_CODE 覆盖），校验通过后签发 HttpOnly + SameSite=Strict 会话 cookie；除 login / health 外所有 /api/* 必须经会话中间件保护；login 端点应限流防暴力尝试。
- 明确排除其它任何鉴权方式：不引入用户名密码、长随机访问令牌、Bearer Token、2FA、IP 白名单等，均视为超出单用户产品范围。
- 彻底移除历史废弃描述：删掉正文与治理说明里所有关于 watson:init 初始化流程、WATSON_ACCESS_TOKEN_HASH、长随机访问令牌方案的残留字样（这些方案已不再使用，不需要再以“已废弃/MUST NOT 重新引入”的形式保留，直接从宪法正文清理即可）。

【二、原则 III「AI 能力可插拔」与 Technology & Scope —— 正式收敛 AI 交互范围】
- 正式确立（不再是待办 TODO）：侧边 AI 是一个纯文本对话板块，只与所有者自由对话，MUST NOT 执行任何日程的增删改查，也不触发日程数据的刷新或联动；所有日程操作只经 UI 与 REST API 的结构化路径完成。
- 保留 provider 可插拔原则不变（OpenAI / Claude / DeepSeek 等 OpenAI-兼容接口，配置外置、可切换），仅收敛「AI 能做什么」的范围，不改「AI 用什么模型接入」的机制。
- 请把 Technology & Scope Constraints 中的「AI 交互范围」表述与此对齐，并把宪法顶部 Sync Impact Report 里那条“可选 MINOR 修正案同步 AI 交互范围（003 纯聊天）”的 Follow-up TODO 标记为已完成（本次修订即完成它）。
- 备注：此收敛标注为当前阶段的产品决策（“暂时如此”）；若未来要恢复 AI 直接操作日程，属于重新定义原则范围，届时走 MAJOR 修订，本次不预留该路径。

【三、原则 IV 数据以「日程卡片」为核心 —— 顺带校正一处措辞】
- 原文有「AI MUST NOT 绕过卡片结构直接篡改存储」，在 AI 已完全不操作日程数据的前提下，请将该句调整为与新范围一致的表述（例如：日程增删改查一律经 UI/API 结构化路径；AI 对话不写入日程存储），避免读者误以为 AI 仍有写数据的通路。

【四、版本与同步】
- 更新宪法顶部的 Sync Impact Report：记录 1.1.0 → 1.2.0、本次修改的原则（I 收口登录、III + Scope 收敛 AI、IV 措辞校正）、清理的废弃项，并标出需要同步的模板/文档（README.md、setup.md、.env.example 若涉及登录或 AI 描述需一并对齐）。
- 底部版本行更新为：Version 1.2.0 | Ratified 保持 2026-07-03 不变 | Last Amended 用今天日期。

请据此产出修订后的 constitution.md，并在 Sync Impact Report 里如实标注哪些下游文档还需人工同步（你只改宪法本身，下游文档我另行处理）。
```

> 跑完后自检：`.specify/memory/constitution.md` 顶部 Version 应为 **1.2.0**，正文里**再也搜不到** `watson:init` / `WATSON_ACCESS_TOKEN_HASH` / 长随机令牌 字样；AI 范围一句明确写「不执行日程 CRUD」；Sync Report 里原来那条 AI 的 Follow-up TODO 已勾掉。
> 宪法对齐后，再往下写第四轮的 specify/plan/tasks。

---

# Watson - 第四轮 Spec Kit 迭代

当前需要添加的新功能
- 所有日程的紧急程度和重要程度属性范围改为0～10。0和10都可选。用户体验：点击该属性后上下拉开一个数字序列（最下面是0,最上面是10），点击数字修改具体值。
- 添加垃圾箱标志在页面最右上角（因为“登出”要删除，所以就取代“登出”的位置即可），用来存储删除的或者已完成的卡片。点击标志打开垃圾箱，垃圾箱的视图与全部视图类似，但是排列顺序依靠的是卡片进入垃圾箱的时间，左上角是刚刚被放入的，右下角是之前的。被打了勾（完成的）日程就标记对勾，被删除的就用红色圆形标记。
- 在日视图和全部视图中，添加“坐标视图”按键，点击后页面中心打开二维视图，以重要程度5和紧急程度5为原点构建二维坐标系，把处于当前日视图或者全部视图的日程卡片按照横坐标为紧急程度，纵坐标为重要程度对应上去。注意，二维视图的视觉效果，就是一个十字坐标系，对应的坐标如果有任务，那么就显示一个圆，该点的任务越多，圆就越大，鼠标放在圆上时，该圆上出现一个信息框，显示那个圆中包含的任务的名称，右上角方向为更重要更紧急。
- 日程卡片添加快捷完成按键，就在打开日程详情后的右上角添加一个方框，点击后出现一个对勾，然后该日程（视为完成）进入垃圾箱。
- 在日视图和全部视图中的日程卡片的右边也添加一个方框，点击后出现对勾，然后该日程（视为完成）进入垃圾箱
- 日程应当有“创建时间”“最后一次修改时间”属性，这两个属性只在点击卡片查看详情时小字浅色显示（显示格式示例 创建时间：2026-0715-1639）
- 删除“登出”键，这个按键没有意义。

旧功能修改
- 在点击日程卡片阅读详情时，应该可以直接更改，把现在的“编辑“改为“恢复”键，点击后回复到本次打开时的原设置，把”关闭“改为”确认“。而不需要点击“编辑”键。用户体验”点击卡片打开详情后右下角只有“恢复”（默认无法点击），”确认“键，直接点击确认或者点击其他地方即退出，如果发生了修改就可以点击恢复回到这次打开时的情况，发生了修改后点击确认或者其他地方就直接确认修改。不需要二次弹窗确认。
- 启动网页后，默认AI助手处于隐藏状态。把“隐藏助手”按键字符改为“AI”不变，按键框在隐藏时为白色，显示时为蓝色即可（深色模式下改为对应颜色。）
- 修改切换主题的按键“主题：浅色”只保留”浅色“，点击后换成”深色“，再次点击为”浅色“即可
- 点击打开卡片详情之后的“删除该日程”改为“删除”即可，点击删除后出现的确认程序的框体再窄一点，现在横向太宽了。
- 取消时间性质属性，一个任务关于时间只有“已安排”“未安排”（目前称为“有无时间”）这两种情况，生成任务时默认起始时间就是创建任务的时间就行了，如果有截至日期，那么就是创建日期到截至日期。如果没有安排时间，就按照未安排属性来创建。

美术/视觉调整
- 日视图/全部视图下，日程应当遵循创建时间排列，第一排中左边为最新创建，最右边为第一排中最晚创建，第二排第一个比第一排最右边更晚，再往右边或者下边就更晚。以此类推。
- 日视图/全部视图下，当页面发生放缩时，日程卡片也会拉伸横向宽度，然后在大幅变化的情况下，才会更改数量。这一轮需要减小拉伸的阈值，也就是说每排的数量更容易发生变化而不是更容易横向拉伸。
- 周视图/月视图下，针对持续性的日程，月视图中的每个日期格子中如果出现了持续性日程，不需要跨格子连接起来，但是需要比其他的任务稍宽一些表示特殊，周视图则延续跨格子连接。

需要解决的bug
- 最开始点击 + 号创建卡片时，是有确认的，在上一轮修改中被移除了，添加回来。最终的用户体验应该是“点击 + 号创建卡片，不管是键盘按enter还是手动点击确认键都可以成功创建“。
- 无论什么视图，留出页边距，现在周视图和月视图最左边的那一个或者那一列已经有点超出页面了。
- 现在的周视图和月视图下任务的竖向排列有问题，导致了日程之间挤在一起。

---

## 第四轮决策定案（澄清后的权威口径，与上面原始需求对照阅读）

> 上面是我原始口述的需求；下面是逐条逻辑分析后拍板的定案。**遇到两者措辞冲突，以本表为准。** 下面的 speckit 指令都按本表写。

| # | 议题 | 定案 |
|---|---|---|
| 1 | 重要/紧急度 | 由 高/中/低 改为 **0~10 整数**（含 0 与 10）。交互：点击属性后竖向拉开数字序列（下 0 上 10），点数字选值。 |
| 2 | 时间模型（重构） | **取消「时间性质」字段**。卡片只分两态：**已安排**（用户主动填了起始时间；可再填截止时间→区间为起始→截止）与 **未安排**（用户没填任何时间字段，起始也**不**默认填）。**未安排卡片只进「全部」视图**，不进日/周/月视图。（推翻原口述「默认起始=创建时间」的说法。） |
| 3 | 垃圾箱 | 右上角新增垃圾箱图标（占用原登出位置）。**删除的卡片**与**完成的卡片**都进箱，且**退出所有常规视图**（日/周/月/全部/坐标都不再显示）。箱内视图仿全部视图，但按**进箱时间倒序**（左上=最新进箱，右下=较早）。完成的标**对勾**、删除的标**红色圆形**。箱内每张卡片可**恢复**回活跃态，也可**永久删除**。 |
| 4 | 完成态 | 卡片新增「完成」操作：①详情右上角方框，点出对勾→视为完成→进箱；②日/全部视图卡片右侧方框，点出对勾→完成→进箱。**完成操作不需要二次确认**（低风险）。 |
| 5 | 坐标视图 | 日视图/全部视图新增「坐标视图」按钮。点击后页面中心打开十字坐标系：**X=紧急度、Y=重要度，原点=(5,5)**，右上=更重要更紧急。取当前视图（日 or 全部）的活跃卡片（**已完成/已删除的不画**；全部视图下**含未安排卡片**，它们也有重/急度）按坐标定位；同一坐标点的多张卡片聚成一个**圆**，**圆越大=该点任务越多**（半径设上限防溢出）；鼠标悬停圆上弹信息框列出该点所有任务名（超过若干条显示「+N」）。 |
| 6 | 详情编辑（重构） | 打开详情即为**可编辑态**，无需先点「编辑」。右下角两键：**「恢复」**（默认禁用；发生修改后可点，作用=把字段重置回本次打开时的值，**不关闭**弹层）、**「确认」**（保存修改并关闭）。**点弹层外部=放弃本次修改并关闭（当取消）**；只有点「确认」才保存。**不要二次弹窗**。保存时若校验失败（标题为空 / 与他卡重名）→ 就地红字提示，不关闭、不保存。 |
| 7 | 创建确认（bug 回归） | 手动创建（+ 号）恢复确认按钮。最终体验：**键盘 Enter 或 手动点确认，二者都能成功创建**。仅标题必填。 |
| 8 | 删除确认 | 详情内「删除该日程」文案改为**「删除」**；点删除后的二次确认框**改窄**（当前横向过宽）。删除进箱这一步保留二次确认；箱内「永久删除」也需二次确认。 |
| 9 | 时间戳属性 | 卡片新增 **创建时间 / 最后修改时间**，仅在详情里以**小字浅色**显示，格式示例 `创建时间：2026-0715-1639`。 |
| 10 | 数据迁移 | 重/急度：**高→8、中→5、低→2**。时间性质：原**持续型**→起始+截止都保留（已安排）；原**截止型**→起始置空、只留截止（已安排，区间=创建→截止 由前端展示时兜底）。旧数据精度可接受损失。 |
| 11 | 排列（日/全部） | 卡片按**创建时间**排列：第一排左=最新，向右递减新度到该排最右；第二排首个比第一排末尾更早；继续向右/向下更早。（即 grid 里按 createdAt 倒序铺，行优先。） |
| 12 | 缩放阈值（日/全部） | 现状缩放时卡片先横向拉伸、大幅变化才改每排数量。**本轮调小拉伸阈值**：让「每排数量」更容易变化，而非优先横向拉伸。 |
| 13 | 持续型展示 | 「持续型」在新模型下重定义=**已安排且起始→截止跨了多天**的卡片。**月视图**：单格内的跨天卡片**不跨格连接**，仅比普通卡片**稍宽**以示区别；**周视图**：**保留跨列连接**（延续 002/003 行为）。 |
| 14 | AI 按钮 | 默认启动时 **AI 助手隐藏**。原「隐藏助手」按钮文字统一为 **「AI」**；隐藏态按钮框**白色**、显示态**蓝色**（深色主题用对应色）。 |
| 15 | 主题按钮 | 主题切换按钮**只显示当前态**：显「浅色」→点击切深色并显「深色」→再点回「浅色」。（去掉「主题：」前缀，二态切换。） |
| 16 | 页边距 bug | 所有视图留出页边距；修复周/月视图最左侧格/列**超出页面**的问题。 |
| 17 | 竖向排列 bug | 修复周/月视图下任务**竖向排列拥挤/重叠**的问题。 |
| 18 | 登出键 | **删除登出键**（位置让给垃圾箱图标）。 |

### 触及后端/DB 的改动（少数几条，plan 里重点标）
- **重/急度 0~10**：data-model 字段类型由枚举改整数 + migration（迁移旧枚举值）。
- **取消时间性质 + 未安排语义**：移除/废弃 `timeNature` 字段、调整时间字段可空规则、GET /api/cards 的 view 过滤逻辑（未安排只进 all）+ migration。
- **卡片状态机（活跃/已完成/已删除 + 进箱时间）**：新增状态字段与 `trashedAt` 时间戳、完成/删除/恢复/永久删除的接口语义（软删除模型）+ migration。
- **创建/修改时间戳**：`createdAt` 已有则复用；新增 `updatedAt`，更新时刷新。
- 其余（坐标视图、拉伸阈值、排列、持续型展示、AI/主题按钮、页边距、竖向拥挤、创建确认、详情编辑态）**预期纯前端**。

---

## 第四轮 Spec Kit 指令（宪法对齐跑完后，依次粘进 Cursor Agent 面板）

> 前提：先跑完上面「宪法对齐」小节的 `/speckit-constitution`（宪法到 1.2.0）。当前激活 feature 是 003，直接跑 `/speckit-specify` 会自动新建 **004** 并把 `.specify/feature.json` 指针切过去——**开新轮 = 跑 specify**。
> 铁律不变：**specify 只谈「做什么/为什么」，技术栈（字段类型、migration、组件、坐标绘制方案）留到 plan。**
> 本轮含**数据模型重构**（重/急度 0~10、取消时间性质、卡片状态机），是三轮里改动最深的一轮，specify 指令显式带「现状 → 目标」并点明 delta。

### ② `/speckit-specify` — 写第四轮基线规格

```
/speckit-specify 这是 Watson 的第四轮迭代，命名为「优先级量化、垃圾箱与坐标视图」。在已上线的 001/002/003 之上，重构优先级与时间模型、引入卡片生命周期（完成/删除/恢复的垃圾箱）、新增二维坐标视图，并做一批交互与视觉修正。以下按用户价值描述要做什么，不涉及技术实现：

【优先级量化】
1. 现状：日程的重要程度、紧急程度都是「高/中/低」三档。目标：改为 0~10 的整数刻度（0 和 10 都可选）。所有者点击该属性时，竖向拉开一列数字（最下 0、最上 10），点选具体数字即设定该值。旧有的高/中/低数据需被合理转换为对应的数值。

【时间模型重构】
2. 现状：卡片有「时间性质」（持续型=起止时间 / 截止型=单一 deadline），且第三轮规定选了时间性质就必须填齐时间。目标：取消「时间性质」这一属性。卡片关于时间只有两种情况——「已安排」：所有者主动填写了起始时间（并可选填截止时间，此时视为一个时间区间）；「未安排」：所有者未填写任何时间字段（起始时间也不代填）。未安排的卡片只出现在「全部」视图，不进日/周/月视图。已安排卡片按其时间正常进入相应视图。

【卡片生命周期与垃圾箱】
3. 现状：卡片只有活跃态，删除即消失且只能靠界面删除（带二次确认）。目标：卡片引入「完成」与「删除」两种归档去向，二者都进入一个「垃圾箱」，进箱后从所有常规视图（日/周/月/全部/坐标）消失。
   - 完成：所有者可将一张日程标记为「已完成」，标记后该卡片进入垃圾箱（完成操作无需二次确认）。
   - 删除：所有者删除一张日程时进入垃圾箱（删除保留二次确认）。
   - 垃圾箱入口：页面右上角一个垃圾箱图标（取代原「登出」按钮的位置）。点击打开垃圾箱。
   - 垃圾箱视图：呈现方式类似「全部」视图，但按「进入垃圾箱的时间」倒序排列——最近进箱的在左上，较早的在右下。已完成的卡片标一个对勾标记，已删除的卡片标一个红色圆形标记，以区分两种进箱原因。
   - 恢复与永久删除：垃圾箱内每张卡片都可以「恢复」回活跃状态（重新出现在常规视图），也可以「永久删除」（不可逆，需二次确认）。

【快捷完成入口】
4. 现状：无一键完成。目标：提供两个完成入口——① 打开卡片详情后，右上角有一个方框，点击后出现对勾，该日程即视为完成并进入垃圾箱；② 在日视图和全部视图中，每张卡片右侧也有一个方框，点击出对勾即完成并进箱。两个入口语义一致。

【二维坐标视图】
5. 现状：日/全部视图是卡片网格/列表。目标：在日视图与全部视图各加一个「坐标视图」按钮，点击后在页面中心打开一个二维坐标视图：以「重要程度 5、紧急程度 5」为原点建立十字坐标系，横轴为紧急程度、纵轴为重要程度，右上方向代表更重要更紧急。把当前所在视图（日 or 全部）中的活跃卡片按其（紧急度, 重要度）坐标落点；同一坐标点上的多张卡片聚合为一个圆，该点任务越多圆越大；鼠标悬停某个圆时弹出信息框，列出该圆所包含的所有任务名称。已完成/已删除的卡片不出现在坐标视图中。

【卡片详情：即时编辑】
6. 现状：点击卡片打开的详情需先点「编辑」才能改，右下角有「编辑」「关闭」，删除/编辑走二次确认。目标：详情打开即可直接编辑各属性，无需先点编辑。右下角改为两个键：「恢复」（默认不可点，一旦本次有修改即可点，作用是把所有字段重置回本次打开详情时的原值，但不关闭详情）与「确认」（保存修改并关闭）。点击详情面板以外的区域＝放弃本次修改并关闭（等同取消）；只有点「确认」才保存。整个过程不再弹二次确认窗。若保存时标题为空或与其他日程重名，则就地提示错误、不关闭也不保存。

【时间戳属性】
7. 目标：每张卡片记录「创建时间」与「最后修改时间」，仅在打开卡片详情时以小号浅色文字展示（示例格式 创建时间：2026-0715-1639），不在常规卡片面上显示。

【交互与文案修正】
8. 删除「登出」按钮（其位置让给垃圾箱图标）——单用户场景下登出无实际意义。
9. 默认启动时右侧 AI 助手为隐藏状态。切换 AI 显隐的按钮文字统一为「AI」，隐藏时按钮为白色、显示时为蓝色（深色主题使用对应配色）。
10. 主题切换按钮只显示当前主题名：显示「浅色」时点击切到深色并显示「深色」，再点回「浅色」，如此二态切换。
11. 详情内的「删除该日程」文案精简为「删除」；点删除后弹出的二次确认框宽度收窄（当前过宽）。
12. 手动创建（+）恢复确认按钮：最终体验是「点 + 号后，无论按 Enter 还是点确认按钮都能成功创建」，仍然只有标题必填。

【视觉与布局】
13. 日/全部视图的卡片按创建时间排列：第一排最左为最新创建、向右递减到该排最右，第二排首个比第一排末尾更早，依此向右向下越来越早。
14. 日/全部视图在页面缩放时，降低卡片「横向拉伸」的阈值，使「每排卡片数量」更容易随宽度变化，而不是优先把卡片拉宽。
15. 持续性日程（已安排且起止跨多天）的展示：月视图中单个日期格内的跨天卡片不做跨格连接，仅比普通卡片略宽以示区别；周视图保留跨列的连续条带表示。
16. 所有视图留出页边距，修复周/月视图最左侧格子/列超出页面边界的问题。
17. 修复周/月视图下日程竖向排列拥挤、相互挤压的问题。

约束与边界：本轮不改变单用户鉴权与「所有日程操作经 UI/API 结构化路径、AI 不操作日程」的宪法约束。完成/删除/恢复/永久删除必须复用统一的卡片生命周期语义。取消时间性质后，「仅标题必填」「未安排只进全部视图」「标题不可重名」等既有规则需与新时间模型彼此自洽。
```

> 预期它会抛几处 `[NEEDS CLARIFICATION]`，见下面 clarify 一并答。

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

倾向答案（它没问到就主动补，均已在上面定案表拍板）：
- **未安排判定**：用户没填任何时间字段即未安排，起始时间不代填；未安排卡片只进全部视图，可正常参与全部视图的筛选/排序。
- **旧数据迁移**：重/急度 高=8、中=5、低=2；原持续型保留起止、原截止型起始置空只留截止。
- **完成 vs 删除**：完成无需二次确认；删除保留二次确认；垃圾箱内永久删除需二次确认；两者都可从箱内恢复回活跃。
- **进箱后可见性**：完成/删除进箱后从所有常规视图与坐标视图消失，仅在垃圾箱可见。
- **坐标视图取值范围**：全部视图开坐标视图时也纳入未安排卡片（它们仍有重/急度）；已完成/删除的不纳入。恰好落在轴线（某维=5）的卡片：按「≥5 归右/上侧」处理。
- **坐标聚合圆**：圆半径设上限防溢出；悬停信息框任务名过多时显示前若干条 + 「+N」。
- **详情编辑点外部**：点外部＝放弃修改并关闭（取消语义），仅「确认」保存；校验失败就地红字提示不关闭。
- **时间戳格式**：创建/最后修改时间按 `YYYY-MMDD-HHmm` 小字浅色示例格式显示（沿用 Asia/Shanghai）。

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上迭代，沿用既定选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向，并明确标注纯前端 vs 触及后端/DB：

【触及后端 + DB（需 Drizzle migration，注意旧数据兼容）】
- 优先级量化：schedule_cards 的 importance / urgency 由枚举列改为 0~10 整数列；写一条 migration 把历史值映射为 高=8/中=5/低=2；create/update 校验值域 [0,10]。
- 时间模型重构：移除或废弃 timeNature 列（保留列但停用，或迁移删除，二选一并说明兼容策略）；起始/截止时间字段改为可空；「未安排」= 起止均空。GET /api/cards 的 view 过滤：未安排卡片仅在 view=all 返回，日/周/月按已安排时间过滤。migration 处理旧持续型（保留起止）与旧截止型（起始置空、仅留截止）。
- 卡片生命周期（软删除 + 完成）：新增状态字段（如 status: active | completed | deleted）与 trashedAt 时间戳；新增/调整接口——完成（PATCH 状态→completed 并置 trashedAt）、删除（软删→deleted 并置 trashedAt，保留既有二次确认）、恢复（→active、清 trashedAt）、永久删除（真正 DELETE，需二次确认）。所有常规查询默认只返回 active；垃圾箱查询返回 completed+deleted，按 trashedAt 倒序。坐标视图数据同样只取 active。
- 时间戳：复用既有 createdAt；新增 updatedAt，任何 update/编辑保存时刷新。详情返回这两个字段。
- 重名约束沿用第三轮的 title_lower 唯一策略，但需只对 active（或 active+completed，按定案）生效——明确「已删除卡片是否占用标题」：倾向仅 active 卡片参与重名判定，进箱后释放标题。请在 plan 里定死这一点。

【纯前端】
- 0~10 数字选择器：点击重/急度属性弹出竖向数字列（下 0 上 10）的选择控件，复用在创建表单与详情编辑态。
- 垃圾箱页面：新增 TrashView，仿 AllView 布局，按 trashedAt 倒序；completed 卡片渲染对勾角标、deleted 卡片渲染红色圆形角标；每卡提供「恢复」「永久删除」操作（永久删除前二次确认，复用 ConfirmDialog）。TopBar 右上角以垃圾箱图标替换登出按钮入口。
- 快捷完成：详情右上角 + 日/全部视图卡片右侧各加一个 checkbox 方框，点击→调用完成接口→invalidate 相关查询使其从当前视图消失。
- 坐标视图：日/全部视图加「坐标视图」切换；新增 QuadrantView 组件，用 SVG/canvas 画十字坐标系（原点=(5,5)，X=紧急 Y=重要，右上更重更急）；按 (urgency,importance) 分桶聚合，圆半径随该桶计数增长并设 max 上限；hover 弹 tooltip 列任务名（超量 +N）。数据来自当前视图的 active 卡片（全部视图含未安排卡片）。
- 详情即时编辑：改造 CardDetailModal 为始终可编辑；右下角「恢复」（重置为打开时快照，dirty 后才启用，不关闭）+「确认」（保存并关闭）；点遮罩外部＝丢弃并关闭；移除原编辑/删除二次弹窗流程中的编辑弹窗，保留删除的二次确认（框体收窄）；保存时前端 + 后端双重校验（标题必填、不可重名），失败就地红字。
- 创建确认回归：CreateCardModal 恢复确认按钮，同时保留 Enter 提交（标题非空即可），二者等价。
- 视觉：日/全部视图卡片 grid 按 createdAt 倒序行优先排布；调小 grid 的 min 列宽 / auto-fit 阈值，使列数更早随宽度变化而非拉伸单卡（用 CSS grid auto-fill/minmax 调参）；持续型（跨天已安排）卡片在月视图单格内加宽标识、不跨格，周视图保留 SpanBar 跨列；给各视图容器统一内边距修复越界；排查并修复周/月视图竖向排列的高度/间距导致的拥挤重叠。
- AI 按钮：默认 chatOpen=false（隐藏）；切换按钮文案固定「AI」，隐藏态白底、显示态蓝底（深色主题对应配色，走 CSS 变量）。
- 主题按钮：显示当前主题名（浅色/深色）的二态切换，去掉「主题：」前缀。
- 删除登出：移除 TopBar 登出按钮及其调用（会话仍可通过 cookie 过期自然失效，无 UI 登出入口）。

请产出实现方案，重点标注：三处 migration（优先级值域、时间模型、生命周期状态）的顺序与旧数据兼容处理；「未安排 vs 已安排」在后端查询与前端各视图的一致过滤口径；重名判定的作用范围（仅 active）；坐标视图的分桶/聚合与轴线归属规则；哪些改动纯前端、哪些触及后端与 DB（预期后端集中在优先级值域、时间模型、生命周期状态机、时间戳这几处）。
```

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成后**人工审阅**：确认任务按「DB migration（优先级值域 → 时间模型 → 生命周期状态）→ 后端接口（完成/删除/恢复/永久删除/未安排过滤/时间戳）→ 优先级 0~10 选择器 → 垃圾箱视图 → 快捷完成入口 → 坐标视图 → 详情即时编辑 → 创建确认回归 → 视觉与布局修正 → AI/主题按钮 + 删登出」分组，每条带明确文件路径，并把触及后端/DB 的少数几条清晰标出。

### 【动手前】`/speckit-analyze` — 查一致性

```
/speckit-analyze
```

> 只读检查 spec/plan/tasks 三者是否自洽，尤其盯这几组相互牵连的规则有没有矛盾：
> - 「取消时间性质」×「未安排只进全部视图」×「持续型（跨天）在月/周视图的展示」是否自洽；
> - 「完成/删除都进箱且退出所有视图」×「坐标视图只取 active」×「重名判定仅对 active」是否一致；
> - 「详情点外部=取消」×「恢复键=重置不关闭」×「确认=保存关闭」三态无歧义。
> 有问题先回改规格，再进 implement。

### ⑥ `/speckit-implement` — 分批实现（务必分批，本轮改动深）

```
/speckit-implement 只执行第 N 阶段的任务，完成后停下等我验证，不要继续后续阶段。
```

> 强烈建议按下面顺序**逐批跑、每批 `npm run build` 双端零错误 + `npm run dev` 手动验证**再进下一批。尤其前两批动了数据库和数据模型，最容易连带打挂既有功能：
> 1. **DB migration + 后端生命周期/时间模型/优先级值域**（最高风险，单独成批；migration 跑完先确认旧数据被正确迁移，再验证 GET /api/cards 各 view 过滤、完成/删除/恢复/永久删除接口）。
> 2. **优先级 0~10 选择器 + 时间戳显示**（前端消费新字段）。
> 3. **垃圾箱视图 + 快捷完成入口**（生命周期的前端闭环）。
> 4. **坐标视图**（相对独立，可单独验证聚合圆与 tooltip）。
> 5. **详情即时编辑 + 创建确认回归 + 删除确认收窄**（交互重构，注意别丢失校验）。
> 6. **视觉与布局修正 + AI/主题按钮 + 删登出**（纯样式收尾）。
> 每批完成用「继续执行第 N+1 阶段」推进；它靠 tasks.md 的 `[X]` 标记断点续跑。

### 全部做完后 `/speckit-converge` — 兜底查缺

```
/speckit-converge
```

> implement 全跑完后用它对照 spec/plan/tasks 扫缺口，append 成新任务再补齐。converged 即本轮收口。别忘了本轮改了数据模型，收口后建议再跑一次端到端：登录 → 建卡（已安排/未安排各一）→ 改优先级 0~10 → 完成/删除进箱 → 箱内恢复/永久删除 → 坐标视图 → 主题/AI 按钮，确认全链路无回归。

# Watson - 第五轮 Spec Kit 迭代
新增内容:
- 在登陆页面下方留出占位符用于放置备案要求的图标和信息
- 日视图和月视图应当和周视图同样具有“上一周”“本周”“下一周”类似的按键和功能
- 日视图的坐标视图按键删除，无需点击坐标视图按键，而是直接出现在日视图下的右侧（用一个圆角方形的凹陷效果背景和其他部分做区分，然后方形内部是二维图）
- 新增交互，任何视图下，用户可以拖拽卡片到垃圾箱。

美术风格调整:
- 周视图下的每个日的框体以及月视图下最下面那一排。下边沿需要向上稍微调整（月视图下是整体向上收缩一点），留出更多空间在页面底部。
- 周视图下的每个日的日程的长条框体需要对齐间隔，最高处稍微向下调整。
- 周视图的日程条应该是现在的2.5倍粗。

待修复的bug:
- 用户点击网页内上方导航栏的日历后，日历有一个删除选项，点击删除后，页面会直接清空（刷新后才可以恢复）。
- 用户导航栏上的日历中的删除键没有意义，应修改为”回到今天“
- 用户点击全部视图后，导航栏中的日历会消失，此设计没有意义并且影响观感。无论切换到什么视图，日历都不需要消失，并且可以继续调整，只不过在全部视图下不会引起主视图部分的改变而已。
- 创建日程时，当前必须先填写开始时间才能填写结束时间，正确的逻辑应该是：用户可以两个时间都不填，视为未安排的卡片；或者只填结束时间，此时默认开始时间就是创建卡片的时刻；但用户填了开始时间，就必须存在结束时间，结束时间默认是24h之后，也就是说用户修改完开始时间后，结束时间自动变为24h之后，然后用户再根据实际情况修改结束时间；

---

## 第五轮决策定案（澄清后的权威口径，与上面原始需求对照阅读）

> 上面是原始口述需求；下面是逐条逻辑分析后拍板的定案。**遇到两者措辞冲突，以本表为准。** 下面的 speckit 指令都按本表写。
> ⚠️ 本轮最重要的一点：**需求 694 主动改写了第四轮定的时间模型**，新规则彻底取代旧规则（见 #6），务必让 specify/plan/analyze 都盯住这处 delta。

| # | 议题 | 定案 |
|---|---|---|
| 1 | 备案占位（登录页） | 登录页下方留出一块**空占位区**（放备案图标/信息的位置），本轮只留位、**暂不填真实备案号**（尚未备案）。占位需在明暗主题下都不破坏登录页现有视觉。 |
| 2 | 三视图前后导航 | 日/周/月三个视图都要有与周视图**对等**的前后翻页 + 回到当前按钮，**各按自身时间粒度**：日视图=前一天 / 今天 / 后一天；周视图=上一周 / 本周 / 下一周（维持现状）；月视图=上一月 / 本月 / 下一月。（原口述「日/月也用上一周本周下一周」是话术照抄，实际按各自粒度。） |
| 3 | 日视图内嵌坐标视图 | **仅日视图**：删除「坐标视图」切换按钮，坐标二维图**常驻显示在日视图右侧**，用一个圆角矩形、凹陷效果背景与主区域区分，方框内部即二维坐标图。**全部视图的坐标视图保持第四轮的「按钮点击弹出」方式不变。** |
| 4 | 拖拽卡片到垃圾箱 | 新增交互：**任何视图**下用户可把日程卡片**拖拽到右上角垃圾箱图标**，松手即视为**删除**（进箱后标红色圆形，与「完成」的对勾区分）。**拖拽删除不弹二次确认**（拖拽动作本身即确认）。注意：这与「点详情内删除按钮仍弹二次确认」并存——两条删除路径，按钮删除留确认、拖拽删除不留。 |
| 5 | 全部视图恢复日历 | 现状：切到全部视图时导航栏日期控件被隐藏（代码 `view !== "all"` 条件）。目标：**全部视图也显示日历/日期控件且可正常调整**，只是全部视图下调整它**不影响主视图内容**（全部视图不按日期过滤）。**垃圾箱视图仍不显示**日期控件（对按进箱时间排序的箱无意义）。 |
| 6 | 时间字段填写逻辑（**取代第四轮时间模型**） | 现状：结束时间输入框在没填开始时间时被禁用（`disabled={!values.startAt}`），且校验要求「填结束必先有开始」。新规则三种情况：**A 两个都空 = 未安排**（只进全部视图）；**B 只填结束 = 已安排**，开始时间默认为创建卡片的时刻（区间=创建时刻→结束）；**C 填了开始 = 已安排且必须有结束**，用户改完开始时间后结束时间**自动置为「开始+24h」**，用户可再手动改，但**不允许最终没有结束时间**。→ 即：已安排卡片在存储层**一定同时有开始与结束**；「只有开始、无结束」这种第四轮曾允许的状态**本轮起不再存在**。 |
| 7 | 底部留白（周/月） | 周视图每个「日」框体、月视图最下面一排的**下边沿向上收**（月视图整体向上收缩一点），在页面底部留出更多空间，避免贴边。 |
| 8 | 周视图日程条对齐 | 周视图每个日内的日程长条框体需**对齐间隔**，最高处（顶端起始位置）**稍微向下调整**，不要顶到格子最上沿。 |
| 9 | 周视图日程条加粗 | 周视图的日程条粗细改为**当前的 2.5 倍**。 |
| 10 | bug：日历清除导致清空 | 现状：导航栏日期控件是原生 `<input type="date">`，其自带的「清除（✕）」会把 anchorDate 置空，导致主视图算不出锚点日期而清空，刷新后重新初始化才恢复。修复：**移除/禁用该清除入口**，日期控件不允许被清空为无效值；锚点日期始终有效。（这就是原口述说的「日历的删除选项」。） |
| 11 | bug：日历「删除/清除」改为「回到今天」 | 与 #10 同源：把那个无意义的清除入口**替换为「回到今天」功能**——一个明确的按钮/操作，点击后把锚点日期重置为今天（配合 #2 的「今天/本周/本月」语义统一）。 |

### 触及后端/DB 的改动（本轮预期极少）
- **仅 #6 时间模型规则收紧可能触及后端校验**：create/update 的校验逻辑需改为——已安排卡片必须同时有开始与结束（只填结束时后端补开始=创建时刻，或由前端提交时补齐后端只做一致性校验，plan 里定死在哪层补）。data-model 字段本身第四轮已是可空的开始/结束，**预期无需新 migration**，只调校验与默认值逻辑。若第四轮已存在「只有开始无结束」的历史卡片，需一条兜底把它们的结束补为「开始+24h」——plan 里评估是否需要。
- 其余（备案占位、三视图导航、日视图内嵌坐标、拖拽删除、全部视图显示日历、周/月留白与日程条样式、日历清除改回到今天）**预期纯前端**。

---

## 第五轮 Spec Kit 指令（第四轮收口后，依次粘进 Cursor Agent 面板）

> 前提：第四轮（004）已 implement + converge 收口。当前激活 feature 是 004，直接跑 `/speckit-specify` 会自动新建 **005** 并把 `.specify/feature.json` 指针切过去。
> 铁律不变：**specify 只谈「做什么/为什么」，技术栈（组件、拖拽实现、日期控件替换方案、校验落点）留到 plan。**
> 本轮多为**改已有行为 + bug 修复**，且 #6 会**推翻第四轮的时间模型**，specify 指令显式带「现状 → 目标」并点明它取代第四轮的哪条规则。

### ② `/speckit-specify` — 写第五轮基线规格

```
/speckit-specify 这是 Watson 的第五轮迭代，命名为「视图导航、拖拽归档与时间模型收紧」。在已上线的 001~004 之上，补齐各视图的时间导航、把日视图的优先级坐标图常驻化、新增拖拽删除、修复导航栏日历相关缺陷，并收紧日程的时间填写规则。以下按用户价值描述要做什么，不涉及技术实现：

【新增能力】
1. 登录页备案占位：在登录页下方留出一块占位区域，用于日后放置网站备案（ICP 等）要求的图标与信息文字。本轮仅预留该位置（暂不填入真实备案号），占位在明暗两种主题下都不应破坏登录页现有的视觉风格。
2. 各视图时间导航：日视图、月视图应当像周视图一样，具备"前一 / 回到当前 / 后一"的时间导航控件，按各自的时间粒度——日视图在相邻日之间前后切换并可一键回到今天；月视图在相邻月之间前后切换并可一键回到本月；周视图维持现有的上一周 / 本周 / 下一周。
3. 日视图内嵌优先级坐标图：现状——日视图里有一个"坐标视图"按钮，点击才打开二维优先级图。目标——日视图取消该按钮，把二维坐标图常驻显示在日视图的右侧区域，用一个带圆角、凹陷质感背景的方形框与主日程区域作视觉区分，方框内部即该二维优先级图（横轴紧急、纵轴重要，原点为 5、5，右上更重更急，沿用第四轮坐标图的其余行为）。注意：全部视图里的坐标视图维持第四轮"按钮点击打开"的方式不变，本条只改日视图。
4. 拖拽归档到垃圾箱：新增一种交互——在任何视图下，用户都可以把一张日程卡片拖拽到右上角的垃圾箱图标上，松手即把该卡片删除并送入垃圾箱（在箱内以"已删除"的红色圆形标记呈现，与"已完成"的对勾区分）。拖拽删除这一动作本身即视为确认，无需再弹二次确认框。（保留另一条路径：从卡片详情里点"删除"按钮删除时仍走既有的二次确认。）

【视觉调整】
5. 周视图每个"日"的框体、以及月视图最下面一排的下边沿，需要整体向上收一些（月视图整体向上收缩），在页面底部留出更多空白，避免内容贴住页面底边。
6. 周视图中每个日内的日程长条，需要对齐彼此的间隔，且其顶端起始位置稍微向下移一点，不要顶到格子最上沿。
7. 周视图的日程长条粗细增加到当前的约 2.5 倍。

【缺陷修复】
8. 导航栏日历清空缺陷：现状——导航栏的日期选择器带有一个"清除"入口，用户触发后当前锚定日期被清空，导致主视图内容直接清空，只有刷新页面才能恢复。目标——锚定日期必须始终是一个有效日期，不允许被清空成无效状态；移除这个会导致清空的"清除/删除"入口。
9. 把上述无意义的"清除/删除"入口替换为"回到今天"：提供一个明确的操作，点击后把锚定日期重置为今天（与第 2 条日/周/月各视图的"回到当前"语义一致）。
10. 全部视图不再隐藏日历：现状——切换到全部视图时，导航栏的日期控件会消失。目标——无论切到哪个常规视图，日期控件都保持显示且可调整；只是在全部视图下调整它不会改变主视图内容（全部视图本就不按单一日期过滤）。（垃圾箱视图仍无需显示日期控件，因为它按进箱时间排序。）
11. 时间填写规则收紧（本条取代第四轮的时间模型）：现状——创建日程时必须先填开始时间才能填结束时间；第四轮曾允许"只有开始时间、没有结束时间"的已安排卡片。目标——重新定义为三种情况且互斥：① 开始与结束都不填 = 未安排卡片（只出现在全部视图）；② 只填结束时间 = 已安排卡片，其开始时间默认为创建该卡片的时刻（形成"创建时刻→结束"的区间）；③ 填了开始时间 = 已安排卡片且必须有结束时间——用户设定/修改开始时间后，结束时间自动置为"开始时间 + 24 小时"，用户可再据实调整，但不允许最终把结束时间留空。也就是说，从本轮起，凡"已安排"卡片都同时具备开始与结束时间，不再存在"只有开始、没有结束"的卡片。

约束与边界：本轮不改变单用户鉴权、卡片核心字段含义、"AI 不操作日程"等宪法约束，也不改变第四轮已确立的垃圾箱生命周期（完成/删除/恢复/永久删除）语义——拖拽只是新增一条触发"删除"的入口。第 11 条时间规则收紧后，"未安排只进全部视图""标题不可重名""已安排卡片一定有起止"等规则需彼此自洽，并与第四轮的坐标视图、各日历视图的取数逻辑保持一致。
```

> 预期它会就一两处抛 `[NEEDS CLARIFICATION]`，见下面 clarify 一并答。

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

倾向答案（它没问到就主动补，均已在上面定案表拍板）：
- **时间规则（#6/11）**：已安排卡片一定同时有开始+结束；只填结束→开始补为创建时刻；填开始→结束自动补开始+24h 且可改但不能清空；两者皆空=未安排。若库里有第四轮遗留的"只有开始无结束"卡片，兜底把结束补为开始+24h。
- **拖拽删除**：等于"删除"（红色圆标记），不弹二次确认；详情内删除按钮仍保留二次确认。
- **日视图坐标图常驻**：只改日视图（右侧凹陷方框常驻）；全部视图保持按钮弹出式。
- **全部视图日历**：全部视图恢复显示且可调，但不影响主视图；垃圾箱视图不显示日期控件。
- **日历"清除"入口**：移除并替换为"回到今天"；锚定日期始终有效、不可为空。
- **备案占位**：只留空位，不填真实备案号；明暗主题下均不破坏登录页视觉。
- **三视图导航粒度**：日=±1 天+今天，月=±1 月+本月，周维持。

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上做以前端为主的迭代，沿用既定选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向，并明确标注纯前端 vs 触及后端：

【可能触及后端（预期无新 migration，只改校验/默认值）】
- 时间规则收紧：把"已安排卡片必须同时有开始与结束"落实为校验规则。明确在哪层补齐：前端 CardFormFields/validateCardForm 负责——只填结束时提交前把开始补为创建时刻（或提交 null 开始、由后端补为 now），填开始时自动把结束设为开始+24h；后端 create/update 校验做一致性兜底（有开始必有结束、结束≥开始）。现有 CardFormFields 里 `disabled={!values.startAt}` 与 validate 中「填结束需先有开始」的旧限制要一并改掉/反向。评估是否需要一次性数据兜底：把库中"有开始无结束"的历史卡片结束补为开始+24h（若存在）。

【纯前端】
- 登录页备案占位：在 LoginPage 底部加一块预留占位容器（如固定高度的 footer 区，含占位文字/图标位），响应式且明暗主题适配，暂不填真实备案信息。
- 各视图时间导航：把周视图已有的前后翻页控件抽象/复用，给日视图（±1 天 + 今天）、月视图（±1 月 + 本月）加对等控件；统一操作 anchorDate（沿用 TopBar 的 anchorDate/onDateChange 与 date-fns 计算）。
- 日视图内嵌坐标图：DayView 布局改为左侧日程 + 右侧常驻坐标图（圆角、inset/凹陷背景的方形容器），复用第四轮 QuadrantView 组件（原点 5,5 / X 紧急 Y 重要 / 右上更重急）；移除日视图的"坐标视图"切换按钮。AllView 保持按钮弹出式不动。
- 拖拽删除：给各视图的卡片加 draggable，垃圾箱图标作为 drop target；drop 时调用既有"删除→进箱"接口（deleted 状态 + trashedAt），无二次确认；用 HTML5 DnD 或指针事件实现，注意移动端触摸兜底（若不做触摸拖拽，需在 spec 注明降级）。详情内删除按钮的二次确认保持不变。
- 全部视图日历：去掉 TopBar 中日期控件对 `view === "all"` 的隐藏（保留对 trash 的隐藏）；全部视图下 anchorDate 变化不触发主视图重查（AllView 不消费 anchorDate 过滤）。
- 日历清除→回到今天：把 TopBar 的原生 `<input type="date">` 换成不可清空的日期控件（自定义封装或屏蔽其 clear），并新增"回到今天"按钮把 anchorDate 设为今天；保证 anchorDate 永不为空。
- 周/月视觉：周视图日框体与月视图末行下边沿上收留白（调容器 padding/height 计算）；周视图日程条对齐间隔、顶端下移、粗细×2.5（改 SpanBar / weekGrid 的尺寸与定位常量）；排查这些改动是否与第四轮"竖向拥挤"修复冲突。

请产出实现方案，重点标注：时间规则收紧的校验落点（前端补齐 vs 后端兜底）与历史数据兼容；拖拽删除的实现方式与移动端降级；日期控件替换为不可清空 + 回到今天的方案；日视图左右布局对既有 DayView 的改动面；哪些纯前端、哪些触及后端（预期仅时间校验一处）。
```

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成后**人工审阅**：确认任务按「时间规则收紧（校验+可能的数据兜底）→ 各视图时间导航 → 日视图内嵌坐标图 → 拖拽删除 → 全部视图恢复日历 → 日历清除改回到今天 → 登录页备案占位 → 周/月视觉调整」分组，每条带明确文件路径，并把可能触及后端校验的那一条单独标出。

### 【动手前】`/speckit-analyze` — 查一致性

```
/speckit-analyze
```

> 只读检查 spec/plan/tasks 三者是否自洽，尤其盯这几组相互牵连处：
> - **#6 时间规则 vs 第四轮**：新规「已安排必有起止」与第四轮 spec/data-model 里「可只有开始」的旧表述是否有残留矛盾（若第四轮 spec 仍写着旧规则，属预期的跨轮 delta，确认 005 明确声明取代，不要求回改 004 历史规格）。
> - **拖拽删除 vs 详情删除**：一个不确认、一个确认，两条路径是否都指向同一"删除→进箱"语义。
> - **全部视图显示日历 vs 全部视图不按日期过滤**：显示但不影响主视图，逻辑是否讲清。
> 有问题先回改规格再 implement。

### ⑥ `/speckit-implement` — 分批实现

```
/speckit-implement 只执行第 N 阶段的任务，完成后停下等我验证，不要继续后续阶段。
```

> 建议分批顺序（每批 `npm run build` 双端零错误 + `npm run dev` 手动验证再进下一批）：
> 1. **时间规则收紧**（唯一可能触后端的一批，先做并验证：未安排/只填结束/填开始自动补24h 三种情况，以及历史数据兜底）。
> 2. **各视图时间导航 + 全部视图恢复日历 + 日历清除改回到今天**（都围绕 TopBar 与 anchorDate，一起做减少反复）。
> 3. **日视图内嵌坐标图**（DayView 布局重构，单独验证左右布局与坐标图）。
> 4. **拖拽删除**（新交互，单独验证桌面拖拽 + 移动端降级）。
> 5. **登录页备案占位 + 周/月视觉调整（留白、日程条对齐/加粗）**（纯样式收尾）。
> 每批完成用「继续执行第 N+1 阶段」推进；靠 tasks.md 的 `[X]` 断点续跑。

### 全部做完后 `/speckit-converge` — 兜底查缺

```
/speckit-converge
```

> implement 全跑完后用它对照 spec/plan/tasks 扫缺口，append 成新任务再补齐。converged 即本轮收口。收口后建议端到端过一遍：登录（看底部占位）→ 日/周/月视图各翻页+回到当前 → 日视图右侧坐标图常驻 → 建卡验证三种时间情况（未安排/只填结束/填开始自动补24h）→ 从各视图拖卡片进垃圾箱 → 全部视图下日历仍在且可调不影响主视图，确认无回归。

---

# Watson - 第六轮 Spec Kit 迭代（体验与美术升级）

> 前五轮把功能做齐了，但一路在赶功能、几乎没打磨交互与视觉。本轮是一次**纯体验/美术升级轮**——不加新业务能力，只提升「好用」和「好看」。
> 下面的建议**不是拍脑袋**：我在第五轮实测后，实际启动了服务（headless Chrome 截图）逐屏看过 **登录页、日视图（浅色）、日视图（深色）** 的真实界面，每条建议都对应看到的具体短板，标了 [证据] 的即为截图实证。

## 一、实测发现的现状短板（建议的依据）

- **设计 token 极简**：`index.css` 只有 6 个颜色变量（bg/fg/panel/border/accent/muted），**没有阴影、圆角、间距、字号、动效的统一 token**——这是「缺设计感」的技术根因，各组件各写各的 `rounded/border`，不成体系。
- **全局零过渡动画**：主题切换、面板开合、hover 全是硬切，是「廉价感」的主要来源。
- **强调色没有状态层次**：只有一个 `--accent`，没有 hover/active/focus 变体，交互反馈单薄。[证据：深色下绿色只孤立出现在选中态和坐标点两处]
- **[证据] 顶栏功能层级混乱**：搜索框过宽（占大半栏），视图切换按钮无分组容器、选中/未选态权重悬殊；垃圾箱（危险操作）与主题、AI 按钮同级并列。
- **[证据] 优先级用裸数字**「重要5 紧急5」，无任何可视化，晦涩难读。
- **[证据] 完成 checkbox 是个空灰方块**，像未加载的占位符，很廉价。
- **[证据] 坐标图四象限无分区背景/标签**，轴线太暗（尤其深色下几乎不可见），单点数据时空间浪费严重且无降级。
- **[证据] 无空状态设计**：少数据时大片留白，没有插画/引导文案，页面像「没做完」。
- **[证据] 卡片在深色下对比度低**，仅靠细边框和背景区分，存在感弱。

---

## 二、10+ 条体验与美术升级建议（本轮 specify 就按这些写）

> 每条都标注了 **价值**、**落点**、**是否触后端**（本轮**几乎全是纯前端**）。按「先立地基（设计系统）→ 再改具体界面」排序。

### A. 设计系统地基（做完这条，后面所有视觉改动才成体系）

**1. 建立统一设计 token（最高优先级，地基）**
在 `index.css` 扩充设计变量：一套**间距刻度**（4/8/12/16/24/32）、**圆角刻度**（sm/md/lg）、**阴影层级**（sm/md/lg，深浅主题各一套）、**字号与字重层级**（标题/正文/辅助/微标）、**强调色的状态变体**（accent / accent-hover / accent-active / accent-subtle 半透明底）。之后所有组件改为消费这些 token，不再散落硬编码。
- 价值：一次立规，全局统一，是「设计感」的根。｜落点：`index.css` + 各组件替换硬编码｜纯前端

**2. 全局引入过渡动效**
给主题切换、面板开合（AI 栏折叠/拖拽）、hover、模态出现/消失加统一的 `transition`（150–250ms、ease-out）；模态/抽屉加淡入+轻微位移，主题切换加颜色渐变过渡。尊重 `prefers-reduced-motion`。
- 价值：消除硬切的廉价感，最高性价比的「质感」提升｜落点：全局 CSS + Modal/Drawer/ChatPanel｜纯前端

### B. 顶部导航栏重构（实测最乱的区域）

**3. 顶栏分组与功能分区**
把顶栏重组为清晰的三区：左（品牌+搜索）、中（视图切换+日期导航）、右（操作区）。视图切换按钮包进一个**带背景的分段控件（segmented control）**容器，选中/未选态用同一视觉语言（不再是「蓝底 vs 裸文字」的突兀对比）。搜索框**收窄**为合理宽度（如 240px，聚焦时再展开）。
- 价值：解决「两头堆中间空」的失衡与层级混乱｜落点：`TopBar.tsx`｜纯前端

**4. 危险操作与普通操作分离**
垃圾箱是通往「删除区」的入口，不应与主题、AI 切换视觉同级。给操作区加分隔（细竖线或间距分组），垃圾箱用独立的、略带警示语义的样式；主题/AI 归为一组「视图偏好」。
- 价值：符合「危险操作要显眼且独立」的通则｜落点：`TopBar.tsx`｜纯前端

### C. 卡片与信息可视化（实测最晦涩的部分）

**5. 优先级可视化，替代裸数字**
「重要5 紧急5」改为**可视化编码**：用小色块/圆点强度条/迷你条形表达 0~10，配合颜色梯度（低=冷/灰，高=暖/强调色）。数字可保留但作为辅助，不再是唯一表达。
- 价值：一眼可读优先级，这是本产品「重要×紧急」核心卖点的视觉兑现｜落点：卡片组件 + `PriorityPicker` 展示态｜纯前端

**6. 卡片左侧分类色条 + 信息层级**
每张卡片左缘加一条**分类色带**（不同分类不同色），标题/时间/标签/优先级拉开字号与颜色层级；深色模式下给卡片**略微抬高的背景色 + 阴影**，不再仅靠细边框区分（解决实测的「深色下卡片存在感弱」）。
- 价值：卡片一眼分得清、有质感｜落点：`CardGrid`/`CardList`/卡片组件 + token｜纯前端

**7. 完成 checkbox 重设计**
把空灰方块换成有明确边框、hover 有反馈、勾选有过渡动画的自定义 checkbox（勾选时用强调色 + 对勾缩放动效）。
- 价值：消除「像未加载占位符」的廉价感｜落点：`CompleteCheckbox.tsx`｜纯前端

### D. 坐标图升级（实测空间浪费+不可读）

**8. 四象限分区背景 + 象限标签**
给坐标图四个象限加**淡背景色分区**（右上「重要且紧急/立即做」暖色调、左上「重要不紧急/计划做」、右下「紧急不重要/授权做」、左下「不重要不紧急/减少做」），四角加象限名称标签；加粗坐标轴、标注原点（解决实测的「轴线太暗、象限无含义」）。
- 价值：把光秃秃的十字变成可理解的艾森豪威尔矩阵，产品理念的点睛之笔｜落点：`QuadrantView.tsx`｜纯前端

**9. 坐标点信息增强**
点旁显示任务名（或首字），hover tooltip 更精致（卡片式、带阴影）；单点/少数据时坐标图给轻量说明而非大片空白。
- 价值：低数据密度时不再像 bug｜落点：`QuadrantView.tsx`｜纯前端

### E. 空状态与反馈（实测完全缺失）

**10. 全套空状态设计**
各视图无数据时给**空状态**：简洁插画/图标 + 一句引导文案 + 主操作按钮（如「还没有日程，点 + 创建第一条」）。垃圾箱空时、搜索无结果时同理。
- 价值：解决实测最大问题「页面像没做完」，也降低新手上手门槛｜落点：各 View + 新增 `EmptyState` 组件｜纯前端

**11. 加载态与操作反馈**
数据加载用**骨架屏（skeleton）**替代空白/闪烁；创建/完成/删除/恢复等操作成功给轻量 **toast 提示**；拖拽到垃圾箱时垃圾箱图标高亮成为放置目标（drop 反馈）。
- 价值：让每个操作「有回应」，专业感的关键｜落点：新增 Toast + Skeleton，接入 TanStack Query 的 loading 态｜纯前端

### F. 排版与细节（锦上添花）

**12. 排版层级与登录页打磨**
正文/标题统一字号字重节奏；登录页——收拢中下部大片空白、优化「WATSON」发光描边的配色精致度（当前绿+紫描边偏廉价）、给验证码框更强的对比度和「请输入验证码」引导文字（实测输入框辨识度不足）。
- 价值：第一屏印象 + 全局排版气质｜落点：`index.css` + `LoginPage.tsx` + `PinCodeInput.tsx`｜纯前端

**13.（可选）强调色系统化点缀**
让 NVIDIA 绿/蓝在深色下有节奏地出现（当前只孤立在两处）：链接、聚焦环、次要强调、图表元素统一走强调色梯度，形成视觉韵律。
- 价值：品牌色成体系而非孤点｜落点：token + 各组件｜纯前端

---

## 三、第六轮 Spec Kit 指令（第五轮收口后，依次粘进 Cursor Agent 面板）

> 前提：005 已收口。跑 `/speckit-specify` 会自动新建 **006**。
> 铁律不变：**specify 只谈「做什么/为什么」，技术实现（token 命名、动效时长、组件方案）留到 plan。**
> 本轮特殊性：几乎**纯前端视觉/交互**，**不动数据模型、不动后端、不动业务逻辑**——这是硬边界，务必在 specify 和 plan 里都写死，避免它顺手改功能。

### ② `/speckit-specify` — 写第六轮基线规格

```
/speckit-specify 这是 Watson 的第六轮迭代，命名为「体验与视觉体系升级」。前五轮功能已齐备，本轮不新增任何业务能力，只系统性提升交互体验与视觉设计质量。以下按用户价值描述要做什么，不涉及技术实现。硬性边界：不改变任何数据模型、后端接口、业务规则与既有功能行为，仅改进前端的呈现、反馈与美观度；任何改动都不得回归破坏 001~005 已验证的功能。

【设计一致性】
1. 现状：界面各处的颜色、圆角、间距、阴影、字号缺乏统一规范，观感零散、不成体系。目标：建立一套统一的视觉规范（间距、圆角、阴影、字号字重层级、强调色的常态/悬停/按下/浅色底等状态），全站一致地应用，使界面呈现协调、有设计感的整体气质。
2. 现状：主题切换、面板开合、悬停、弹层出现都是瞬间硬切，显得生硬廉价。目标：为这些交互加入平顺的过渡动效，弹层有柔和的出现/消失，主题切换有颜色过渡；尊重系统「减少动态效果」的无障碍偏好。

【导航栏体验】
3. 现状：顶部导航栏布局失衡——搜索框过宽、视图切换按钮分组不清且选中态与未选态视觉落差突兀、右侧按钮孤立漂浮。目标：把顶栏重组为「品牌+搜索 / 视图切换+日期导航 / 操作区」清晰三区；视图切换做成有容器的分段式控件，选中与未选用统一视觉语言；搜索框收窄到合理宽度（可在聚焦时展开）。
4. 现状：垃圾箱（通往删除区的入口）与主题、AI 切换按钮同级并排，危险操作与偏好设置混在一起。目标：把操作区按语义分组、加视觉分隔，让「进入垃圾箱」与「切换主题/AI」在视觉层级上区分开。

【信息可视化】
5. 现状：日程卡片上的重要度、紧急度以「重要5 紧急5」这样的裸数字呈现，晦涩难懂。目标：用直观的可视化方式表达 0~10 的重要度与紧急度（如强度色块/圆点条/颜色梯度，低值冷淡、高值醒目），数字可作辅助，让用户一眼看懂优先级。
6. 现状：卡片信息层级偏平，分类不易区分，深色模式下卡片与背景对比弱、存在感低。目标：给卡片加入分类的颜色标识（如左缘色带）、拉开标题/时间/标签/优先级的视觉层级，并让卡片在深浅主题下都与背景有清晰区分（适度的背景抬升与阴影）。
7. 现状：卡片上的「完成」勾选框是一个空的灰色方块，像未渲染的占位符。目标：把它重设计为语义清晰、悬停有反馈、勾选有轻动效的勾选控件。

【优先级坐标图】
8. 现状：日视图右侧与全部视图的优先级坐标图只是一个光秃秃的十字，四个象限没有含义标注、轴线很暗（深色下几乎看不见），用户无法理解象限意义。目标：把它升级为可理解的「重要—紧急」四象限矩阵——四个象限有淡背景分区与名称标签（如立即做/计划做/授权做/减少做），坐标轴与原点清晰可见，深浅主题下都可读。
9. 现状：坐标图上的点没有标识，数据很少时大片空白像出错。目标：点可显示对应任务的标识、悬停有精致的信息浮层；数据稀少时给轻量说明而非突兀空白。

【空状态与反馈】
10. 现状：任何视图在没有数据时只是一片空白，没有任何引导，页面像没做完；操作成功与否也缺乏反馈。目标：为各视图（含垃圾箱、搜索无结果）设计空状态——图标/插画 + 引导文案 + 主操作入口；为创建/完成/删除/恢复等操作提供轻量的成功反馈提示；数据加载时以占位骨架替代空白闪烁。
11. 现状：把卡片拖向垃圾箱时缺乏放置反馈。目标：拖拽过程中垃圾箱作为放置目标要有高亮/激活反馈，让用户明确「松手即删除」。

【排版与第一印象】
12. 现状：全局排版层级偏弱；登录页中下部大片空白、品牌标题的发光描边配色偏廉价、验证码输入框辨识度不足且无输入引导。目标：统一正文与标题的排版节奏；打磨登录页的构图平衡、品牌标题质感、验证码框的对比度并补上输入引导文字。

约束与边界：本轮是纯前端的体验与美术升级，禁止改动数据模型、后端接口、鉴权、业务规则或任何既有功能语义；所有视觉改动完成后，001~005 的功能必须逐一回归验证无损。成功标准应可从「界面一致性、交互反馈完整性、信息可读性、空状态覆盖、明暗主题可读性」等可观察维度衡量，且技术无关。
```

> 预期它会抛几处 `[NEEDS CLARIFICATION]`，见下面 clarify 一并答。

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

倾向答案（它没问到就主动补）：
- **设计风格基调**：延续现有「简洁 + 科技感」，强调色沿用（浅色蓝 / 深色 NVIDIA 绿），本轮做体系化而非推倒重来。
- **动效强度**：克制、快速（150–250ms），以「顺滑」为目标，非炫技；遵守 reduced-motion。
- **优先级可视化形式**：倾向颜色梯度 + 强度条（保留数字作辅助），最终形式由 plan 定。
- **象限命名**：用艾森豪威尔四象限语义（立即做/计划做/授权做/减少做），原点 (5,5) 沿用。保留现在的坐标轴上的“重要度”“紧急度”说明坐标轴朝向。
- **空状态**：图标级即可（不必强求定制插画），文案 + 主操作按钮是重点。
- **反馈形式**：轻量 toast（自动消失），不打断操作。
- **边界**：不动后端与数据模型；若发现必须动后端才能实现某条视觉，标出来单独确认，不擅自改。

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有前端技术栈（React 19 + Vite + Tailwind + CSS 变量主题）上做纯前端的体验与视觉升级，不引入后端改动、不动数据模型与业务逻辑。技术约束与实现方向：

【设计系统地基（先做）】
- 在 index.css 扩充设计 token：间距刻度（如 --space-1..8）、圆角（--radius-sm/md/lg）、阴影（--shadow-sm/md/lg，明暗各一套）、字号字重层级（--text-xs..2xl / 权重）、强调色状态变体（--accent / --accent-hover / --accent-active / --accent-subtle）。深浅主题各自定义整套。逐步把各组件里散落的硬编码 rounded/border/颜色替换为消费这些 token。
- 全局过渡：定义统一的 transition 时长/缓动 token；对主题切换（根节点颜色过渡）、Modal/Drawer/ChatPanel 开合、hover、CompleteCheckbox 勾选加过渡；用媒体查询 prefers-reduced-motion 关闭动效。

【顶栏（TopBar.tsx）】
- 三区布局；视图切换封装为 segmented control（带容器背景、统一选中态）；搜索框收窄（聚焦展开可选）；操作区按语义分组加分隔，垃圾箱与主题/AI 视觉分级。

【卡片与可视化】
- 优先级可视化组件：把 0~10 的 importance/urgency 渲染为颜色梯度 + 强度条（复用于卡片与 PriorityPicker 展示态），数字辅助显示。
- 卡片：左缘分类色带（分类→颜色映射，可基于 categoryId 稳定生成）；信息层级用 token 化字号/颜色；深色下卡片背景抬升 + 阴影。
- CompleteCheckbox.tsx：自定义勾选控件，边框 + hover + 勾选动效。

【坐标图（QuadrantView.tsx）】
- 四象限淡背景分区（明暗各一套色）+ 四角象限标签；加粗坐标轴、标原点；点加任务标识 + 精致 hover tooltip（卡片式、阴影）；少数据时轻量说明。

【空状态与反馈】
- 新增 EmptyState 组件（图标 + 文案 + 主操作），接入各 View（DayView/WeekView/MonthView/AllView/TrashView/搜索结果）的空数据分支。
- 新增轻量 Toast 机制（context + portal），create/complete/delete/restore/permanent-delete 成功后触发。
- 新增 Skeleton 占位，接入 TanStack Query 的 isLoading 态替代空白。
- 拖拽：垃圾箱 drop target 的 dragover 高亮反馈。

【排版与登录页】
- 统一排版 token；LoginPage.tsx 构图平衡 + 品牌标题发光/描边配色优化 + PinCodeInput.tsx 对比度增强与输入引导文案。

请产出实现方案，重点标注：token 体系的组织方式与迁移策略（避免一次性全量替换导致回归）；哪些是新增组件（EmptyState/Toast/Skeleton/优先级可视化）、哪些是改造现有组件；动效如何统一且可被 reduced-motion 关闭；如何保证纯前端、零后端改动，以及每类视觉改动后如何回归验证既有功能不受影响。明确列出「本轮绝不触碰」的文件/层（backend 全部、db、业务 hooks 的数据逻辑）。
```

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成后**人工审阅**：确认任务按「设计 token 地基 → 全局动效 → 顶栏重构 → 优先级可视化 + 卡片 + checkbox → 坐标图升级 → 空状态/Toast/Skeleton + 拖拽反馈 → 排版 + 登录页」分组，每条带明确文件路径，且**全部集中在 frontend/src 下**（若有任务指向 backend/db，说明规格越界了，回改）。

### 【动手前】`/speckit-analyze` — 查一致性

```
/speckit-analyze
```

> 只读检查：① 是否真的零后端/数据模型改动（本轮硬边界）；② token 地基任务是否排在其余视觉任务之前（否则改动不成体系）；③ 各条视觉改动是否互不冲突、且不改变既有功能行为。有越界或矛盾先回改规格再 implement。

### ⑥ `/speckit-implement` — 分批实现（先地基，后表层）

```
/speckit-implement 只执行第 N 阶段的任务，完成后停下等我验证，不要继续后续阶段。
```

> 分批顺序（每批 `npm run build` 双端零错误 + `npm run dev` 肉眼验证明暗两主题再进下一批）：
> 1. **设计 token 地基 + 全局动效**（先立规，后面才成体系；这批改完全站观感就会有变化，重点看没破坏布局）。
> 2. **顶栏重构**（分组 + segmented control + 搜索收窄 + 操作区分级）。
> 3. **优先级可视化 + 卡片分类色带/层级 + CompleteCheckbox**（信息可读性核心）。
> 4. **坐标图四象限升级**（分区背景 + 标签 + 轴线 + tooltip）。
> 5. **空状态 + Toast + Skeleton + 拖拽反馈**（反馈体系）。
> 6. **排版统一 + 登录页打磨**（第一印象收尾）。
> 每批完成用「继续执行第 N+1 阶段」推进。**每批都要明暗主题各看一遍**，本轮最容易在深色下翻车（低对比、色带不可见）。

### 全部做完后 `/speckit-converge` — 兜底查缺

```
/speckit-converge
```

> 收口后强烈建议**明暗两主题各走一遍全流程回归**：登录 → 日/周/月/全部/垃圾箱各视图（看空状态、卡片、优先级可视化）→ 坐标图四象限 → 创建/完成/删除/恢复看 toast 反馈 → 拖拽进垃圾箱看 drop 高亮 → 切主题看过渡动效。确认视觉升级的同时 001~005 功能零回归。

# Watson - 第七轮 Spec Kit 迭代（体验和美术升级）

## 修正或者新需求

> 说明：本节是与 AI 逐条讨论、澄清后的**细化定案**（原始口述见下方「附：原始口述需求」）。遇到措辞冲突，以本节为准。本轮编号沿用原始口述（**无第 3 条**，跳过当作无需求）。
> 本轮性质提示：虽命名为「体验和美术升级」，但第 1（日报）、第 2（分类自定义）、第 6（日程阶段）三条实为**新增数据能力，会触及后端 + Drizzle migration**，并非纯前端；其余条目为纯前端交互/视觉。specify/plan 时务必区分。

---

### 1. 日视图 · GRAI 日报（触后端 + 新表 + migration）

- **功能**：日视图新增一块「每日日报」区，用 GRAI 方法帮用户记录与复盘当天琐碎事件。
- **形态**：三个文本输入框，**一排三列**并排；从左到右依次为 **Goal / Result / Analysis & Insight**。
- **位置**：见第 5 条的日视图整体布局定案。日报三栏位于**左侧区域的「下部」**（左侧被上下非均等分割：上部=日程卡片三区块，下部=GRAI 日报），横贯左侧宽度；不在右侧坐标图下方。
- **Markdown**：**编辑时是纯文本**（textarea 直接敲），**失焦后把内容渲染为 Markdown** 展示；再次点击进入编辑态。（编辑/渲染两态切换，非所见即所得。）
- **按天绑定**：日报**按天各存一份**——日视图翻到哪一天（锚定日期），就显示并编辑**那一天**的 GRAI 内容。翻页切换日期时随之切换日报内容。
- **保存**：**失焦时保存**（blur 触发写入后端），不做输入中实时保存。
- **持久化（后端）**：需新增按天存储的日报数据（如 `daily_reports` 表：日期(唯一) + goal + result + analysis 三个文本字段 + 时间戳），配套 GET/PUT 接口（按日期读/写），需一条 migration 建表。日报数据与「日程卡片」完全独立，不影响卡片模型。
- **响应式**：窄屏下三列可堆叠为三行（plan 定断点）。
- **边界**：日报只在日视图出现；周/月/全部/垃圾箱/坐标视图均不显示。

### 2. 分类自定义增删（触后端 · 复用既有 categories 表）

- **现状**：分类是固定集合，创建/编辑卡片时只能从下拉选，不能增删。
- **目标**：分类可由用户自行**新增**与**删除**。
- **入口**：**在分类下拉旁边**加入新增/删除入口（不做独立「管理分类」面板）——即在创建表单与详情编辑态的「分类」选择控件旁，提供「+ 新增分类」与「删除当前分类」的操作。
- **删除后归属**：删除某个分类后，原本属于该分类的卡片，其分类改为 **「无」**——「无」就是一个**名为「无」的固定分类**（不是 null）。即系统保证始终存在一个不可删除的「无」分类作为兜底归属。
- **「无」与现有默认分类「个人」的关系**：当前 `CardFormFields` 里新卡片默认分类是 **「个人」**（`categories.find(c => c.name === "个人")`）。本轮**新增**一个「无」分类作为兜底，**不替换、不改动「个人」这个默认逻辑**——「无」只是删除分类时卡片的归属去向，「个人」仍是新建卡片的默认选项。二者是并列的两个分类。
- **后端**：复用既有 `categories` 表（沿用 name/name_lower 唯一策略）。新增「新增分类」「删除分类」接口；删除分类时把关联卡片的 categoryId 改指向「无」分类。需一条 migration/seed 确保「无」分类存在（幂等插入）；历史上 categoryId 为 null 的卡片是否一并归入「无」由 plan 评估（倾向：null 保持 null 或归「无」，plan 定死）。
- **约束**：「无」分类不可被删除；新增分类名沿用不可重名（trim+小写）规则。

### 4. 顶栏 Logo + 标题（纯前端）

- **现状**：顶栏 logo 位为空，标题文字是「Watson——对话式日程」。
- **目标**：放上一个**简洁线性风格**的 logo（AI 直接生成一个 SVG 图标，符合「日程/日历」语义、线性极简、能适配明暗主题的单色描边风格），标题文字精简为 **「Watson」**。

### 5. 日视图 · 时间三区块分组（纯前端）

- **目标**：日视图内，把**与锚定日相关的已安排卡片**按时间关系分成三个纵向排列的区块，像文章分章节一样，**不需要额外的强视觉区分**——就在原先的（非坐标）主区域里，从上到下依次是带小标题的三段：
  1. **今天截止**（最上）
  2. **正在进行**（中间）
  3. **今天开始**（最下）
  每个小标题下面直接跟属于该段的卡片。
- **精确判据**（已安排卡片一定同时有开始+结束时间；锚定日 = 当前日视图选中的那天）：
  - **今天截止** = 结束时间落在锚定日当天。
  - **今天开始** = 开始时间落在锚定日当天。
  - **正在进行** = 开始时间早于当天、且结束时间晚于当天（横跨当天，当天既非始也非终）。
  - **同时满足「今天截止」和「今天开始」**（当天开始且当天结束）→ **优先归入「今天截止」**（不重复出现在两段）。
  - 三区块**兜住所有与锚定日相关的已安排卡片，不漏不重**。
- **过滤口径（方案 A · 跟随翻页）**：日视图**仍按锚定日期过滤**——只显示与锚定日相关的已安排卡片；与当天无关的卡片（纯未来 / 纯过去 / 未安排）**不在日视图显示**（未安排卡片仍只进全部视图）。
- **日视图整体布局定案**（本轮把日视图左侧主区重构；下图为目标关系）：
  - **现状核实**（`DayView.tsx`）：主区已是 `flex md:flex-row`——**左** = 卡片区（`flex-1 overflow-auto`，本身已是独立滚动容器，用 `CardGrid`）｜**右** = 坐标图（`shrink-0`，凹陷 inset-shadow 方框，约占 1/3 宽 2/3 高居中，`QuadrantView variant="embedded"`）。右侧坐标图**本轮保留不变**。
  - **本轮改动**：只重构**左侧**——把当前"单一滚动卡片区"改为**上下非均等分割（上大下小）**：
    - **上部 = 日程卡片三区块**（今天截止 / 正在进行 / 今天开始，替换现在的 `CardGrid` 平铺）。
    - **下部 = GRAI 日报三列**（第 1 条，Goal/Result/Analysis&Insight）。
  - **独立滚动（关键）**：卡片过多时，**只有左侧上部（三区块）内部上下滚动**（延续现状左侧已有的 `overflow-auto`，但收窄到"上部"这一容器）——**不推动下部日报、不影响右侧坐标图**。三区域各自独立布局/滚动，互不干扰（下部日报与右侧坐标图位置固定）。

    ```
    ┌───────────────────────┬─────────────────┐
    │ 左·上部：日程卡片三区块      │                 │
    │  今天截止 / 进行中 /       │   右：二维坐标图   │
    │  今天开始  【独立滚动】      │  （常驻·凹陷方框） │
    ├───────────────────────┤    保留不变       │
    │ 左·下部：GRAI 日报三列       │                 │
    │  Goal / Result / A&I     │                 │
    └───────────────────────┴─────────────────┘
    ```
- **与坐标图共存**：右侧常驻坐标图（`QuadrantView embedded`）**保留不变**；本轮只改左侧。
- **卡片视觉密度提醒**：本轮后，单张卡片可能同时叠加——分类左缘色带(六轮) + stage 状态徽章(第6条) + 完成 checkbox + 过期泛红(第11条，仅全部视图) + hover 顶部窄条(第8条)。**plan 时须统筹卡片上各视觉元素的排布与优先级，避免信息过载看不清**（如徽章走角标、色带走左缘、窄条走顶边，互不重叠）。

### 6. 新增卡片属性「日程所处阶段」（触后端 + 新字段 + migration）

- **新属性**：`stage`（日程所处阶段），三态枚举：**未开始 / 正在处理 / 等待收尾**。
- **语义**：**与时间完全无关**——纯粹描述用户当前有没有在做这件事，是独立于「时间三区块」的正交维度（时间三区块讲卡片相对当天的时间位置，stage 讲人的处理进度，二者不混）。
- **默认值**：新卡片默认 **「未开始」**；旧数据 migration 一律填 **「未开始」**。
- **视觉表达**：卡片上需有**明显视觉表达**——用一个带颜色的**状态徽章/色标**呈现三态（三态各配一种可辨识颜色，明暗主题下都清晰；具体色值 plan 定，倾向：未开始=中性灰、正在处理=强调色/进行中暖色、等待收尾=收尾提示色）。
- **筛选**：**全部视图**新增按「日程阶段」筛选的筛选器（融入全部视图既有的多维筛选体系）。
- **后端**：`schedule_cards` 新增 `stage` 列（枚举，默认「未开始」），create/update 支持读写该字段，migration 给历史卡片填默认值。

### 7. 继续提升美术/交互体验（纯前端 · 本轮具体设计如下）

> 以下是本轮拟定的具体美术/交互提升项（在前六轮已建立的 token/动效/四象限/卡片可视化体系之上再打磨一层）。全部**纯前端**，不动数据模型/后端/业务逻辑。用户可从中勾选纳入本轮。

**7.1 视图切换的滑动指示器（segmented control 升级）**
- 现状：顶栏视图切换是分段控件，选中态靠背景块。目标：给选中背景块加一个**滑动过渡**——切换视图时高亮块从旧位置平滑滑到新位置（transform 过渡，200ms ease-out），而非瞬间跳变。｜落点：`TopBar.tsx`

**7.2 卡片进出场的交错动画（stagger）**
- 切换视图 / 筛选 / 翻页导致卡片列表重渲染时，卡片以**轻微交错的淡入+上移**依次出现（每张延迟 20–30ms 递增，整体控制在 300ms 内），让列表刷新有节奏感而非整片闪现。数据量大时限制只对前 N 张做交错，其余直接显示避免拖沓。遵守 reduced-motion。｜落点：`CardGrid`/各 View

**7.3 优先级拖动条的实时数值气泡**
- 配合第 9 条的横向拖动条：拖动时在滑块上方浮出一个**跟随的数值气泡**（显示当前 0~10 的整数），松手后气泡淡出。让离散吸附有明确反馈。｜落点：`PriorityPicker`（拖动条版）

**7.4 坐标图圆点的悬停放大 + 连带高亮**
- 坐标图上 hover 某个聚合圆时，圆**轻微放大 + 描边高亮**，同时（若可行）在左侧对应的卡片上做**联动高亮**（反之 hover 卡片时高亮坐标图上对应的点）。强化"卡片↔象限位置"的对应认知。｜落点：`QuadrantView` + 卡片区（若联动成本高，可只做圆点自身放大高亮，联动作为可选）

**7.5 主题切换的柔性过渡**
- 切换明/暗主题时，根节点颜色变量做一个**短暂的颜色渐变过渡**（约 250ms），避免整屏配色瞬间硬切造成的突兀闪烁。（第六轮已引入动效 token，此处确保主题切换确实吃到根色过渡。）遵守 reduced-motion。｜落点：`index.css` + 主题切换逻辑

**7.6 空状态插画的呼吸微动**
- 各视图空状态（第六轮已加图标+文案）的图标加一个**极轻的呼吸/浮动微动画**（缓慢缩放或上下浮 2–3px，循环），让空页面不死板。reduced-motion 下静止。｜落点：`EmptyState`

**7.7 GRAI 日报编辑态的聚焦强调**
- 配合第 1 条日报：某一栏（Goal/Result/A&I）进入编辑态时，该栏**边框/背景轻微强调**（accent-subtle 底 + 强调色描边），其余两栏保持常态，明确"当前在编辑哪一栏"；失焦渲染 md 后回归常态。｜落点：日报组件

**7.8 滚动区域的渐隐边缘（fade mask）**
- 配合第 5 条三区块独立滚动：给可滚动的三区块容器上下边缘加**渐隐遮罩（mask/gradient）**，提示"上/下还有更多内容可滚"，比生硬的滚动条截断更精致。｜落点：三区块滚动容器

**7.9 危险操作的确认框语义强化**
- 删除/永久删除的二次确认框（第六轮已收窄），给主危险按钮更明确的**危险语义配色**（红色系）与 hover 反馈，取消按钮弱化为次要样式，降低误点。｜落点：`ConfirmDialog`

**7.10 拖拽到垃圾箱的轨迹反馈增强**
- 配合已有拖拽删除：拖拽卡片时给被拖卡片**半透明+轻微缩放的拖影**，垃圾箱图标在可放置时除高亮外再加一个**轻微的"吸附"脉冲**，松手成功后垃圾箱做一个**短促的接收动画**（图标抖动/盖子开合意象）。reduced-motion 下降级为纯静态高亮。｜落点：拖拽逻辑 + 垃圾箱图标

### 8. 卡片 hover 顶部窄条动画（纯前端）

- **范围**：日视图与全部视图的卡片。
- **效果**：鼠标悬停到卡片上时，卡片**顶部出现一条横向窄条动画**（如强调色从一侧滑入的细条），**纯装饰性**——仅用来标识「鼠标已悬停在此卡片上」，**不承载任何操作**（不是按钮条）。移开鼠标窄条消失。遵守 reduced-motion。
- **归属注意**：窄条是**卡片自身内部的元素**（跟随单张卡片，随卡片一起滚动），**不属于滚动容器边缘**——因此不受第 7.8「三区块滚动容器上下渐隐遮罩」影响，二者互不干扰。

### 9. 优先级改为横向拖动条（纯前端 · 替换旧交互）

- **现状**：点击重要/紧急属性后竖向拉开数字列（0 下 10 上）选值（第四轮做的 PriorityPicker）。
- **目标**：**替换**为**横向颜色拖动条**——在创建卡片和查看/编辑详情时，直接拖动「重要程度」「紧急程度」上的横向色条来调值；仍是**离散 0~10**（拖动吸附到整数刻度）。色条颜色随值梯度变化（低=冷/淡，高=暖/强）。**旧的竖向数字列交互整体替换掉，不并存。**
- **只读态注意**：拖动条只用于**可编辑态**（创建表单、详情编辑态）。详情**只读展示**时，优先级仍沿用第六轮已做的「颜色梯度 + 强度条」可视化展示，不换成拖动条（拖动条是输入控件，不是展示控件）。
- **对坐标图无影响**：拖动条只改输入交互，importance/urgency 值域仍是 0~10 整数，第四轮坐标图按此定位的逻辑不变。

### 10. Bug：导航按键 hover 上浮被遮挡（纯前端 · bug）

- **现象**：日/周/月视图的「上一 / 本 / 下一」翻页按键，hover 时有上浮动画，但**按键框体上部被遮盖看不到**（疑似父容器 overflow:hidden 裁切 / z-index / 上方 padding 不足）。
- **修复**：让上浮后的按键完整可见（排查并修 overflow/z-index/容器上内边距）。

### 11. 全部视图 · 过期未完成卡片背景泛红（纯前端）

- **规则**：某任务**结束时间已过去、但卡片仍存在（未完成、活跃态）** → 卡片**背景泛红**作为提示色。
- **范围**：**仅全部视图**生效（按原口述，不扩大到日/周/月）。
- **表现形式**：**卡片背景泛红**（不是仅左缘色带、也不是仅边框；用一层低饱和红色背景叠加，明暗主题各调）。
- **例外**：**未安排卡片不受影响**（无结束时间，无所谓过期）；已完成/已删除（进箱）的卡片不在常规视图出现，天然不适用。

### 12. 创建/详情弹层的进出场动画（纯前端）

- **目标**：创建卡片、打开详情、关闭详情，**都要有流畅的出现/消失动画**（淡入淡出 + 轻微缩放/位移），不要突兀地瞬间出现或消失。
- **说明**：第六轮已给 Modal 建立过统一动效 token；本条按 AI 判断落地——若创建/详情弹层当时未完全吃到那套进出场动效（等于漏做），本轮补齐并统一；遵守 reduced-motion。

### 13. 创建/详情弹层的字段布局重排（纯前端）

- **现状**（`CardFormFields.tsx`）纵向排布为：标题 → `时间（可选）` legend + 开始时间(独占一行) + 结束时间(独占一行) → 重要/紧急(已是同行两列) → 分类(独占一行) → 描述(rows=2)。
- **目标**（弹层整体大小不变，重排内部字段以腾出描述空间）：
  1. **删掉「时间（可选）」这一行 legend**。
  2. **开始时间 + 结束时间放在同一行**（两列）。
  3. **重要程度 + 紧急程度保持相对位置不变**（仍同行两列）。
  4. 下面改成 **分类 + 日程所处阶段（第 6 条新属性）同一行**（两列）。
  5. 上述压缩腾出的纵向空间，**全部给「描述」扩充高度**（textarea 行数/高度加大）。
- **约束**：字段语义与校验不变；仅重排布局与增高描述框。日程阶段字段随第 6 条一并加入表单。

---

## 附：原始口述需求（细化前，仅存档对照）

> 以下为最初的原始口述，正式口径以上面「修正或者新需求」为准。

1. 在每日视图下添加日报的功能，帮助记录和规划一些琐碎事件，使用GRAI方法帮助分析每日
用户体验：用户打开网页之后，日视图中下方出现三个文本输入框，成三列一排排列。这些输入文本框可以渲染md文本内容。从左往右分别是Goal，Result，Analysis&Insight。用户可以在任何时候编辑这些文本框中的内容。并且自动保存。
2. 丰富日程卡片分类
日程卡片的分类属性改为可以自己添加和编辑，比如用户可以直接点击加好添加一个新的分类属性，或者删除某个分类属性。如果某个分类属性被删除，原本是这个分类属性的卡片，分类改为“无”。
4. 添加logo
现在浏览器打开网页的时候上方的bar显示的是空的logo和文字“Watson——对话式日程“，你先随便找个符合意思的logo放上去，然后把文字部分改为”Watson“
5. 添加时间筛选器和排序器
在日视图下把所有任务根据其时间属性，今天截至 正在进行 今天开始 分为三个区块。最上方是今天截至的任务，中间是正在进行，下方是今天开始。如果出现同时满足今天截至和今天开始的任务优先放置在今天截至。
6. 任务属性，未开启，正在进行，要有明显的视觉表达
添加一个新的卡片属性“日程所处阶段”包含属性“未开始”“正在处理”“等待收尾”，注意这个属性和时间没有任何关系，单纯是描述用户当前有没有在做这件事。同时也在全部视图下添加这个筛选器。
7. 继续提升美术体验
继续提出若干可以优化交互和美术体验的设计
8. 添加鼠标指针悬浮在卡片上时的交互
在日视图或者全部视图中添加当鼠标指针悬浮到卡片上空后的交互效果，如鼠标放置在上面之后卡片上方出现一个横向的窄条动画
9. 添加直接拖拽重要程度和紧急程度
点击卡片或者创建卡片时，直接拖拽“重要程度”“紧急程度”上的横向颜色拖动条来调整（仍然是离散的0～10）
10. 解决当前日/周/月视图下“上一”“本”“下一”几个按键的显示bug
当前鼠标放置在这些按键的时候，按键会呈现上浮的交互效果，但是按键框体的上部似乎被遮盖了。看不到
11. 全部视图下如果某个任务的结束时间已经过去但是仍然存在 卡片提示色就会变为红色。（未安排时间的卡片不受此影响）未安排
12. 修改创建任务和查看任务详情时的视图。首先不管是创建，还是打开详情还是关闭详情都要有流畅的动画效果，不要突然出现或者消失
13. 修改创建任务和查看任务详情时的视图，整体大小不用发生变化，把“时间（可选）”这一行删掉。把开始时间和结束时间放在同一行，重要程度和紧急程度保持相对位置不变，下面改成分类和日程所属阶段在同一行。留出来的空间给“描述”扩充高度

## Spec Kit指令

> 前提：006 已 implement + converge 收口，当前激活 feature 是 `006-ux-visual-upgrade`。跑 `/speckit-specify` 会自动扫描现有编号（当前最大 006）新建 **007**，并把 `.specify/feature.json` 指针切过去。**开新轮 = 跑 specify。**
> 铁律不变：**specify 只谈「做什么/为什么」，技术栈（建表、migration、组件、拖拽/滑块/md 渲染方案）留到 plan。**
> ⚠️ 本轮特殊性：命名虽为「体验和美术升级」，但实际含 **3 处触后端 + migration**（第 1 日报新表、第 2 分类增删、第 6 stage 新字段），是继第四轮后改动较深的一轮；其余为纯前端。specify/plan/analyze 都要盯住"哪些触后端、哪些纯前端"这条边界，并盯住第 5 条日视图三区重构与既有 DayView 的 delta。
> 口径以上面「## 修正或者新需求」细化定案为准（无第 3 条）；第 7 条 10 个子项（7.1–7.10）全部纳入本轮。

---

### ② `/speckit-specify` — 写第七轮基线规格

```
/speckit-specify 这是 Watson 的第七轮迭代，命名为「日报、分类管理、日程阶段与体验升级」。在已上线的 001~006 之上，新增每日 GRAI 日报、可自定义的分类管理、日程处理阶段属性、日视图时间分区与内嵌布局重构，并做一批交互/美术打磨与若干缺陷修复。以下按用户价值描述要做什么，不涉及技术实现。凡标注「触及数据」的条目会新增/改变持久化数据，其余为纯前端呈现与交互增强；请在规格里区分二者，但具体技术留到 plan。

【每日 GRAI 日报（触及数据）】
1. 日视图新增「每日日报」区，帮所有者用 GRAI 方法记录与复盘当天的琐碎事件。呈现为一排三列的三个文本区，从左到右为 Goal、Result、Analysis & Insight。编辑时是纯文本，失去焦点后把内容按 Markdown 渲染展示，再次点击可回到编辑。日报按天各存一份——日视图翻到哪一天就显示并编辑那一天的日报，翻页切换日期随之切换内容；失去焦点时自动保存。日报只出现在日视图，不进其他视图。

【分类自定义管理（触及数据）】
2. 现状：日程分类是固定集合，创建/编辑卡片时只能从下拉中选，不能增减。目标：所有者可以自行新增分类、删除分类，入口就在分类选择控件旁边（不做独立管理页）。系统始终存在一个名为「无」的兜底分类且不可删除；当某个分类被删除时，原本属于它的卡片自动归入「无」。新增分类沿用不可重名规则。（说明：这不改变新建卡片的既有默认分类行为，「无」只是删除分类后的归属去向。）

【日程所处阶段（触及数据）】
3. 新增一个卡片属性「日程所处阶段」，取值为「未开始 / 正在处理 / 等待收尾」三选一，用来描述所有者当前有没有在做这件事——它与卡片的时间安排完全无关，是一个独立维度。新卡片默认为「未开始」，历史卡片一律视为「未开始」。该阶段要在卡片上有明显的视觉标识（颜色区分三态，明暗主题下都清晰）。全部视图新增一个按「日程阶段」筛选的筛选器。

【日视图时间分区与布局（呈现增强）】
4. 现状：日视图左侧是把当天卡片平铺成一个列表。目标：把左侧当天卡片按其与当天的时间关系分成三段依次呈现，像文章分章节——最上「今天截止」、中间「正在进行」、最下「今天开始」；判据为：结束时间落在当天=今天截止、开始时间落在当天=今天开始、开始早于当天且结束晚于当天=正在进行；若一张卡片当天既开始又结束，则归入「今天截止」不重复。三段兜住所有与当天相关的已安排卡片，不漏不重；日视图仍只显示与所锚定日期相关的已安排卡片（与当天无关的、未安排的都不显示，未安排仍只进全部视图）。
5. 日视图的整体布局调整为：右侧保留既有的常驻二维优先级坐标图不变；左侧上下分割（上大下小），上部是上述卡片三区段、下部是每日 GRAI 日报区。当卡片很多时，只有左侧上部区段内部可上下滚动查看更多，且该滚动不得推动下部日报、也不影响右侧坐标图；三个区域各自独立、互不干扰。

【交互与美术升级（纯呈现）】
6. 顶栏：把当前空的 logo 位换上一个符合「日程」语义、简洁线性风格的标识，并把标题文字精简为「Watson」。
7. 卡片悬停反馈：在日视图与全部视图，鼠标悬停到某张卡片上时，卡片顶部浮现一条横向窄条动画作为「已悬停」的纯装饰标识，不承载任何操作，移开即消失。
8. 优先级输入方式：把重要程度、紧急程度的取值方式从「点击后弹出的竖向数字列」改为「可直接拖动的横向色条」——拖动即调整，仍是 0~10 的离散整数（吸附到整数），色条颜色随取值由淡到浓表达强弱。此改动用于创建与编辑卡片；查看（只读）时优先级维持既有的可视化展示。
9. 一批体验/美术打磨（在既有视觉体系上再提升一层，均为纯呈现、不改数据与业务）：视图切换的选中高亮做平滑滑动过渡；卡片列表刷新时以轻微交错的淡入依次出现；优先级色条拖动时浮出跟随的当前数值气泡；坐标图上悬停某个聚合圆时圆放大并高亮（并尽量与左侧对应卡片联动高亮）；切换明暗主题时整屏配色做柔和过渡而非硬切；各视图空状态的图标加极轻的呼吸微动；日报某一栏进入编辑时该栏轻微强调、其余保持常态；可滚动的卡片区段上下边缘加渐隐提示「还有更多」；删除/永久删除的确认框强化危险语义配色、弱化取消；把卡片拖入垃圾箱时给拖影、垃圾箱放置目标高亮与成功接收的轻反馈。以上一切动效都要尊重系统「减少动态效果」偏好。
10. 创建卡片、打开与关闭卡片详情，都要有流畅的出现/消失过渡动画，不再突兀地瞬间出现或消失。
11. 卡片创建/详情表单的字段排布优化（弹层整体大小不变）：去掉「时间（可选）」这一行标签，把开始时间与结束时间并排到同一行；重要程度与紧急程度保持并排不变；其下把「分类」与「日程所处阶段」并排到同一行；由此腾出的空间用来加大「描述」输入区的高度。

【缺陷修复】
12. 全部视图中，一张已安排的卡片如果其结束时间已经过去、卡片却仍然存在（未完成、处于活跃态），其卡片背景应泛红作为过期提示；未安排（无结束时间）的卡片不受影响。此提示只在全部视图生效。
13. 修复日/周/月视图中「上一 / 本 / 下一」翻页按键的显示缺陷：当前悬停时按键有上浮动画，但按键上部被遮挡看不全；修复后上浮的按键应完整可见。

约束与边界：本轮不改变单用户鉴权、AI 不操作日程、卡片核心生命周期（完成/删除/恢复/垃圾箱）等既有宪法约束与功能语义。新增的日报数据、分类的增删、日程阶段属性三处会触及持久化，需与既有的「仅标题必填」「未安排只进全部视图」「已安排一定有起止」「标题不可重名」等规则彼此自洽；日视图布局重构不得破坏既有的坐标图、翻页、拖拽删除等能力。所有纯呈现的美术/动效改动完成后，001~006 已验证的功能必须逐一回归无损。
```

> 预期它会抛几处 `[NEEDS CLARIFICATION]`，见下面 clarify 一并答。

---

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

倾向答案（它没问到就主动补，均以上面定案为准）：
- **日报存储**：按「日期」为键各存一份（date 唯一），字段 Goal/Result/Analysis 三段文本 + 时间戳；与日程卡片完全独立，不影响卡片模型；失焦保存；只在日视图出现；窄屏三列可堆叠为三行。
- **分类「无」**：名为「无」的固定分类，不可删除，幂等保证存在；删分类→其卡片 categoryId 改指「无」；不改动新建卡片默认「个人」的既有行为；null 分类卡片是否并入「无」由 plan 定（倾向保持现状）。
- **日程阶段**：三态「未开始/正在处理/等待收尾」，默认与历史迁移一律「未开始」；与时间无关；卡片上以带色徽章/角标呈现；全部视图加该筛选。
- **日视图三区段判据**：结束落当天=今天截止、开始落当天=今天开始、跨越当天=正在进行；当天既始又终→归今天截止不重复；三段兜住当天所有相关已安排卡片；日视图仍按锚定日过滤，未安排/无关卡片不显示。
- **日视图布局**：右侧坐标图不变；左侧上下非均等（上大下小），上=三区段（内部独立滚动，不推动下部日报、不影响右侧坐标图）、下=GRAI 日报。
- **优先级色条**：替换旧竖向数字列，不并存；离散 0~10 吸附；仅编辑态用色条，只读态沿用第六轮强度条可视化；坐标图定位逻辑不变。
- **过期泛红**：仅全部视图；已安排且结束时间已过且活跃未完成→背景泛红；未安排不受影响。
- **卡片视觉密度**：单卡可能同时有分类左缘色带 + 阶段徽章 + 完成 checkbox + hover 顶部窄条（+ 全部视图下过期红底），需统筹排布不重叠（色带走左缘、徽章走角标、窄条走顶边）。
- **动效**：一律尊重 prefers-reduced-motion；时长克制（约 150–250ms）。

---

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上迭代，沿用既定选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向，并明确标注纯前端 vs 触及后端/DB：

【触及后端 + DB（需 Drizzle migration，注意旧数据兼容）】
- 每日 GRAI 日报：在 backend/src/db/schema.ts 新增 daily_reports 表（date 唯一键 + goal/result/analysis 文本 + createdAt/updatedAt），写一条 migration 建表（走既有 db/migrate.ts 流程）；新增按日期读写的接口（如 GET /api/daily-reports/:date 与 PUT /api/daily-reports/:date，upsert 语义），新增对应 route 与 service；纳入既有会话鉴权中间件。日报与 schedule_cards 完全解耦。
- 分类自定义增删：复用 categories 表与 category.service.ts / routes/categories.ts；确保存在一个不可删除的「无」分类（seed/幂等插入，参考 db/seed.ts），删除分类接口把关联卡片 categoryId 改指「无」再删该分类（事务）；沿用 name/name_lower 唯一。评估 null categoryId 卡片处理并在 plan 定死。前端在分类下拉旁加「新增/删除分类」入口，调用这些接口后 invalidate categories 与 cards 查询。
- 日程阶段 stage：在 schedule_cards 增加 stage 列（枚举 not_started | in_progress | wrapping_up，默认 not_started），migration 给历史卡片填默认值；schedule.service.ts 的 create/update 读写该字段；GET /api/cards 支持按 stage 过滤（并入既有 view/filter 参数体系）；同步更新 backend/src/types.ts 的卡片类型与前端 lib/api.ts 的 ScheduleCard 类型。

【纯前端】
- 日视图重构（DayView.tsx）：把现有「左 CardGrid + 右 QuadrantView(embedded)」的左侧，改为上下非均等两段——上=按三区段（今天截止/正在进行/今天开始）分组的卡片区、下=GRAI 日报区；上部为唯一的 overflow-auto 独立滚动容器，下部日报与右侧坐标图不随其滚动（用嵌套 flex + min-h-0 控制）。三区段的时间归类在前端按锚定日 + 卡片 start/end 计算（沿用 calendar/tz 的时区）。
- GRAI 日报组件：新增组件，三列文本区，编辑态用 textarea、失焦渲染 Markdown（选一个轻量 md 渲染方案，plan 定）；按 date 用 TanStack Query 读、失焦 PUT 保存；窄屏三列堆叠。
- 分类管理入口：改造 CardFormFields.tsx 的分类字段旁加「+新增 / 删除」控件（弹小输入/确认），复用新分类接口。
- 日程阶段前端：CardFormFields 加 stage 选择字段（第 11 条要求它与「分类」并排同行）；卡片组件加 stage 徽章/角标（三态配色走 CSS 变量，明暗各调）；AllView.tsx 的筛选器加 stage 维度。
- 顶栏 logo + 标题：TopBar 换上一个内联 SVG 线性 logo（单色 currentColor 描边，适配明暗），标题文案改「Watson」。
- 优先级横向色条：把 PriorityPicker 的竖向数字列实现替换为横向 range 滑块/自定义拖动条（离散 0~10、吸附整数、颜色梯度），复用于创建与编辑态；只读展示仍用第六轮的强度条可视化组件；拖动时浮出跟随数值气泡（第 9 条的 7.3）。
- 卡片 hover 顶部窄条：卡片组件加一个顶部装饰窄条（卡片内部元素，随卡片滚动，hover 时 CSS 过渡滑入），只在日/全部视图启用；不受滚动容器渐隐遮罩影响。
- 表单字段重排（CardFormFields.tsx）：去掉「时间（可选）」legend；开始+结束时间同行（两列 grid）；重要+紧急保持同行；分类+日程阶段同行；描述 textarea 增高。整体弹层尺寸不变。
- 过期泛红：AllView 中，已安排且 endAt < now 且状态为 active 的卡片背景加低饱和红色叠加（明暗各调）；未安排不适用；仅全部视图。
- 翻页按键遮挡 bug（ViewTimeNav.tsx 及其容器）：排查父容器 overflow:hidden 裁切 / z-index / 顶部 padding 不足，修到上浮态完整可见。
- 弹层进出场动画（第 10 条）+ 一批美术打磨（第 9 条 7.1–7.10）：均在既有 index.css token/动效体系上做；Modal/Drawer 进出场过渡、segmented control 滑动指示、卡片列表 stagger 淡入、坐标图圆点 hover 放大高亮（QuadrantView.tsx）、主题切换根色过渡、EmptyState 图标微动、日报编辑态强调、滚动区渐隐遮罩、ConfirmDialog 危险语义配色、拖拽进垃圾箱的拖影/放置高亮/接收反馈。全部动效走统一 duration/ease token 并受 prefers-reduced-motion 关闭。

请产出实现方案，重点标注：三处触后端改动（日报新表 + 接口、分类增删 + 「无」兜底、stage 新列 + 过滤）的 migration 顺序与旧数据兼容；日视图三区独立滚动的布局实现（嵌套 flex + min-h-0，确保只有上部滚动）；优先级色条替换对 PriorityPicker 既有调用点的影响面；表单重排与新增 stage 字段的协同；哪些纯前端、哪些触后端（预期后端集中在日报、分类、stage 三处）；单卡多视觉元素（色带/徽章/checkbox/窄条/过期红底）的排布统筹方案。
```

---

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成后**人工审阅**：确认任务按「DB & 后端（日报表+接口 → 分类增删+「无」兜底 → stage 列+过滤）→ 日视图三区重构 → GRAI 日报组件 → 分类管理入口 → 日程阶段前端(字段+徽章+筛选) → 优先级横向色条(替换) → 表单字段重排 → 顶栏 logo+标题 → 卡片 hover 窄条 → 过期泛红 → 翻页按键遮挡 bug → 弹层动画 + 美术打磨 7.1–7.10」分组，每条带明确文件路径，并把触及后端/DB 的三块清晰单列标出。

---

### 【动手前】`/speckit-analyze` — 查一致性

```
/speckit-analyze
```

> 只读检查 spec/plan/tasks 三者是否自洽，尤其盯这几组相互牵连处：
> - **触后端边界**：本轮明确只有日报、分类增删、stage 三处触后端，其余纯前端；确认没有把纯前端条目误排成后端任务，也没漏掉这三处的 migration。
> - **日视图三区 × 独立滚动 × 日报 × 坐标图**：三区段只兜「与当天相关的已安排卡片」，未安排/无关不显示；上部滚动不影响下部日报与右侧坐标图——布局与数据口径是否一致。
> - **stage（处理阶段）× 时间三区段**：两者正交（一个讲进度、一个讲时间），确认没有把 stage 和时间分区混为一谈。
> - **优先级色条替换**：竖向数字列被整体替换、只读态仍用强度条、坐标图定位不变——三处口径是否一致。
> - **分类「无」× 默认「个人」× 不可重名**：新增「无」不改默认「个人」、不可删除、删分类归入「无」——是否彼此自洽。
> - **单卡视觉密度**：色带/阶段徽章/完成 checkbox/hover 窄条/过期红底的排布有没有冲突规格。
> 有越界或矛盾先回改规格，再进 implement。

---

### ⑥ `/speckit-implement` — 分批实现（本轮含 3 处后端，务必分批）

```
/speckit-implement 只执行第 N 阶段的任务，完成后停下等我验证，不要继续后续阶段。
```

> 强烈建议按下面顺序**逐批跑、每批 `npm run build` 双端零错误 + `npm run dev` 手动验证（明暗两主题各看一遍）**再进下一批。前三批动后端/数据模型，最容易连带打挂既有功能：
> 1. **DB + 后端（最高风险，可拆成 3 小批分别验证）**：① 日报 daily_reports 表 + GET/PUT 接口；② 分类「无」兜底 + 新增/删除分类接口（删分类归入「无」的事务）；③ schedule_cards 加 stage 列 + create/update 读写 + GET /api/cards 按 stage 过滤 + migration 迁历史值。每小批跑完先确认旧数据被正确迁移、既有 CRUD/各 view 过滤无回归。
> 2. **日视图三区重构 + GRAI 日报组件**（DayView 布局重构 + 日报读写 UI；重点验证只有上部滚动、下部日报与右侧坐标图不动、翻页切日报内容）。
> 3. **日程阶段前端 + 分类管理入口**（CardFormFields 加 stage 字段与分类增删入口、卡片 stage 徽章、AllView 加 stage 筛选）。
> 4. **优先级横向色条（替换）+ 表单字段重排**（PriorityPicker 换实现 + CardFormFields 重排；注意别丢校验、只读态仍用强度条）。
> 5. **卡片 hover 窄条 + 过期泛红 + 翻页按键遮挡 bug**（较独立的前端小改）。
> 6. **弹层进出场动画 + 美术打磨 7.1–7.10**（纯样式/动效收尾；本轮最容易在深色下翻车，明暗各验证；全部动效确认受 reduced-motion 关闭）。
> 每批完成用「继续执行第 N+1 阶段」推进；靠 tasks.md 的 `[X]` 断点续跑。

---

### 全部做完后 `/speckit-converge` — 兜底查缺

```
/speckit-converge
```

> implement 全跑完后用它对照 spec/plan/tasks 扫缺口，append 成新任务再补齐。converged 即本轮收口。本轮改了数据（日报表、分类增删、stage 字段），收口后强烈建议**明暗两主题各走一遍端到端回归**：登录 → 日视图（左上三区段独立滚动 + 左下 GRAI 日报失焦保存/翻页切换 + 右侧坐标图不动）→ 建卡（含 stage、拖动色条调优先级、表单新布局）→ 分类新增/删除并确认卡片归入「无」→ 全部视图按 stage 筛选 + 过期卡片泛红 → 各视图卡片 hover 窄条 → 翻页按键上浮完整可见 → 创建/详情弹层进出场动画 → 切主题看柔和过渡，确认视觉升级同时 001~006 功能零回归。

---

# Watson - 第八轮 Spec Kit 迭代（日程卡片组合功能）


## 需求与设计逻辑

本轮引入「父卡片」机制，用卡片组合解决标准卡片数量过多时的视图冗余问题：在概览类视图（周 / 月 / 全部）里把一组卡片折叠成一张父卡片，在专注当天的日视图里仍摊开显示每张子卡片。

---

### 一、卡片模型：两种卡片

- 全系统只有两种日程卡片：**标准卡片** 与 **父卡片**。
- **标准卡片**：即本项目此前的普通卡片，保留全部既有属性（标题、时间、重要度 / 紧急度、分类、阶段、描述、生命周期等）。只能通过点击「+」手动创建。
- **父卡片**：一个用来承载多张标准卡片的容器。只能通过在全部视图拖拽标准卡片组合形成，不能手动「+」创建。父卡片只有以下属性：名称、开始时间、结束时间、已 / 未安排状态；父卡片不具备重要度 / 紧急度、分类、阶段等标准卡片专有属性。
- 一张标准卡片同时只能属于一个父卡片。父卡片里只能装标准卡片，不允许多层嵌套（父卡片不能再装父卡片）。

---

### 二、组合与移出（拖拽交互，仅在全部视图）

组合操作只能在全部视图中通过拖拽完成。共有三条组合路径，除此之外没有其它组合方式：

1. **标准卡片 A 拖到标准卡片 B**：弹出「创建父卡片」确认框；确认后 A、B 组成一张新父卡片 D，A、B 从全部视图消失，由 D 取代。
2. **标准卡片 A 拖到已有父卡片 D**：A 进入 D，成为 D 的一张子卡片。
3. **父卡片 A 拖到父卡片 B**：A 的所有子卡片并入 B，A 因子卡清空而消失。

无效操作：父卡片拖到标准卡片上不产生任何动作。

「创建父卡片」确认框包含：
- **名称**：必填，不可重名。
- **时间**：默认按子卡片自动汇总，允许手动修改（修改约束见第三节）。
- **即将进入该父卡片的卡片列表**：展示本次会被组合进去的标准卡片。
- **取消**：拖错时可取消，不创建父卡片。

移出：打开父卡片详情后，把子卡片从详情页里拖出即为移出。被移出的标准卡片恢复为独立卡片，重新显示在各视图中。

---

### 三、父卡片的时间与已 / 未安排状态

- **自动汇总**：父卡片时间默认等于所有子卡片时间的包络——开始时间 = 所有子卡片开始时间的最小值，结束时间 = 所有子卡片结束时间的最大值。例如子卡片 A 为 07-10 至 07-11、B 为 07-19 至 08-20，则父卡片默认为 07-10 至 08-20。
- **手动调整只能更宽、不能更窄**：允许把父卡片时间调得比子卡片包络更宽，但不能窄于包络。约束为：父卡片开始 ≤ 所有子卡片开始的最小值，且 父卡片结束 ≥ 所有子卡片结束的最大值。承上例，可改为 06-10 至 08-20，但不能改为 07-11 至 08-19。
- **子卡片时间变化时的跟随**：若用户手动调整过父卡片时间，之后某张子卡片时间发生变化时，保留用户手调的值不变；仅当子卡片时间突破了当前父卡片边界，父卡片边界才被动扩展到刚好包住新的子卡片包络。
- 当父卡片内所有子卡片都是未安排（无时间）时，父卡片的开始与结束时间也为空。
- **已 / 未安排状态**：父卡片有已 / 未安排属性——有时间即为已安排，无时间即为未安排；在全部视图中据此归入对应分段。
- 父卡片只是承载容器，不改变子卡片自身的任何属性。子卡片仍按其自己的时间在日视图中分区、在坐标图中定位。

---

### 四、各视图的差异化呈现

设标准卡片 A、B 组成父卡片 D，C 为独立标准卡片。各处的显示规则如下：

| 场景 | 父卡片 D | 子卡片 A、B | 独立卡片 C |
|---|---|---|---|
| 全部视图 | 显示 D，并按其已 / 未安排状态进入对应分段 | 不显示，由 D 取代 | 照常显示 |
| 周 / 月视图 | 显示 D，按其时间摆入日历网格 | 不显示，点开 D 才可见 | 照常显示 |
| 日视图 | 不显示 D | 照常显示 A、B，各按自己的时间进入「今天截止 / 正在进行 / 今天开始」三区块；卡片右下角加一个折页角标标识其归属父卡片，点击角标打开父卡片详情 | 照常显示 |
| 垃圾箱 | 不显示 D | 从父卡片进入的标准卡片显示时，标注其父卡片名称 | 照常显示 |
| 二维坐标图 | 不出现 D | 照常按各自的重要度 / 紧急度定位 A、B | 照常显示 |

补充说明：
- 坐标图完全不因本轮改动而变化：标准卡片进入父卡片后，仍照常在坐标图上定位。
- 周 / 月视图中，父卡片按其时间摆入日历网格；跨多天时沿用既有持续型日程的跨格 / 跨列展示规则。
- 日视图中子卡片的折页角标用于标识归属父卡片并可点击跳转到父卡片详情，具体美术样式后续细化，本轮先采用折页角标方案。

---

### 五、父卡片的生命周期

- 父卡片没有「删除」和「完成」操作，它不是一个可独立删除或完成的实体，只能编辑名称 / 时间、管理子卡片。
- 父卡片通过「清空」而消失：其子卡片经由移出、完成或删除而减少，当子卡片减到 0 张时，父卡片直接消失，不进垃圾箱。
- 父卡片剩 1 张子卡片时仍然存在，不自动解散。
- 子卡片的完成 / 删除连锁：完成或删除某张子卡片时，该卡片按既有语义进入垃圾箱，同时从父卡片中移除；若因此使父卡片子卡片清零，父卡片随之消失。

---

### 六、父卡片详情页

- 入口：在全部视图点击父卡片，或在日视图点击子卡片右下角的折页角标，都打开同一个父卡片详情页。
- 可执行的操作：
  - 修改名称（必填、不可重名）。
  - 修改时间（受第三节的时间约束）。
  - 查看并管理子卡片列表：把子卡片拖出即为移出。
- 子卡片在详情页内按全局创建时间排序。

---

### 七、重名与恢复规则

- 重名范围：父卡片名称与标准卡片标题互斥、都不可重名——父卡片名称既不能与任何标准卡片标题相同，也不能与其它父卡片名称相同。沿用既有的去首尾空格 + 小写比较，仅对活跃卡片判定。
- 从垃圾箱恢复曾属于某父卡片的标准卡片时：
  - 若同名父卡片仍存在，该卡片直接归回现存的父卡片，不新建。
  - 若该父卡片已不存在（此前因清零而消失），则自动重建同名父卡片（时间按当前子卡片重新汇总），该卡片归入其中。
- 恢复时的撞名兜底：任何卡片（标准或父）从垃圾箱恢复时，若名称与现有活跃卡片重名，自动在名称前加下划线前缀；若加一个下划线后仍重名，则继续递增下划线直到不再重名。

---

### 八、技术方向

- 不新建表，在既有 schedule_cards 表上扩展两个字段：
  - kind：取值 standard 或 parent，默认 standard（既有卡片天然为 standard）。
  - parentId：标准卡片指向其所属父卡片的 id，可空；独立标准卡片为空。
- 父卡片是一条 kind = parent 的记录，复用 title 作为父卡片名称、复用 startAt / endAt 作为时间；重要度 / 紧急度 / 分类 / 阶段 / 描述等字段对父卡片不适用；父卡片时间可空（未安排）。
- 尽量复用既有的增删改查、垃圾箱软删与各视图查询；新增接口集中在：组合（建父卡片 / 加入子卡片 / 父卡片并入父卡片）、移出、父卡片名称与时间更新、按父卡片聚合的视图查询、清零消失、以及恢复重建。
- 需一条数据迁移，给历史卡片补齐 kind = standard、parentId 为空。

---

### 九、交互 / 美术 / 视觉设计

延续 Watson 既有设计体系：全部复用现有设计 token（间距 / 圆角 / 阴影 / 字号）、明暗双主题、强调色（浅色蓝 #2563eb、深色 NVIDIA 绿 #76b900）。前提约束：标准卡片上的视觉元素已较密（分类左缘色带、阶段徽章、完成勾选框、悬停顶部窄条、过期红底），本轮不再往标准卡片上继续堆叠；父卡片相关的视觉走「卡片形态差异」与「新容器」，不增加标准卡片的元素负担。

#### 1. 父卡片的外观（区别于标准卡片）

父卡片表达「一叠卡片」的隐喻，一眼可辨。三个手段叠加：

- **堆叠影（核心识别符）**：父卡片底部 / 右下露出 1–2 层「叠在下方的纸」的边——用比卡片背景稍深一档的颜色画 2–3px 的错位描边，制造「一叠」的实体感。这是识别父卡片最主要的符号。
- **计数徽章**：卡片右上角一个 pill 徽章，显示当前子卡片数量（如「3 项」）。用 accent-subtle 作底、强调色作字。徽章只反映当前子卡片数，不做完成进度。
- **不显示标准卡片专有元素**：父卡片不显示重要度 / 紧急度强度条、不显示分类色带、不显示阶段徽章、不显示完成勾选框（父卡片没有这些属性）。这种「更简洁」本身也成为与标准卡片的区分。

父卡片仍显示：名称（占标题位）、时间（起止；无时间时显示「未安排」）。整卡圆角与阴影沿用既有 radius-lg / shadow-md token。

#### 2. 日视图子卡片的折页角标

- 位置与形态：子卡片右下角一个约 16–20px 的三角形「折页」（像便签被折起一角），层级下沉，不遮挡卡片右侧的完成勾选框。
- 配色：折页用强调色，翻起处露出的底面用比卡片背景稍深一档的颜色；明暗主题各定义一套。
- 交互：悬停折页时轻微放大并浮出提示（显示所属父卡片名，如「属于：项目筹备」）；点击折页打开该父卡片详情。

#### 3. 拖拽组合的过程反馈（全部视图）

分四个阶段给出反馈：

1. **拖起**：被拖卡片浮起——加重阴影（shadow-lg）+ 轻微放大（约 1.03）+ 半透明（约 0.9），跟随指针；原位留一个虚线占位框。
2. **悬停目标**：
   - 目标是标准卡片：目标描边高亮为强调色，出现「合并」意象（如目标卡顶部滑入一条强调色条）。
   - 目标是父卡片：父卡片整体高亮，计数徽章预览递增（如 3 → 4），提示「放入此组」。
   - 目标无效（父卡片拖到标准卡片）：目标显示禁止态，不高亮。
3. **松手创建**：确认框以既有 Modal 进出场动效淡入；确认后被合并的卡片向父卡片位置做收拢（fly-to）短动画，强化「被收进去」的感受。
4. **完成**：新父卡片出现处做一次轻微落定弹跳（scale 1 → 1.05 → 1），并以轻量 toast 提示「已创建父卡片：×××」。

移出（在详情页拖出子卡片）同理：拖出时详情页边缘高亮为「释放区」，松手后 toast 提示「已移出：×××」。

所有动效走既有 duration-normal（约 200ms）/ ease-standard，并在 prefers-reduced-motion 下降级为无动画的即时状态切换。

#### 4. 父卡片详情页

沿用既有详情弹层骨架，内容分上下两区：

- **上区**：父卡片名称 + 时间，就地编辑（沿用既有详情页「点击即编辑、确认 / 恢复、点外部关闭、就地红字校验」的交互范式）。时间编辑若违反「只能更宽」约束，就地红字提示，不弹二次确认窗。
- **下区**：子卡片以比正常卡片小一号的迷你卡片网格呈现，按全局创建时间排列。迷你子卡片不带折页角标（此处语境已明确）。点击迷你子卡片可查看 / 编辑该标准卡片；把子卡片拖出下区边界即为移出，边界在拖拽悬停时高亮为虚线「释放区」。

#### 5. 配色与主题

- 不引入新色板：父卡片识别用 accent / accent-subtle；堆叠影用 panel 的深浅变体；折页用强调色。
- 深色主题（绿系）下尤其注意折页与堆叠影的对比度，明暗各自定义一套取值，两个主题都需实测确认可读。

---

## Spec Kit 指令

> 前提：007 已 implement + converge 收口，当前激活 feature 是 `007-*`。跑 `/speckit-specify` 会自动扫描现有编号新建 **008** 并把 `.specify/feature.json` 指针切过去。**开新轮 = 跑 specify。**
> 铁律不变：**specify 只谈「做什么/为什么」，技术栈（字段、migration、拖拽实现、组件方案）留到 plan。**
> ⚠️ 本轮特殊性：引入「父卡片」这一新的卡片形态与卡片间关系，**触后端 + migration（schedule_cards 加 kind/parentId、组合/移出/恢复重建接口）**，是继第四轮后改动较深的一轮；并牵动四视图差异化呈现、拖拽交互、垃圾箱恢复重建、坐标图不受影响等多处既有逻辑。specify/plan/analyze 都要盯住「父卡片可见性模型在各视图的一致性」与「组合拖拽 vs 既有拖到垃圾箱删除的共存」。
> 口径以上面「## 需求与设计逻辑」（一~八）与「### 九、交互/美术/视觉设计」为准。

---

### ② `/speckit-specify` — 写第八轮基线规格

```
/speckit-specify 这是 Watson 的第八轮迭代，命名为「日程卡片组合与父卡片」。在已上线的 001~007 之上，引入「父卡片」机制，用卡片组合解决标准卡片数量过多时的视图冗余问题：在概览类视图（周/月/全部）里把一组卡片折叠成一张父卡片，在专注当天的日视图里仍摊开显示每张子卡片。以下按用户价值描述要做什么，不涉及技术实现。

【卡片模型：两种卡片】
1. 全系统只有两种日程卡片：标准卡片与父卡片。标准卡片即此前的普通卡片，保留全部既有属性，只能通过点击「+」手动创建。父卡片是一个承载多张标准卡片的容器，只能通过在全部视图拖拽标准卡片组合形成，不能手动创建。父卡片只有名称、开始时间、结束时间、已/未安排状态四项属性，不具备重要度/紧急度、分类、阶段等标准卡片专有属性。一张标准卡片同时只能属于一个父卡片；父卡片里只能装标准卡片，不允许多层嵌套。

【组合与移出（仅在全部视图拖拽）】
2. 组合只能在全部视图中拖拽完成，共三条路径：① 标准卡片 A 拖到标准卡片 B，弹出「创建父卡片」确认框，确认后 A、B 组成新父卡片并在全部视图中由父卡片取代；② 标准卡片拖到已有父卡片，进入该父卡片；③ 父卡片 A 拖到父卡片 B，A 的所有子卡片并入 B、A 随之消失。父卡片拖到标准卡片上为无效操作。「创建父卡片」确认框含：必填且不可重名的名称、默认自动汇总（可手改）的时间、即将进入该父卡片的卡片列表、取消按钮。移出：打开父卡片详情后把子卡片拖出详情区即为移出，被移出的卡片恢复为独立卡片重新显示在各视图。

【父卡片的时间与状态】
3. 父卡片时间默认等于所有子卡片时间的包络（开始取最小、结束取最大）。允许手动把时间调得比包络更宽，但不能更窄（父卡片开始必须不晚于所有子卡片最早开始、结束不早于所有子卡片最晚结束）。用户手调过时间后，之后子卡片时间变化时保留手调值，仅当子卡片突破当前父卡片边界时才被动扩展。父卡片内全部子卡片都未安排时，父卡片时间为空。父卡片有已/未安排状态：有时间为已安排、无时间为未安排。父卡片不改变子卡片自身的任何属性，子卡片仍按其自己的时间参与日视图分区与坐标图定位。

【各视图差异化呈现】
4. 设标准卡片 A、B 组成父卡片 D、C 为独立卡片：全部视图显示 D（按其已/未安排进对应分段）、不显示 A、B；周/月视图显示 D（按其时间摆入日历网格）、不显示 A、B（点开 D 才见）；日视图不显示 D，照常显示 A、B（各按自己时间进「今天截止/正在进行/今天开始」三区块），并在子卡片上加一个可点击跳转到父卡片详情的归属标识；垃圾箱不显示 D，从父卡片进入的标准卡片显示时标注其父卡片名称；二维坐标图完全不受本轮影响，照常按各自重要度/紧急度定位标准卡片、不出现父卡片。独立卡片 C 在所有场景一切不变。

【父卡片生命周期】
5. 父卡片没有「删除」和「完成」操作，只能编辑名称/时间、管理子卡片。父卡片通过「清空」而消失：其子卡片经移出、完成或删除而减少，减到 0 张时父卡片直接消失（不进垃圾箱）；剩 1 张子卡片时父卡片仍然存在。子卡片被完成或删除时按既有语义进垃圾箱、同时从父卡片移除，若因此清零则父卡片消失。

【父卡片详情页】
6. 在全部视图点击父卡片、或在日视图点击子卡片上的归属标识，都打开同一个父卡片详情页。详情页可修改父卡片名称（必填、不可重名）与时间（受上述时间约束），并查看、管理子卡片列表（拖出即移出）；子卡片按全局创建时间排列。

【重名与恢复规则】
7. 父卡片名称与标准卡片标题互斥、都不可重名（父卡片名既不能与任何标准卡片标题相同、也不能与其它父卡片名相同）。从垃圾箱恢复曾属于某父卡片的标准卡片时：若同名父卡片仍存在则直接归回现存父卡片，若该父卡片已不存在则自动重建同名父卡片（时间按当前子卡片重新汇总）后归入。任何卡片从垃圾箱恢复时若名称与现有活跃卡片重名，自动在名称前加下划线前缀，仍重名则继续递增下划线直到不再重名。

【交互与视觉】
8. 父卡片以「一叠卡片」的视觉呈现，与标准卡片明显区分：用堆叠影（露出叠在下方的纸边）作为核心识别、右上角计数徽章显示当前子卡片数量、并且不显示标准卡片专有的优先级/分类/阶段/完成等元素。日视图子卡片的归属标识为右下角一个可点击的折页角标。拖拽组合全过程要有清晰反馈：拖起浮动、悬停可组合目标时高亮（悬停父卡片时预览计数递增、无效目标显示禁止态）、松手后被合并卡片向父卡片收拢、完成后轻量成功提示；移出同理。所有动效尊重系统「减少动态效果」偏好。全部沿用既有明暗双主题与强调色，深色主题下需保证堆叠影与折页的对比度可读。

约束与边界：本轮不改变单用户鉴权、AI 不操作日程、标准卡片既有属性与生命周期语义、二维坐标图的既有行为等约束。父卡片是新增的卡片形态，其组合/移出/清零消失/恢复重建必须与既有的完成/删除/垃圾箱软删语义自洽；「一子一母」「两层封顶」「父卡片名与标准标题互斥不可重名」「未安排只进全部视图」等规则需彼此一致。所有改动完成后 001~007 的功能必须逐一回归无损。
```

> 预期它会抛几处 `[NEEDS CLARIFICATION]`，见下面 clarify 一并答。

---

### ③ `/speckit-clarify` — 消歧

```
/speckit-clarify
```

倾向答案（它没问到就主动补，均以上面定案为准）：
- **卡片类型落点**：schedule_cards 加 kind（standard|parent，默认 standard）+ parentId（子卡指向父卡，可空）；父卡片是一条 kind=parent 记录，复用 title/startAt/endAt，其余标准卡专有字段对父卡片不适用。
- **组合入口**：只在全部视图拖拽；三条路径（标准+标准建父、标准入父、父并父）；父拖标准无效。
- **父卡片时间**：默认子卡包络，可手调更宽不可更窄（父.start ≤ min 子.start 且 父.end ≥ max 子.end）；手调后保留、子卡突破边界才被动扩展；全子未安排则父时间空。
- **可见性**：全部/周/月 显示父卡不显子卡；日视图显子卡不显父卡（子卡带折页角标跳父详情）；垃圾箱不显父卡、子卡标注父卡名；坐标图完全不变、不画父卡。
- **生命周期**：父卡无删除/完成按钮；子卡移出/完成/删除减少，清零则父卡直接消失（不进垃圾箱），剩 1 仍在。
- **重名**：父卡名与标准标题互斥不可重名（trim+小写、仅 active）；恢复归入现存父卡或重建同名父卡；撞名递增下划线前缀。
- **详情页**：全部视图点父卡 = 日视图点折页角标 → 同一详情页；可改名/时间（受约束就地校验）、拖出子卡=移出；子卡按创建时间排列。
- **视觉**：父卡=堆叠影+计数徽章（当前子卡数）+ 不显示标准卡专有元素；折页角标在日视图子卡右下角、层级下沉不挡完成框；拖拽四阶段反馈；动效受 reduced-motion。
- **拖拽共存**：组合拖拽（拖到卡片）与既有拖到右上垃圾箱删除并存，靠 drop 目标不同区分。

---

### ④ `/speckit-plan` — 技术方案（此处才引入技术栈）

```
/speckit-plan 在现有技术栈上迭代，沿用既定选型（Fastify + SQLite/Drizzle 后端，React 19 + Vite + Tailwind + TanStack Query 前端，单端口部署）。技术约束与实现方向，并明确标注纯前端 vs 触及后端/DB：

【触及后端 + DB（需 Drizzle migration，注意旧数据兼容）】
- 卡片模型扩展：在 backend/src/db/schema.ts 的 scheduleCards 增加 kind 列（text enum standard|parent，默认 standard）与 parentId 列（text，可空，自引用 schedule_cards.id）；加 parentId 索引。写一条 migration 给历史卡片补 kind=standard、parentId=null（走既有 db/migrate.ts 流程）。同步更新 backend/src/types.ts 与前端 lib/api.ts 的 ScheduleCard 类型。父卡片复用 title(=名称)/startAt/endAt；importance/urgency/categoryId/stage/description 对 parent 存默认或忽略（父卡片 categoryId 非空约束需处理：给一个占位或放宽，plan 定死）。
- 组合/移出/父卡片接口（schedule.service.ts + routes/cards.ts）：新增服务与路由——创建父卡片（由两张标准卡组合，事务内建父卡记录 + 设两子 parentId）、加入子卡片（设 parentId）、父卡并父卡（把 A 的子卡 parentId 改指 B，删 A 父卡记录）、移出（清子卡 parentId）、更新父卡片名称/时间（校验时间 ≥ 子卡包络、名称不可重名）。父卡片时间的自动汇总与「手调后保留、突破才扩展」策略：确定放在后端计算维护（每次子卡增减/改时间时重算或按需重算），plan 定死汇总触发点。
- 清零消失：移出/完成/删除子卡后，若某父卡片 parentId 引用数归零，事务内删除该父卡记录（父卡不进垃圾箱、无 trashedAt）。
- 恢复重建：扩展既有 restore 逻辑——恢复带原父卡名的标准卡时，查同名活跃父卡：存在则设 parentId 归入、不存在则新建父卡记录再归入；恢复任何卡片时若名称撞既有活跃卡片，加下划线前缀（递增直到唯一）。
- 视图查询聚合：GET /api/cards 各 view 的返回需按父卡片可见性重构——all/week/month 返回父卡片记录并隐藏其子卡（子卡不出现在一级）；day 返回子卡片（含 parentId 供前端画折页）不返回父卡；trash 返回被删子卡并带其原父卡名。明确父卡片时间如何参与 week/month 网格与 all 的已/未安排分段。
- 重名唯一：父卡名纳入既有 title_lower 唯一体系（父卡与标准卡共用一套标题唯一，仅 active）。

【纯前端】
- 拖拽组合（AllView.tsx + CardGrid.tsx）：复用/扩展 CardGrid 既有 draggable 基础设施，让卡片既能拖到右上垃圾箱（既有删除，TopBar.tsx 的 drop target 不变）、也能拖到另一张卡片/父卡片（新 drop target）。drop 到标准卡→弹「创建父卡片」确认框（复用 Modal + 就地校验）；drop 到父卡→加入；父拖父→并入；父拖标准→无效态。拖拽四阶段反馈（拖起浮动、悬停高亮/计数预览/禁止态、松手 fly-to 收拢、落定弹跳 + toast）走既有动效 token，受 prefers-reduced-motion 关闭。
- 父卡片外观（卡片组件 + CardGrid）：新增父卡片渲染分支——堆叠影（伪元素错位描边，明暗各一套色）、右上计数徽章（accent-subtle 底 + 强调色，显示子卡数）、不渲染标准卡的优先级条/分类色带/阶段徽章/完成框。父卡片按 kind 判定走此分支。
- 日视图折页角标（DayView.tsx + 卡片组件）：给有 parentId 的子卡右下角渲染折页角标（层级下沉不挡完成框），hover 提示父卡名、点击打开父卡详情；子卡仍照常进 buildDaySections 三区块（按自己时间，不受 parentId 影响）。
- 父卡片详情页（新增组件，复用既有详情弹层骨架）：上区父卡名/时间就地编辑（沿用「点击即编辑、确认/恢复、点外部关闭、就地红字校验」范式，时间违反 ≥子卡包络约束就地红字）；下区迷你子卡网格（比正常卡小一号、无折页角标、按创建时间排列），拖出下区=移出（边界高亮释放区）。全部视图点父卡与日视图点折页角标都打开它。
- 全部视图分段兼容：父卡片按已/未安排落入第七轮已做的「已安排/未安排」两段。
- 周/月视图（WeekView/MonthView + SpanBar）：父卡片按其时间摆入网格，跨天沿用既有 SpanBar 跨列/持续型展示；子卡不出现。
- 坐标图（QuadrantView）：确认不受影响——继续只画标准卡片（含已进父卡的子卡按其自身重/急度），不画父卡。

请产出实现方案，重点标注：kind/parentId 的 migration 与旧数据兼容（含父卡片对 categoryId 非空约束的处理）；父卡片时间自动汇总与「手调保留、突破才扩展」的维护位置与触发点；各 view 查询在父卡片可见性下的返回口径（哪个 view 返回父卡、哪个返回子卡）；清零消失与恢复重建的事务处理；组合拖拽与既有拖到垃圾箱删除的 drop 目标区分；哪些纯前端、哪些触后端（预期后端集中在模型扩展、组合/移出/父卡片接口、视图聚合查询、恢复重建）。
```

---

### ⑤ `/speckit-tasks` — 拆任务

```
/speckit-tasks
```

生成后**人工审阅**：确认任务按「DB & 后端（kind/parentId + migration → 组合/移出/父卡并父卡接口 → 父卡时间汇总维护 → 清零消失 → 恢复重建 → 各 view 聚合查询 + 重名唯一）→ 拖拽组合交互（AllView/CardGrid + 确认框）→ 父卡片外观（堆叠影 + 计数徽章）→ 日视图折页角标 → 父卡片详情页（编辑 + 移出）→ 周/月网格兼容 → 全部视图分段兼容 → 坐标图不受影响验证 → 拖拽四阶段视觉反馈」分组，每条带明确文件路径，并把触及后端/DB 的几块清晰单列标出。

---

### 【动手前】`/speckit-analyze` — 查一致性

```
/speckit-analyze
```

> 只读检查 spec/plan/tasks 三者是否自洽，尤其盯这几组相互牵连处：
> - **可见性模型一致性**：全部/周/月 显父卡隐子卡、日视图显子卡隐父卡、垃圾箱标父卡名、坐标图完全不变——各 view 查询口径与前端渲染是否处处一致、无遗漏视图。
> - **父卡时间约束**：自动汇总 × 「只能更宽」× 「手调保留、突破才扩展」× 全子未安排则空——四条是否彼此自洽、有无矛盾场景。
> - **生命周期闭环**：父卡无删除/完成按钮 × 清零直接消失（不进垃圾箱）× 剩 1 仍在 × 子卡完成删除的连锁——是否讲清。
> - **恢复重建**：归入现存父卡 vs 重建同名父卡 vs 撞名加下划线——三条路径是否覆盖所有情况、无二义。
> - **拖拽共存**：组合拖拽（drop 到卡片）与既有拖到垃圾箱删除（drop 到垃圾箱图标）是否明确区分、互不误触。
> - **一子一母 / 两层封顶 / 名称互斥不可重名**：是否被所有接口一致强制。
> 有越界或矛盾先回改规格，再进 implement。

---

### ⑥ `/speckit-implement` — 分批实现（本轮含后端模型改动，务必分批）

```
/speckit-implement 只执行第 N 阶段的任务，完成后停下等我验证，不要继续后续阶段。
```

> 强烈建议按下面顺序**逐批跑、每批 `npm run build` 双端零错误 + `npm run dev` 手动验证（明暗两主题各看一遍）**再进下一批。前几批动数据模型与查询口径，最容易连带打挂既有功能：
> 1. **DB 模型 + migration**（scheduleCards 加 kind/parentId + 索引 + migration 迁历史值 + 类型同步；先只建模型，确认旧数据与既有各 view 无回归）。
> 2. **后端组合/移出/父卡片接口 + 父卡时间汇总 + 清零消失 + 恢复重建**（最高风险，单独成批，逐个接口用 API 验证：建父卡、加子卡、父并父、移出、改父卡名/时间校验、子卡清零父卡消失、恢复归入/重建/撞名下划线）。
> 3. **各 view 聚合查询口径**（all/week/month 返父卡隐子卡、day 返子卡带 parentId、trash 标父卡名；确认坐标图数据不变）。
> 4. **拖拽组合交互 + 创建父卡片确认框**（AllView/CardGrid 拖到卡片/父卡的 drop 逻辑，与拖到垃圾箱删除共存；确认框名称校验 + 汇总时间 + 取消）。
> 5. **父卡片外观（堆叠影 + 计数徽章）+ 日视图折页角标 + 父卡片详情页（编辑 + 拖出移出）**（前端呈现与详情闭环）。
> 6. **周/月网格兼容 + 全部视图分段兼容 + 拖拽四阶段视觉反馈 + 坐标图不受影响回归**（收尾 + 全链路视觉与动效，明暗各验证，动效受 reduced-motion）。
> 每批完成用「继续执行第 N+1 阶段」推进；靠 tasks.md 的 `[X]` 断点续跑。**尤其第 1~3 批动了模型与查询，务必逐批确认既有 CRUD、垃圾箱、坐标图、各视图零回归。**

---

### 全部做完后 `/speckit-converge` — 兜底查缺

```
/speckit-converge
```

> implement 全跑完后用它对照 spec/plan/tasks 扫缺口，append 成新任务再补齐。converged 即本轮收口。本轮改了卡片模型与可见性，收口后强烈建议**明暗两主题各走一遍端到端回归**：登录 → 全部视图拖两张标准卡建父卡（看确认框、堆叠影、计数徽章）→ 再拖一张标准卡入父卡（计数递增）→ 父卡拖父卡合并 → 打开父卡详情改名/改时间（试越界被拦）→ 详情里拖出子卡（移出）→ 日视图看子卡折页角标并点击进父卡详情、确认父卡不出现在日视图 → 周/月看父卡按时间入网格 → 坐标图确认只画标准卡、父卡不出现、行为同旧 → 完成/删除子卡至清零看父卡消失 → 垃圾箱看子卡标父卡名、恢复触发归入现存父卡/重建同名父卡/撞名加下划线，确认 001~007 功能零回归。

